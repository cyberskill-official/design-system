# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
dist-cdn
storybook-static
packages/*/dist
*.tsbuildinfo

# Editor / OS cruft
.DS_Store
.vite
.idea
.vscode/settings.json
*.swp
*.swo

# Logs & local files
*.log
*.local
.env
.env.*
!.env.example

# Volatile audit caches — regenerated by scripts/check-* on every CI run.
# The DATED snapshots in docs/_audit/{YYYY-MM-DD}/ are the canonical record.
docs/_audit/coverage.json
docs/_audit/bundle-size.json
docs/_audit/doc-freshness.json
docs/_audit/doc-freshness.md
docs/_audit/apca-contrast.json

# Generated AI rules file — regenerate with `pnpm build:design-md`.
# Currently tracked for offline / air-gapped consumers; uncomment to ignore:
# DESIGN.md
