# BRAIN protocol — local agent memory store (per AGENTS.md / CLAUDE.md)

# Audit framework — local working outputs, regenerable from
#   github.com/cyberskill-official/design-system-audit-framework.
# Re-run a SCAN audit any time you need fresh reports.
meta/
_audit/

# Atomic-write temp files (BRAIN protocol §4.4)
.tmp.*.part

# OS junk
.DS_Store
Thumbs.db

# Local-only session handoff (not part of the canonical three files)
HANDOFF.md

# Package manager installs
node_modules/

# Playwright screenshot-regression working outputs (baselines under
# packages/style-packs/__screenshots__/ ARE committed — do not ignore those).
test-results/
playwright-report/
blob-report/
.playwright/
**/__screenshots__/**/*-actual.png
**/__screenshots__/**/*-diff.png

# >>> cyberos (managed block - regenerated by cyberos init; edit outside the markers) >>>
# vendored machine + local BRAIN store + render intermediates (regenerable via init; tenant data)
.cyberos/
# skill symlinks -> .cyberos/plugin/skills (regenerable via init)
.claude/skills/ship-feature-requests
.grok/skills/ship-feature-requests
.commandcode/skills/ship-feature-requests
.codex/skills/ship-feature-requests
.opencode/skill/ship-feature-requests
# <<< cyberos <<<
