# App Store Connect identity — fill when promoting this sample to a real product.
# CI soft-skips without ASC_KEY_ID / ASC_ISSUER_ID / ASC_KEY_P8 (Decision 1C).

app_identifier("world.cyberskill.sample") # CHANGE ME
apple_id(ENV["ASC_APPLE_ID"] || "REPLACE_WITH_APPLE_ID") # CHANGE ME
team_id(ENV["ASC_TEAM_ID"] || "REPLACE_WITH_TEAM_ID") # CHANGE ME

# App Store Connect API key (preferred over Apple ID password):
#   ASC_KEY_ID, ASC_ISSUER_ID, ASC_KEY_P8 (PEM contents or path)
for_platform :ios do
  # itc_team_id ENV["ASC_ITC_TEAM_ID"]
end
