**ButtonGroup** — segmented cluster of Buttons (shared borders, one rounded outline).

```jsx
<ButtonGroup label="View">
  <Button variant="secondary" size="sm">Board</Button>
  <Button variant="secondary" size="sm">List</Button>
</ButtonGroup>
```
