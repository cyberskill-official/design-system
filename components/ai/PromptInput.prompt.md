**PromptInput** — the AI-native "make a wish" box. Enter submits, Shift+Enter newlines; controlled or uncontrolled. Keep the disclosure `hint` (a human reviews clear wishes).

```jsx
<PromptInput value={q} onChange={setQ} onSubmit={send} placeholder="Make a wish…" />
```
