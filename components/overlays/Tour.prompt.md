**Tour** — spotlight walkthrough over selectors (bilingual Skip/Back/Next/Done).

```jsx
<Tour open={on} onClose={()=>setOn(false)} steps={[{target:"#kpis",title:"Portfolio",body:"Filter by clicking a KPI."}]}/>
```
