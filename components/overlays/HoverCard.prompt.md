**HoverCard** — hover/focus preview panel (delayed). Rich content (avatar + text), unlike single-line Tooltip.

```jsx
<HoverCard trigger={<a href="#">@anle</a>}><b>An Lê</b> · Platform lead</HoverCard>
```
