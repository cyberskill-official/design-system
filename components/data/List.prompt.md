**List / ListItem** — vertical list of rows (lead · title/subtitle · trail). `onClick` makes a row an interactive button.

```jsx
<List>
  <ListItem lead={<Avatar name="An Le" size="sm" />} title="An Le" subtitle="Platform" trail={<Badge>Owner</Badge>} />
</List>
```
