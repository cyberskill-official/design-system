**Comment** — discussion entry with nested `replies` (default bilingual "Reply" action).

```jsx
<Comment avatar={<Avatar name="An Le" size="sm"/>} author="An Lê" meta="2h">
  Looks good — ship it.
</Comment>
```
