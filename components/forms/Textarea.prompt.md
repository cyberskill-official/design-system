**Textarea** — multi-line input in the `.cs-field` frame; description + error like TextField, vertically resizable.

```jsx
<Textarea label="Tell us your wish" description="A sentence or two is plenty." rows={4} />
```
