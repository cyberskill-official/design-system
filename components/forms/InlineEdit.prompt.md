**InlineEdit** — click-to-edit text (Enter/blur commits · Escape cancels). Bilingual edit/empty labels.

```jsx
<InlineEdit defaultValue="Design System v1.6" onChange={rename} label="Project name"/>
```
