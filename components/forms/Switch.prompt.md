**Switch** — accessible on/off toggle (`role="switch"`), Umber track when on. For instant-apply settings (use Checkbox in forms that submit).

```jsx
<Switch label="Dark theme" checked={dark} onChange={e => setDark(e.target.checked)} />
```
