**Toast / ToastStack** — transient notification inside a fixed bottom-right stack. Presentational; manage timing + queue in app state.

```jsx
<ToastStack>
  <Toast variant="success" title="Saved" onClose={dismiss}>Your wish is on its way.</Toast>
</ToastStack>
```
