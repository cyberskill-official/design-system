**Skeleton** — shimmer placeholder while content loads. Use `lines` for text, or a `block`/`circle` sized by width/height.

```jsx
<Skeleton variant="circle" width={40} height={40} />
<Skeleton lines={3} />
```
