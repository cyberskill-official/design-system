**EmptyState** — placeholder for empty views: icon, title, message, optional actions.

```jsx
<EmptyState title="No projects yet" actions={<Button>Make a wish</Button>}>
  Start by telling Lumi what you'd like to build.
</EmptyState>
```
