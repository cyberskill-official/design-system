**Badge** — compact status label. Neutral by default; `solid`/`ochre` for emphasis; `success`/`danger`/`warning`/`info` for state; optional leading `dot`.

```jsx
<Badge variant="success" dot>Shipped</Badge>
<Badge variant="ochre">Now</Badge>
```
