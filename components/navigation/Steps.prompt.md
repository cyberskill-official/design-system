**Steps** — horizontal progress stepper. Steps before `current` are done, `current` is active.

```jsx
<Steps current={1} steps={[{title:"Make a wish"},{title:"Shape it"},{title:"Build"},{title:"Last"}]} />
```
