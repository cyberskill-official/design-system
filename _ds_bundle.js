/* @ds-bundle: {"format":4,"namespace":"CyberSkillDesignSystem_847b3c","components":[{"name":"AIDisclosureBadge","sourcePath":"components/ai/AIDisclosureBadge.jsx"},{"name":"ChatMessage","sourcePath":"components/ai/ChatMessage.jsx"},{"name":"CitationList","sourcePath":"components/ai/CitationList.jsx"},{"name":"ConfidenceMeter","sourcePath":"components/ai/ConfidenceMeter.jsx"},{"name":"HumanReviewGate","sourcePath":"components/ai/HumanReviewGate.jsx"},{"name":"PromptInput","sourcePath":"components/ai/PromptInput.jsx"},{"name":"PromptSuggestions","sourcePath":"components/ai/PromptSuggestions.jsx"},{"name":"TypingIndicator","sourcePath":"components/ai/TypingIndicator.jsx"},{"name":"LumiAvatar","sourcePath":"components/brand/LumiAvatar.jsx"},{"name":"Button","sourcePath":"components/button/Button.jsx"},{"name":"Accordion","sourcePath":"components/data/Accordion.jsx"},{"name":"Avatar","sourcePath":"components/data/Avatar.jsx"},{"name":"AvatarGroup","sourcePath":"components/data/Avatar.jsx"},{"name":"Card","sourcePath":"components/data/Card.jsx"},{"name":"CardHeader","sourcePath":"components/data/Card.jsx"},{"name":"CardBody","sourcePath":"components/data/Card.jsx"},{"name":"CardFooter","sourcePath":"components/data/Card.jsx"},{"name":"CodeBlock","sourcePath":"components/data/CodeBlock.jsx"},{"name":"DescriptionList","sourcePath":"components/data/DescriptionList.jsx"},{"name":"Divider","sourcePath":"components/data/Divider.jsx"},{"name":"Kbd","sourcePath":"components/data/Kbd.jsx"},{"name":"List","sourcePath":"components/data/List.jsx"},{"name":"ListItem","sourcePath":"components/data/List.jsx"},{"name":"Stat","sourcePath":"components/data/Stat.jsx"},{"name":"Timeline","sourcePath":"components/data/Timeline.jsx"},{"name":"Tooltip","sourcePath":"components/data/Tooltip.jsx"},{"name":"DataTable","sourcePath":"components/datatable/DataTable.jsx"},{"name":"Dialog","sourcePath":"components/dialog/Dialog.jsx"},{"name":"Alert","sourcePath":"components/feedback/Alert.jsx"},{"name":"Badge","sourcePath":"components/feedback/Badge.jsx"},{"name":"EmptyState","sourcePath":"components/feedback/EmptyState.jsx"},{"name":"ProgressBar","sourcePath":"components/feedback/ProgressBar.jsx"},{"name":"Skeleton","sourcePath":"components/feedback/Skeleton.jsx"},{"name":"Spinner","sourcePath":"components/feedback/Spinner.jsx"},{"name":"StatusIndicator","sourcePath":"components/feedback/StatusIndicator.jsx"},{"name":"Tag","sourcePath":"components/feedback/Tag.jsx"},{"name":"ToastStack","sourcePath":"components/feedback/Toast.jsx"},{"name":"Toast","sourcePath":"components/feedback/Toast.jsx"},{"name":"Checkbox","sourcePath":"components/forms/Checkbox.jsx"},{"name":"FileUpload","sourcePath":"components/forms/FileUpload.jsx"},{"name":"NumberField","sourcePath":"components/forms/NumberField.jsx"},{"name":"Radio","sourcePath":"components/forms/RadioGroup.jsx"},{"name":"RadioGroup","sourcePath":"components/forms/RadioGroup.jsx"},{"name":"SearchField","sourcePath":"components/forms/SearchField.jsx"},{"name":"SegmentedControl","sourcePath":"components/forms/SegmentedControl.jsx"},{"name":"Select","sourcePath":"components/forms/Select.jsx"},{"name":"Slider","sourcePath":"components/forms/Slider.jsx"},{"name":"Switch","sourcePath":"components/forms/Switch.jsx"},{"name":"Textarea","sourcePath":"components/forms/Textarea.jsx"},{"name":"CS_ICONS","sourcePath":"components/icon/Icon.jsx"},{"name":"Icon","sourcePath":"components/icon/Icon.jsx"},{"name":"Logo","sourcePath":"components/logo/Logo.jsx"},{"name":"CS_LOGO_VIEWBOX","sourcePath":"components/logo/logo-data.js"},{"name":"CS_LOGO_MARK_INNER","sourcePath":"components/logo/logo-data.js"},{"name":"Breadcrumb","sourcePath":"components/navigation/Breadcrumb.jsx"},{"name":"CommandPalette","sourcePath":"components/navigation/CommandPalette.jsx"},{"name":"Menu","sourcePath":"components/navigation/Menu.jsx"},{"name":"MenuItem","sourcePath":"components/navigation/Menu.jsx"},{"name":"Pagination","sourcePath":"components/navigation/Pagination.jsx"},{"name":"Sidebar","sourcePath":"components/navigation/Sidebar.jsx"},{"name":"NavItem","sourcePath":"components/navigation/Sidebar.jsx"},{"name":"Steps","sourcePath":"components/navigation/Steps.jsx"},{"name":"Tabs","sourcePath":"components/navigation/Tabs.jsx"},{"name":"Tab","sourcePath":"components/navigation/Tabs.jsx"},{"name":"Drawer","sourcePath":"components/overlays/Drawer.jsx"},{"name":"Popover","sourcePath":"components/overlays/Popover.jsx"},{"name":"TextField","sourcePath":"components/textfield/TextField.jsx"}],"sourceHashes":{"components/ai/AIDisclosureBadge.jsx":"f255b23e0b9b","components/ai/ChatMessage.jsx":"40fcf9e4a353","components/ai/CitationList.jsx":"3770c109bf22","components/ai/ConfidenceMeter.jsx":"ea5603e46f25","components/ai/HumanReviewGate.jsx":"2f85a1bf0889","components/ai/PromptInput.jsx":"188e1b8886fe","components/ai/PromptSuggestions.jsx":"218fe0db5faa","components/ai/TypingIndicator.jsx":"8018f4a104d2","components/brand/LumiAvatar.jsx":"0cd2fea9e02f","components/button/Button.jsx":"ae2a7067b59e","components/data/Accordion.jsx":"298e53237503","components/data/Avatar.jsx":"e1eeed2edcda","components/data/Card.jsx":"9f981091743e","components/data/CodeBlock.jsx":"7b4cd6eda5aa","components/data/DescriptionList.jsx":"089db27a3655","components/data/Divider.jsx":"e3bdcda9b02f","components/data/Kbd.jsx":"5dd1ce54836c","components/data/List.jsx":"830cbdeef110","components/data/Stat.jsx":"09be1d9a8070","components/data/Timeline.jsx":"4b9b8bd1d18d","components/data/Tooltip.jsx":"91e0df1e66ff","components/datatable/DataTable.jsx":"8d73b4d82aa2","components/dialog/Dialog.jsx":"287a8aa7550a","components/feedback/Alert.jsx":"c396e4a9fa0c","components/feedback/Badge.jsx":"e8a638cb173a","components/feedback/EmptyState.jsx":"44140bba6fc8","components/feedback/ProgressBar.jsx":"fd7f03c2756e","components/feedback/Skeleton.jsx":"3d53cc25cedc","components/feedback/Spinner.jsx":"790bac04260a","components/feedback/StatusIndicator.jsx":"3ff007c5d403","components/feedback/Tag.jsx":"e521979dc1ea","components/feedback/Toast.jsx":"42408b7ca669","components/forms/Checkbox.jsx":"4cc1c87fbc08","components/forms/FileUpload.jsx":"e6d978a92d7a","components/forms/NumberField.jsx":"e7fabb7dbfa2","components/forms/RadioGroup.jsx":"fa035fd63b35","components/forms/SearchField.jsx":"4f0f58433c6c","components/forms/SegmentedControl.jsx":"502aef359d3f","components/forms/Select.jsx":"4b1882bc8272","components/forms/Slider.jsx":"942bc9faeeff","components/forms/Switch.jsx":"7c98eef77841","components/forms/Textarea.jsx":"778e30632175","components/icon/Icon.jsx":"379dc6de903e","components/logo/Logo.jsx":"8f3ceaf67af1","components/logo/logo-data.js":"f1a251a50c8f","components/navigation/Breadcrumb.jsx":"d41d7ea07252","components/navigation/CommandPalette.jsx":"f595c68fc28b","components/navigation/Menu.jsx":"53a25edef7eb","components/navigation/Pagination.jsx":"12c1c9d49710","components/navigation/Sidebar.jsx":"6a86252d122d","components/navigation/Steps.jsx":"2c4071f02934","components/navigation/Tabs.jsx":"fa2ca817beaf","components/overlays/Drawer.jsx":"4db36a19ad11","components/overlays/Popover.jsx":"827b7c764c3a","components/textfield/TextField.jsx":"7e26d2493195","image-slot.js":"0394ad34f685","tokens/tokens.js":"85649eff2ac2","ui_kits/status-hub/StatusHub.jsx":"45f04e43c218","ui_kits/status-hub/data.js":"ac58c3167e3c","ui_kits/website/Website.jsx":"c3a0938d9279","ui_kits/website/copy.js":"9d42c52c17cb"},"inlinedExternals":[],"unexposedExports":[{"name":"tokens","sourcePath":"tokens/tokens.js"}]} */

(() => {

const __ds_ns = (window.CyberSkillDesignSystem_847b3c = window.CyberSkillDesignSystem_847b3c || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// components/ai/AIDisclosureBadge.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}
let uid = 0;
function useId(prefix) {
  const [id] = React.useState(() => `${prefix}-${++uid}`);
  return id;
}

/**
 * CyberSkill AIDisclosureBadge — a pill that discloses AI involvement and, when
 * expanded, explains what was generated and lists sources. Never decorative:
 * this is an explainability affordance (info-blue), not a style flourish.
 */
function AIDisclosureBadge({
  label = "AI assisted",
  details = "This content was generated or transformed with AI assistance.",
  sources = [],
  className
}) {
  const [open, setOpen] = React.useState(false);
  const panelId = useId("cs-ai-disclosure");
  const sourceList = (sources || []).filter(Boolean);
  return /*#__PURE__*/React.createElement("span", {
    className: cx("cs-ai-disclosure", className)
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cs-ai-disclosure__badge",
    "aria-expanded": open,
    "aria-controls": panelId,
    onClick: () => setOpen(v => !v)
  }, label), open ? /*#__PURE__*/React.createElement("span", {
    id: panelId,
    role: "status",
    className: "cs-ai-disclosure__panel"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cs-ai-disclosure__details"
  }, details), sourceList.length ? /*#__PURE__*/React.createElement("span", {
    className: "cs-ai-disclosure__sources"
  }, "Sources: ", sourceList.join(", ")) : null) : null);
}
Object.assign(__ds_scope, { AIDisclosureBadge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/AIDisclosureBadge.jsx", error: String((e && e.message) || e) }); }

// components/ai/ChatMessage.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/**
 * CyberSkill ChatMessage — one turn in a Lumi conversation. role "lumi" (genie,
 * Ochre avatar) or "user" (Umber, right-aligned). Pass `avatar` to override.
 */
function ChatMessage({
  role = "lumi",
  name,
  avatar,
  children,
  className
}) {
  const isUser = role === "user";
  const defaultAvatar = isUser ? /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true"
  }, "You") : /*#__PURE__*/React.createElement("svg", {
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.9",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M12 3l1.8 5.4L19 10l-5.2 1.6L12 17l-1.8-5.4L5 10l5.2-1.6z"
  }));
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-chat-msg", isUser ? "cs-chat-msg--user" : "cs-chat-msg--lumi", className)
  }, /*#__PURE__*/React.createElement("div", {
    className: "cs-chat-msg__avatar"
  }, avatar ?? defaultAvatar), /*#__PURE__*/React.createElement("div", {
    className: "cs-chat-msg__col"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cs-chat-msg__name"
  }, name ?? (isUser ? "You" : "Lumi")), /*#__PURE__*/React.createElement("div", {
    className: "cs-chat-msg__bubble"
  }, children)));
}
Object.assign(__ds_scope, { ChatMessage });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/ChatMessage.jsx", error: String((e && e.message) || e) }); }

// components/ai/CitationList.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill CitationList — numbered provenance sources for AI output. Rows link out when href is set. */
function CitationList({
  label = "Sources",
  items = [],
  className
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-citations", className)
  }, label ? /*#__PURE__*/React.createElement("div", {
    className: "cs-citations__label"
  }, label) : null, items.map((it, i) => {
    const inner = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
      className: "cs-citation__num"
    }, i + 1), /*#__PURE__*/React.createElement("span", {
      className: "cs-citation__text"
    }, it.title, it.source ? /*#__PURE__*/React.createElement("span", {
      className: "cs-citation__src"
    }, " \xB7 ", it.source) : null));
    return it.href ? /*#__PURE__*/React.createElement("a", {
      key: i,
      className: "cs-citation",
      href: it.href,
      target: "_blank",
      rel: "noreferrer"
    }, inner) : /*#__PURE__*/React.createElement("div", {
      key: i,
      className: "cs-citation"
    }, inner);
  }));
}
Object.assign(__ds_scope, { CitationList });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/CitationList.jsx", error: String((e && e.message) || e) }); }

// components/ai/ConfidenceMeter.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}
const TONE = {
  low: {
    color: "var(--cs-color-semantic-danger)",
    label: "Low",
    fill: 2
  },
  medium: {
    color: "var(--cs-color-semantic-warning)",
    label: "Medium",
    fill: 3
  },
  high: {
    color: "var(--cs-color-semantic-success)",
    label: "High",
    fill: 5
  }
};

/**
 * CyberSkill ConfidenceMeter — segmented AI-confidence indicator. Pass a numeric
 * `value` 0–1, or a `level` ("low"|"medium"|"high"). Confidence is stated in
 * words too, never colour alone — pair with AIDisclosureBadge / HumanReviewGate.
 */
function ConfidenceMeter({
  value,
  level,
  segments = 5,
  label = "Confidence",
  className
}) {
  let tone, filled;
  if (value != null) {
    tone = value < 0.4 ? "low" : value < 0.75 ? "medium" : "high";
    filled = Math.max(1, Math.round(value * segments));
  } else {
    tone = level || "medium";
    filled = Math.round(TONE[tone].fill / 5 * segments);
  }
  const meta = TONE[tone];
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-confidence", className)
  }, /*#__PURE__*/React.createElement("div", {
    className: "cs-confidence__head"
  }, /*#__PURE__*/React.createElement("span", null, label), /*#__PURE__*/React.createElement("span", {
    className: "cs-confidence__level",
    style: {
      color: meta.color
    }
  }, meta.label)), /*#__PURE__*/React.createElement("div", {
    className: "cs-confidence__track",
    role: "meter",
    "aria-valuemin": 0,
    "aria-valuemax": segments,
    "aria-valuenow": filled,
    "aria-label": label + ": " + meta.label
  }, Array.from({
    length: segments
  }).map((_, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    className: "cs-confidence__seg",
    style: i < filled ? {
      background: meta.color
    } : undefined
  }))));
}
Object.assign(__ds_scope, { ConfidenceMeter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/ConfidenceMeter.jsx", error: String((e && e.message) || e) }); }

// components/ai/HumanReviewGate.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/**
 * CyberSkill HumanReviewGate — an AI-native checkpoint: a risk label, a
 * plain-language summary, an optional reviewer, and Approve / Reject actions.
 * Keeps a human in the loop before a high-stakes AI action proceeds.
 */
function HumanReviewGate({
  risk = "review required",
  summary,
  reviewer,
  onApprove,
  onReject,
  approveLabel = "Approve",
  rejectLabel = "Reject",
  className
}) {
  return /*#__PURE__*/React.createElement("section", {
    className: cx("cs-review-gate", className),
    "aria-label": "Human review gate"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cs-review-gate__risk"
  }, risk), /*#__PURE__*/React.createElement("p", {
    className: "cs-review-gate__summary"
  }, summary), reviewer ? /*#__PURE__*/React.createElement("p", {
    className: "cs-review-gate__reviewer"
  }, "Reviewer: ", reviewer) : null, /*#__PURE__*/React.createElement("div", {
    className: "cs-review-gate__actions"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cs-button cs-button--secondary",
    onClick: onReject
  }, rejectLabel), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cs-button cs-button--primary",
    onClick: onApprove
  }, approveLabel)));
}
Object.assign(__ds_scope, { HumanReviewGate });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/HumanReviewGate.jsx", error: String((e && e.message) || e) }); }

// components/ai/PromptInput.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/**
 * CyberSkill PromptInput — the AI-native ask box. Enter submits (Shift+Enter =
 * newline). Works controlled (value/onChange) or uncontrolled. The hint states
 * that Lumi replies, then hands clear wishes to a human — keep that disclosure.
 */
function PromptInput({
  value,
  onChange,
  onSubmit,
  placeholder = "Type your wish…",
  sendLabel = "Send",
  hint = "Lumi replies, then hands clear wishes to a human.",
  disabled = false,
  busy = false,
  className
}) {
  const [inner, setInner] = React.useState("");
  const val = value != null ? value : inner;
  const setVal = v => onChange ? onChange(v) : setInner(v);
  const submit = () => {
    if (!disabled && !busy && String(val).trim()) onSubmit && onSubmit(val);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-prompt", className)
  }, /*#__PURE__*/React.createElement("textarea", {
    className: "cs-prompt__field",
    rows: 1,
    value: val,
    placeholder: placeholder,
    disabled: disabled,
    onChange: e => setVal(e.target.value),
    onKeyDown: e => {
      if (e.key === "Enter" && !e.shiftKey) {
        e.preventDefault();
        submit();
      }
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "cs-prompt__bar"
  }, hint ? /*#__PURE__*/React.createElement("span", {
    className: "cs-prompt__hint"
  }, /*#__PURE__*/React.createElement("svg", {
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M12 3l1.8 5.4L19 10l-5.2 1.6L12 17l-1.8-5.4L5 10l5.2-1.6z"
  })), hint) : null, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cs-button cs-button--primary cs-button--sm",
    onClick: submit,
    disabled: disabled || busy
  }, busy ? /*#__PURE__*/React.createElement("span", {
    className: "cs-button__spinner",
    "aria-hidden": "true"
  }) : null, sendLabel)));
}
Object.assign(__ds_scope, { PromptInput });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/PromptInput.jsx", error: String((e && e.message) || e) }); }

// components/ai/PromptSuggestions.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill PromptSuggestions — tappable "wish" starter chips for a chat/prompt. */
function PromptSuggestions({
  suggestions = [],
  onSelect,
  className
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-suggest", className)
  }, suggestions.map((s, i) => {
    const label = typeof s === "string" ? s : s.label;
    const icon = typeof s === "string" ? null : s.icon;
    return /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      onClick: () => onSelect && onSelect(label)
    }, icon ?? /*#__PURE__*/React.createElement("svg", {
      width: "14",
      height: "14",
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeWidth: "1.8",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      "aria-hidden": "true"
    }, /*#__PURE__*/React.createElement("path", {
      d: "M12 3l1.8 5.4L19 10l-5.2 1.6L12 17l-1.8-5.4L5 10l5.2-1.6z"
    })), label);
  }));
}
Object.assign(__ds_scope, { PromptSuggestions });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/PromptSuggestions.jsx", error: String((e && e.message) || e) }); }

// components/ai/TypingIndicator.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill TypingIndicator — animated dots for when Lumi is composing a reply. */
function TypingIndicator({
  label = "Lumi is typing",
  className
}) {
  return /*#__PURE__*/React.createElement("span", {
    className: cx("cs-typing", className),
    role: "status",
    "aria-label": label
  }, /*#__PURE__*/React.createElement("span", null), /*#__PURE__*/React.createElement("span", null), /*#__PURE__*/React.createElement("span", null));
}
Object.assign(__ds_scope, { TypingIndicator });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/ai/TypingIndicator.jsx", error: String((e && e.message) || e) }); }

// components/brand/LumiAvatar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill LumiAvatar — the golden-genie mascot avatar. Pass `src` (e.g. lumi-poster.webp) or fall back to the ✦ glyph. */
function LumiAvatar({
  src,
  size = "md",
  ring = false,
  alt = "Lumi",
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cx("cs-lumi", `cs-lumi--${size}`, ring && "cs-lumi--ring", className)
  }, props), src ? /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: alt
  }) : /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true"
  }, "\u2726"));
}
Object.assign(__ds_scope, { LumiAvatar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/brand/LumiAvatar.jsx", error: String((e && e.message) || e) }); }

// components/button/Button.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/**
 * CyberSkill Button. Umber primary by default; warm hover; Ochre focus ring;
 * 44px comfortable touch target. Renders a real <button>.
 */
function Button({
  variant = "primary",
  size = "md",
  loading = false,
  disabled = false,
  fullWidth = false,
  icon,
  children,
  className,
  type = "button",
  ...props
}) {
  const isDisabled = disabled || loading;
  return /*#__PURE__*/React.createElement("button", _extends({}, props, {
    type: type,
    disabled: isDisabled,
    "aria-busy": loading || undefined,
    className: cx("cs-button", `cs-button--${variant}`, `cs-button--${size}`, fullWidth && "cs-button--full", isDisabled && "is-disabled", loading && "is-loading", className)
  }), icon ? /*#__PURE__*/React.createElement("span", {
    className: "cs-button__icon",
    "aria-hidden": "true"
  }, icon) : null, /*#__PURE__*/React.createElement("span", {
    className: "cs-button__label"
  }, children), loading ? /*#__PURE__*/React.createElement("span", {
    className: "cs-button__spinner",
    "aria-hidden": "true"
  }) : null);
}
Object.assign(__ds_scope, { Button });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/button/Button.jsx", error: String((e && e.message) || e) }); }

// components/data/Accordion.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Accordion — collapsible sections. Single-open by default; allowMultiple for many. */
function Accordion({
  items = [],
  defaultOpen = 0,
  allowMultiple = false,
  className
}) {
  const [open, setOpen] = React.useState(() => allowMultiple ? defaultOpen != null ? [defaultOpen] : [] : defaultOpen);
  const isOpen = i => allowMultiple ? open.includes(i) : open === i;
  const toggle = i => {
    if (allowMultiple) setOpen(o => o.includes(i) ? o.filter(x => x !== i) : [...o, i]);else setOpen(o => o === i ? -1 : i);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-accordion", className)
  }, items.map((it, i) => /*#__PURE__*/React.createElement("div", {
    className: "cs-accordion__item",
    key: i
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cs-accordion__trigger",
    "aria-expanded": isOpen(i),
    onClick: () => toggle(i)
  }, it.title, /*#__PURE__*/React.createElement("span", {
    className: "cs-accordion__chevron",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("svg", {
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 9l6 6 6-6"
  })))), isOpen(i) ? /*#__PURE__*/React.createElement("div", {
    className: "cs-accordion__panel"
  }, it.content) : null)));
}
Object.assign(__ds_scope, { Accordion });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Accordion.jsx", error: String((e && e.message) || e) }); }

// components/data/Avatar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Avatar — image or initials on Umber. sizes sm|md|lg; square variant. */
function Avatar({
  src,
  name = "",
  size = "md",
  square = false,
  className,
  ...props
}) {
  const initials = name ? name.trim().split(/\s+/).map(w => w[0]).slice(0, 2).join("").toUpperCase() : "";
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cx("cs-avatar", `cs-avatar--${size}`, square && "cs-avatar--square", className),
    title: name || undefined
  }, props), src ? /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: name
  }) : /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true"
  }, initials));
}

/** Overlapping row of Avatars. */
function AvatarGroup({
  className,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-avatar-group", className)
  }, children);
}
Object.assign(__ds_scope, { Avatar, AvatarGroup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Avatar.jsx", error: String((e && e.message) || e) }); }

// components/data/Card.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Card — warm panel (14px radius, soft shadow). Compose with CardHeader/Body/Footer. */
function Card({
  interactive = false,
  flat = false,
  as,
  className,
  children,
  ...props
}) {
  const Tag = as || (interactive ? "button" : "div");
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("cs-card", flat && "cs-card--flat", interactive && "cs-card--interactive", className)
  }, props), children);
}
function CardHeader({
  title,
  subtitle,
  children,
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("cs-card__header", className)
  }, props), title ? /*#__PURE__*/React.createElement("h3", {
    className: "cs-card__title"
  }, title) : null, subtitle ? /*#__PURE__*/React.createElement("p", {
    className: "cs-card__subtitle"
  }, subtitle) : null, children);
}
function CardBody({
  className,
  children,
  ...props
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("cs-card__body", className)
  }, props), children);
}
function CardFooter({
  className,
  children,
  ...props
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("cs-card__footer", className)
  }, props), children);
}
Object.assign(__ds_scope, { Card, CardHeader, CardBody, CardFooter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Card.jsx", error: String((e && e.message) || e) }); }

// components/data/CodeBlock.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill CodeBlock — dark mono code panel with a filename bar + copy button. */
function CodeBlock({
  code = "",
  filename,
  language = "code",
  showBar = true,
  className
}) {
  const [copied, setCopied] = React.useState(false);
  const copy = () => {
    try {
      navigator.clipboard.writeText(code);
      setCopied(true);
      setTimeout(() => setCopied(false), 1400);
    } catch (e) {/* no-op */}
  };
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-code", className)
  }, showBar ? /*#__PURE__*/React.createElement("div", {
    className: "cs-code__bar"
  }, /*#__PURE__*/React.createElement("span", null, filename || language), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cs-code__copy",
    onClick: copy
  }, copied ? "Copied ✓" : "Copy")) : null, /*#__PURE__*/React.createElement("pre", null, /*#__PURE__*/React.createElement("code", null, code)));
}
Object.assign(__ds_scope, { CodeBlock });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/CodeBlock.jsx", error: String((e && e.message) || e) }); }

// components/data/DescriptionList.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill DescriptionList — term/value metadata grid. */
function DescriptionList({
  items = [],
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("dl", _extends({
    className: cx("cs-dl", className)
  }, props), items.map((it, i) => /*#__PURE__*/React.createElement("div", {
    key: i
  }, /*#__PURE__*/React.createElement("dt", null, it.term), /*#__PURE__*/React.createElement("dd", null, it.value))));
}
Object.assign(__ds_scope, { DescriptionList });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/DescriptionList.jsx", error: String((e && e.message) || e) }); }

// components/data/Divider.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Divider — hairline rule. `label` centres text; `vertical` for inline splits. */
function Divider({
  vertical = false,
  label,
  className,
  children,
  ...props
}) {
  if (label) {
    return /*#__PURE__*/React.createElement("div", _extends({
      className: cx("cs-divider", "cs-divider--label", className),
      role: "separator"
    }, props), label);
  }
  return /*#__PURE__*/React.createElement("hr", _extends({
    className: cx("cs-divider", vertical && "cs-divider--vertical", className),
    "aria-orientation": vertical ? "vertical" : "horizontal"
  }, props));
}
Object.assign(__ds_scope, { Divider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Divider.jsx", error: String((e && e.message) || e) }); }

// components/data/Kbd.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Kbd — keyboard key hint. */
function Kbd({
  children,
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("kbd", _extends({
    className: cx("cs-kbd", className)
  }, props), children);
}
Object.assign(__ds_scope, { Kbd });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Kbd.jsx", error: String((e && e.message) || e) }); }

// components/data/List.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill List container. Compose ListItem children. */
function List({
  className,
  children,
  ...props
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    role: "list",
    className: cx("cs-list", className)
  }, props), children);
}

/** A row in a List: optional lead (icon/avatar), title, subtitle, trailing node. */
function ListItem({
  lead,
  title,
  subtitle,
  trail,
  onClick,
  children,
  className,
  ...props
}) {
  const interactive = !!onClick;
  const Tag = interactive ? "button" : "div";
  return /*#__PURE__*/React.createElement(Tag, _extends({
    role: "listitem",
    className: cx("cs-list__item", interactive && "cs-list__item--button", className),
    onClick: onClick
  }, props), lead != null ? /*#__PURE__*/React.createElement("span", {
    className: "cs-list__lead"
  }, lead) : null, /*#__PURE__*/React.createElement("span", {
    className: "cs-list__main"
  }, title != null ? /*#__PURE__*/React.createElement("span", {
    className: "cs-list__title"
  }, title) : null, subtitle != null ? /*#__PURE__*/React.createElement("span", {
    className: "cs-list__sub"
  }, subtitle) : null, children), trail != null ? /*#__PURE__*/React.createElement("span", {
    className: "cs-list__trail"
  }, trail) : null);
}
Object.assign(__ds_scope, { List, ListItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/List.jsx", error: String((e && e.message) || e) }); }

// components/data/Stat.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Stat — labelled metric with optional delta. trend: up | down | flat. */
function Stat({
  label,
  value,
  delta,
  trend = "flat",
  className,
  ...props
}) {
  const arrow = trend === "up" ? "M12 5v14M6 11l6-6 6 6" : trend === "down" ? "M12 5v14M6 13l6 6 6-6" : "M5 12h14";
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("cs-stat", className)
  }, props), /*#__PURE__*/React.createElement("div", {
    className: "cs-stat__label"
  }, label), /*#__PURE__*/React.createElement("div", {
    className: "cs-stat__value"
  }, value), delta != null ? /*#__PURE__*/React.createElement("div", {
    className: cx("cs-stat__delta", `cs-stat__delta--${trend}`)
  }, /*#__PURE__*/React.createElement("svg", {
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.4",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("path", {
    d: arrow
  })), delta) : null);
}
Object.assign(__ds_scope, { Stat });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Stat.jsx", error: String((e && e.message) || e) }); }

// components/data/Timeline.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Timeline — vertical event trail. item.state: "done" (default) | "now" | "todo". */
function Timeline({
  items = [],
  className
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-timeline", className)
  }, items.map((it, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: cx("cs-timeline__item", it.state === "now" && "cs-timeline__item--now", it.state === "todo" && "cs-timeline__item--todo")
  }, /*#__PURE__*/React.createElement("span", {
    className: "cs-timeline__marker",
    "aria-hidden": "true"
  }, it.state === "todo" ? "" : it.state === "now" ? "→" : "✓"), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "cs-timeline__title"
  }, it.title), it.meta ? /*#__PURE__*/React.createElement("div", {
    className: "cs-timeline__meta"
  }, it.meta) : null, it.body ? /*#__PURE__*/React.createElement("div", {
    className: "cs-timeline__body"
  }, it.body) : null))));
}
Object.assign(__ds_scope, { Timeline });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Timeline.jsx", error: String((e && e.message) || e) }); }

// components/data/Tooltip.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Tooltip — hover/focus bubble above the trigger. Keep label short. */
function Tooltip({
  label,
  children,
  className
}) {
  return /*#__PURE__*/React.createElement("span", {
    className: cx("cs-tooltip", className)
  }, children, /*#__PURE__*/React.createElement("span", {
    className: "cs-tooltip__bubble",
    role: "tooltip"
  }, label));
}
Object.assign(__ds_scope, { Tooltip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Tooltip.jsx", error: String((e && e.message) || e) }); }

// components/datatable/DataTable.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/**
 * CyberSkill DataTable — semantic table with caption, column scope headers,
 * per-column render, and an empty state. Solid surface (dense content).
 */
function DataTable({
  caption,
  columns,
  rows,
  rowKey = "id",
  emptyState = "No records",
  className
}) {
  const normalized = Array.isArray(rows) ? rows : [];
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-table-wrap", className)
  }, /*#__PURE__*/React.createElement("table", {
    className: "cs-table"
  }, caption ? /*#__PURE__*/React.createElement("caption", null, caption) : null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map(c => /*#__PURE__*/React.createElement("th", {
    key: c.key,
    scope: "col"
  }, c.header)))), /*#__PURE__*/React.createElement("tbody", null, normalized.length === 0 ? /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", {
    colSpan: columns.length,
    className: "cs-table__empty"
  }, emptyState)) : normalized.map((row, i) => /*#__PURE__*/React.createElement("tr", {
    key: row[rowKey] ?? i
  }, columns.map(c => /*#__PURE__*/React.createElement("td", {
    key: c.key
  }, c.render ? c.render(row) : row[c.key])))))));
}
Object.assign(__ds_scope, { DataTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/datatable/DataTable.jsx", error: String((e && e.message) || e) }); }

// components/dialog/Dialog.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}
let uid = 0;
function useId(prefix) {
  const [id] = React.useState(() => `${prefix}-${++uid}`);
  return id;
}

/**
 * CyberSkill Dialog — modal overlay + panel. aria-modal, labelled by title,
 * scrim click closes. Compose actions in the footer with Buttons.
 */
function Dialog({
  open,
  title,
  children,
  actions,
  onClose,
  className,
  closeLabel = "Close",
  ...props
}) {
  const titleId = useId("cs-dialog") + "-title";
  if (!open) return null;
  return /*#__PURE__*/React.createElement("div", {
    className: "cs-dialog-layer"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cs-dialog__overlay",
    onClick: onClose,
    "aria-hidden": "true"
  }), /*#__PURE__*/React.createElement("section", _extends({}, props, {
    role: "dialog",
    "aria-modal": "true",
    "aria-labelledby": titleId,
    className: cx("cs-dialog", className)
  }), /*#__PURE__*/React.createElement("header", {
    className: "cs-dialog__header"
  }, /*#__PURE__*/React.createElement("h2", {
    id: titleId,
    className: "cs-dialog__title"
  }, title), onClose ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cs-button cs-button--ghost cs-button--sm",
    onClick: onClose,
    "aria-label": closeLabel
  }, "\u2715") : null), /*#__PURE__*/React.createElement("div", {
    className: "cs-dialog__body"
  }, children), actions ? /*#__PURE__*/React.createElement("footer", {
    className: "cs-dialog__actions"
  }, actions) : null));
}
Object.assign(__ds_scope, { Dialog });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dialog/Dialog.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Alert.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}
const PATHS = {
  info: "M12 8h.01M11 12h1v4h1 M12 3a9 9 0 1 0 0 18 9 9 0 0 0 0-18z",
  success: "M4 12.5l5 5 11-11",
  warning: "M12 3l9 16H3z M12 10v4 M12 17h.01",
  danger: "M12 3a9 9 0 1 0 0 18 9 9 0 0 0 0-18z M9 9l6 6 M15 9l-6 6"
};
function DefaultIcon({
  variant
}) {
  return /*#__PURE__*/React.createElement("svg", {
    width: "20",
    height: "20",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.9",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("path", {
    d: PATHS[variant] || PATHS.info
  }));
}

/** CyberSkill Alert — inline banner. variant: info | success | warning | danger. */
function Alert({
  variant = "info",
  title,
  icon,
  children,
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    role: "status",
    className: cx("cs-alert", `cs-alert--${variant}`, className)
  }, props), /*#__PURE__*/React.createElement("span", {
    className: "cs-alert__icon"
  }, icon ?? /*#__PURE__*/React.createElement(DefaultIcon, {
    variant: variant
  })), /*#__PURE__*/React.createElement("div", null, title ? /*#__PURE__*/React.createElement("p", {
    className: "cs-alert__title"
  }, title) : null, children ? /*#__PURE__*/React.createElement("div", {
    className: "cs-alert__body"
  }, children) : null));
}
Object.assign(__ds_scope, { Alert });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Alert.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Badge.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Badge — compact status label. variant: neutral | solid | ochre | success | danger | warning | info. */
function Badge({
  variant = "neutral",
  dot = false,
  children,
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cx("cs-badge", variant !== "neutral" && `cs-badge--${variant}`, className)
  }, props), dot ? /*#__PURE__*/React.createElement("span", {
    className: "cs-badge__dot",
    "aria-hidden": "true"
  }) : null, children);
}
Object.assign(__ds_scope, { Badge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Badge.jsx", error: String((e && e.message) || e) }); }

// components/feedback/EmptyState.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill EmptyState — friendly placeholder for empty views. */
function EmptyState({
  icon,
  title,
  children,
  actions,
  className
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-empty", className)
  }, /*#__PURE__*/React.createElement("span", {
    className: "cs-empty__icon"
  }, icon ?? /*#__PURE__*/React.createElement("svg", {
    width: "26",
    height: "26",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M3 8l2-4h14l2 4M3 8v10a1 1 0 0 0 1 1h16a1 1 0 0 0 1-1V8M3 8h6l1 3h4l1-3h6"
  }))), title ? /*#__PURE__*/React.createElement("div", {
    className: "cs-empty__title"
  }, title) : null, children ? /*#__PURE__*/React.createElement("div", {
    className: "cs-empty__body"
  }, children) : null, actions ? /*#__PURE__*/React.createElement("div", {
    className: "cs-empty__actions"
  }, actions) : null);
}
Object.assign(__ds_scope, { EmptyState });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/EmptyState.jsx", error: String((e && e.message) || e) }); }

// components/feedback/ProgressBar.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill ProgressBar — determinate progress. variant: ochre (default) | umber | success. */
function ProgressBar({
  value = 0,
  max = 100,
  variant,
  label,
  className
}) {
  const pct = Math.max(0, Math.min(100, value / max * 100));
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-progress", variant && `cs-progress--${variant}`, className),
    role: "progressbar",
    "aria-valuenow": Math.round(value),
    "aria-valuemin": 0,
    "aria-valuemax": max,
    "aria-label": label
  }, /*#__PURE__*/React.createElement("span", {
    className: "cs-progress__fill",
    style: {
      inlineSize: pct + "%",
      width: pct + "%"
    }
  }));
}
Object.assign(__ds_scope, { ProgressBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/ProgressBar.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Skeleton.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Skeleton — shimmer placeholder. Use `lines` for text blocks, or variant="circle"/"block". */
function Skeleton({
  variant = "block",
  width,
  height,
  lines,
  radius,
  className,
  style
}) {
  if (lines) {
    return /*#__PURE__*/React.createElement("div", {
      className: className,
      "aria-hidden": "true",
      role: "presentation"
    }, Array.from({
      length: lines
    }).map((_, i) => /*#__PURE__*/React.createElement("span", {
      key: i,
      className: "cs-skeleton cs-skeleton--text",
      style: {
        width: i === lines - 1 ? "70%" : "100%"
      }
    })));
  }
  return /*#__PURE__*/React.createElement("span", {
    className: cx("cs-skeleton", variant === "circle" && "cs-skeleton--circle", className),
    "aria-hidden": "true",
    style: {
      display: "block",
      width,
      height,
      borderRadius: radius,
      ...style
    }
  });
}
Object.assign(__ds_scope, { Skeleton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Skeleton.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Spinner.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Spinner — indeterminate loading indicator. */
function Spinner({
  size = 20,
  label = "Loading",
  className,
  style,
  ...props
}) {
  return /*#__PURE__*/React.createElement("span", _extends({
    role: "status",
    "aria-label": label,
    className: cx("cs-spinner", className),
    style: {
      inlineSize: size,
      blockSize: size,
      width: size,
      height: size,
      ...style
    }
  }, props));
}
Object.assign(__ds_scope, { Spinner });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Spinner.jsx", error: String((e && e.message) || e) }); }

// components/feedback/StatusIndicator.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill StatusIndicator — coloured dot + label. status: online | busy | offline | error. */
function StatusIndicator({
  status = "offline",
  pulse = false,
  children,
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cx("cs-status", `cs-status--${status}`, pulse && "cs-status--pulse", className)
  }, props), /*#__PURE__*/React.createElement("span", {
    className: "cs-status__dot",
    "aria-hidden": "true"
  }), children);
}
Object.assign(__ds_scope, { StatusIndicator });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/StatusIndicator.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Tag.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Tag — chip with optional remove button. */
function Tag({
  children,
  onRemove,
  removeLabel = "Remove",
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cx("cs-tag", className)
  }, props), children, onRemove ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cs-tag__close",
    "aria-label": removeLabel,
    onClick: onRemove
  }, /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.4",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 6l12 12M18 6L6 18"
  }))) : null);
}
Object.assign(__ds_scope, { Tag });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Tag.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Toast.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** Fixed-position stack for Toasts (bottom-right). */
function ToastStack({
  children,
  className
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-toast-stack", className),
    role: "region",
    "aria-label": "Notifications"
  }, children);
}

/** CyberSkill Toast — transient notification. variant: default | success | danger. */
function Toast({
  variant = "default",
  title,
  icon,
  onClose,
  children,
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cx("cs-toast", `cs-toast--${variant}`, className),
    role: "status"
  }, props), /*#__PURE__*/React.createElement("span", {
    className: "cs-toast__icon",
    "aria-hidden": "true"
  }, icon ?? /*#__PURE__*/React.createElement("svg", {
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M4 12.5l5 5 11-11"
  }))), /*#__PURE__*/React.createElement("div", null, title ? /*#__PURE__*/React.createElement("div", {
    className: "cs-toast__title"
  }, title) : null, children ? /*#__PURE__*/React.createElement("div", {
    className: "cs-toast__body"
  }, children) : null), onClose ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cs-toast__close",
    "aria-label": "Dismiss",
    onClick: onClose
  }, /*#__PURE__*/React.createElement("svg", {
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 6l12 12M18 6L6 18"
  }))) : null);
}
Object.assign(__ds_scope, { ToastStack, Toast });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Toast.jsx", error: String((e && e.message) || e) }); }

// components/forms/Checkbox.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Checkbox — native checkbox (brand accent-color) + label/description. */
function Checkbox({
  label,
  description,
  disabled = false,
  className,
  children,
  ...props
}) {
  return /*#__PURE__*/React.createElement("label", {
    className: cx("cs-check", disabled && "is-disabled", className)
  }, /*#__PURE__*/React.createElement("input", _extends({
    type: "checkbox",
    disabled: disabled
  }, props)), /*#__PURE__*/React.createElement("span", {
    className: "cs-check__text"
  }, /*#__PURE__*/React.createElement("span", null, label), description ? /*#__PURE__*/React.createElement("span", {
    className: "cs-check__desc"
  }, description) : null));
}
Object.assign(__ds_scope, { Checkbox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Checkbox.jsx", error: String((e && e.message) || e) }); }

// components/forms/FileUpload.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill FileUpload — click-or-drag dropzone. Calls onFiles(File[]) on select/drop. */
function FileUpload({
  title = "Drop files here or browse",
  hint = "PNG, JPG, or PDF up to 10MB",
  accept,
  multiple = false,
  onFiles,
  icon,
  className
}) {
  const [drag, setDrag] = React.useState(false);
  const pick = files => {
    if (files && files.length && onFiles) onFiles(Array.from(files));
  };
  return /*#__PURE__*/React.createElement("label", {
    className: cx("cs-dropzone", drag && "is-dragging", className),
    onDragOver: e => {
      e.preventDefault();
      setDrag(true);
    },
    onDragLeave: () => setDrag(false),
    onDrop: e => {
      e.preventDefault();
      setDrag(false);
      pick(e.dataTransfer.files);
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "cs-dropzone__icon"
  }, icon ?? /*#__PURE__*/React.createElement("svg", {
    width: "28",
    height: "28",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M12 16V4M7 9l5-5 5 5"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M5 16v3a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1v-3"
  }))), /*#__PURE__*/React.createElement("span", {
    className: "cs-dropzone__title"
  }, title), /*#__PURE__*/React.createElement("span", {
    className: "cs-dropzone__hint"
  }, hint), /*#__PURE__*/React.createElement("input", {
    type: "file",
    accept: accept,
    multiple: multiple,
    style: {
      display: "none"
    },
    onChange: e => pick(e.target.files)
  }));
}
Object.assign(__ds_scope, { FileUpload });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/FileUpload.jsx", error: String((e && e.message) || e) }); }

// components/forms/NumberField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill NumberField — numeric stepper with −/+ buttons and clamping. */
function NumberField({
  value,
  onChange,
  min,
  max,
  step = 1,
  disabled = false,
  className,
  children,
  ...props
}) {
  const [inner, setInner] = React.useState(0);
  const val = value != null ? value : inner;
  const clamp = n => {
    if (min != null) n = Math.max(min, n);
    if (max != null) n = Math.min(max, n);
    return n;
  };
  const set = n => {
    const c = clamp(n);
    onChange ? onChange(c) : setInner(c);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-stepper", className)
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Decrease",
    disabled: disabled || min != null && val <= min,
    onClick: () => set(val - step)
  }, "\u2212"), /*#__PURE__*/React.createElement("input", _extends({
    type: "number",
    value: val,
    min: min,
    max: max,
    step: step,
    disabled: disabled,
    onChange: e => set(Number(e.target.value))
  }, props)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Increase",
    disabled: disabled || max != null && val >= max,
    onClick: () => set(val + step)
  }, "+"));
}
Object.assign(__ds_scope, { NumberField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/NumberField.jsx", error: String((e && e.message) || e) }); }

// components/forms/RadioGroup.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** A single labelled radio (brand accent-color). Usually composed via RadioGroup. */
function Radio({
  label,
  description,
  disabled = false,
  className,
  children,
  ...props
}) {
  return /*#__PURE__*/React.createElement("label", {
    className: cx("cs-radio", disabled && "is-disabled", className)
  }, /*#__PURE__*/React.createElement("input", _extends({
    type: "radio",
    disabled: disabled
  }, props)), /*#__PURE__*/React.createElement("span", {
    className: "cs-radio__text"
  }, /*#__PURE__*/React.createElement("span", null, label), description ? /*#__PURE__*/React.createElement("span", {
    className: "cs-radio__desc"
  }, description) : null));
}

/** CyberSkill RadioGroup — a fieldset of radios from `options`, controlled by value/onChange. */
function RadioGroup({
  legend,
  name,
  value,
  onChange,
  options = [],
  className
}) {
  const gid = React.useId();
  const nm = name ?? gid;
  return /*#__PURE__*/React.createElement("fieldset", {
    className: cx("cs-radio-group", className)
  }, legend ? /*#__PURE__*/React.createElement("legend", null, legend) : null, options.map(o => /*#__PURE__*/React.createElement(Radio, {
    key: o.value,
    name: nm,
    value: o.value,
    label: o.label,
    description: o.description,
    disabled: o.disabled,
    checked: value === o.value,
    onChange: () => onChange && onChange(o.value)
  })));
}
Object.assign(__ds_scope, { Radio, RadioGroup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/RadioGroup.jsx", error: String((e && e.message) || e) }); }

// components/forms/SearchField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill SearchField — pill search input with icon + clear. Controlled or uncontrolled. */
function SearchField({
  value,
  onChange,
  onClear,
  placeholder = "Search…",
  className,
  children,
  ...props
}) {
  const [inner, setInner] = React.useState("");
  const val = value != null ? value : inner;
  const set = v => onChange ? onChange(v) : setInner(v);
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-search", className)
  }, /*#__PURE__*/React.createElement("svg", {
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "11",
    cy: "11",
    r: "7"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M21 21l-4.3-4.3"
  })), /*#__PURE__*/React.createElement("input", _extends({}, props, {
    type: "text",
    role: "searchbox",
    value: val,
    placeholder: placeholder,
    onChange: e => set(e.target.value)
  })), String(val).length ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cs-search__clear",
    "aria-label": "Clear search",
    onClick: () => {
      set("");
      onClear && onClear();
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 6l12 12M18 6L6 18"
  }))) : null);
}
Object.assign(__ds_scope, { SearchField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/SearchField.jsx", error: String((e && e.message) || e) }); }

// components/forms/SegmentedControl.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill SegmentedControl — pill toggle group (single select). Matches the Status Hub lens switcher. */
function SegmentedControl({
  options = [],
  value,
  onChange,
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    role: "tablist",
    className: cx("cs-segmented", className)
  }, props), options.map(o => /*#__PURE__*/React.createElement("button", {
    key: o.value,
    type: "button",
    role: "tab",
    "aria-selected": value === o.value,
    "aria-pressed": value === o.value,
    onClick: () => onChange && onChange(o.value)
  }, o.icon ? /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true"
  }, o.icon) : null, o.label)));
}
Object.assign(__ds_scope, { SegmentedControl });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/SegmentedControl.jsx", error: String((e && e.message) || e) }); }

// components/forms/Select.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Select — native <select> in the .cs-field frame with a chevron.
 *  Pass `options` [{value,label}] or <option> children. */
function Select({
  id,
  label,
  description,
  error,
  options,
  children,
  disabled = false,
  className,
  ...props
}) {
  const gid = React.useId();
  const sid = id ?? gid;
  const descId = description ? sid + "-desc" : undefined;
  const errId = error ? sid + "-err" : undefined;
  const describedBy = [descId, errId].filter(Boolean).join(" ") || undefined;
  return /*#__PURE__*/React.createElement("label", {
    className: cx("cs-field", disabled && "is-disabled", error && "is-invalid", className),
    htmlFor: sid
  }, label ? /*#__PURE__*/React.createElement("span", {
    className: "cs-field__label"
  }, label) : null, description ? /*#__PURE__*/React.createElement("span", {
    id: descId,
    className: "cs-field__description"
  }, description) : null, /*#__PURE__*/React.createElement("span", {
    className: "cs-select"
  }, /*#__PURE__*/React.createElement("select", _extends({}, props, {
    id: sid,
    disabled: disabled,
    "aria-invalid": error ? true : undefined,
    "aria-describedby": describedBy,
    className: "cs-field__control"
  }), options ? options.map(o => /*#__PURE__*/React.createElement("option", {
    key: o.value,
    value: o.value
  }, o.label)) : children), /*#__PURE__*/React.createElement("span", {
    className: "cs-select__chevron",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("svg", {
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 9l6 6 6-6"
  })))), error ? /*#__PURE__*/React.createElement("span", {
    id: errId,
    className: "cs-field__error",
    role: "alert"
  }, error) : null);
}
Object.assign(__ds_scope, { Select });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Select.jsx", error: String((e && e.message) || e) }); }

// components/forms/Slider.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Slider — brand-tinted native range input. Pass native props (min/max/step/value/onChange). */
function Slider({
  className,
  children,
  ...props
}) {
  return /*#__PURE__*/React.createElement("input", _extends({
    type: "range",
    className: cx("cs-slider", className)
  }, props));
}
Object.assign(__ds_scope, { Slider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Slider.jsx", error: String((e && e.message) || e) }); }

// components/forms/Switch.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Switch — accessible toggle (role="switch"); Umber on, Ochre knob-track in dark. */
function Switch({
  label,
  disabled = false,
  className,
  children,
  ...props
}) {
  return /*#__PURE__*/React.createElement("label", {
    className: cx("cs-switch", disabled && "is-disabled", className)
  }, /*#__PURE__*/React.createElement("input", _extends({
    type: "checkbox",
    role: "switch",
    disabled: disabled
  }, props)), /*#__PURE__*/React.createElement("span", {
    className: "cs-switch__track",
    "aria-hidden": "true"
  }), label ? /*#__PURE__*/React.createElement("span", {
    className: "cs-switch__label"
  }, label) : null);
}
Object.assign(__ds_scope, { Switch });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Switch.jsx", error: String((e && e.message) || e) }); }

// components/forms/Textarea.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Textarea — multi-line input in the .cs-field frame; label/description/error. */
function Textarea({
  id,
  label,
  description,
  error,
  disabled = false,
  rows = 4,
  className,
  ...props
}) {
  const gid = React.useId();
  const tid = id ?? gid;
  const descId = description ? tid + "-desc" : undefined;
  const errId = error ? tid + "-err" : undefined;
  const describedBy = [descId, errId].filter(Boolean).join(" ") || undefined;
  return /*#__PURE__*/React.createElement("label", {
    className: cx("cs-field", disabled && "is-disabled", error && "is-invalid", className),
    htmlFor: tid
  }, label ? /*#__PURE__*/React.createElement("span", {
    className: "cs-field__label"
  }, label) : null, description ? /*#__PURE__*/React.createElement("span", {
    id: descId,
    className: "cs-field__description"
  }, description) : null, /*#__PURE__*/React.createElement("textarea", _extends({}, props, {
    id: tid,
    rows: rows,
    disabled: disabled,
    "aria-invalid": error ? true : undefined,
    "aria-describedby": describedBy,
    className: "cs-field__control"
  })), error ? /*#__PURE__*/React.createElement("span", {
    id: errId,
    className: "cs-field__error",
    role: "alert"
  }, error) : null);
}
Object.assign(__ds_scope, { Textarea });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Textarea.jsx", error: String((e && e.message) || e) }); }

// components/icon/Icon.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * CyberSkill line-icon set (in-repo, no external icon library). Each icon is
 * data — a viewBox plus primitive elements — rendered through this one <Icon>.
 * Line icons use currentColor stroke so they follow the surrounding text token.
 * Sizes come from --cs-icon-* tokens (sm/md/lg). Decorative by default; pass
 * `label` to expose a meaningful icon to assistive tech.
 */
const CS_ICONS = {
  close: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M6 6l12 12M18 6L6 18"
    }]]
  },
  sun: {
    viewBox: "0 0 24 24",
    els: [["circle", {
      cx: 12,
      cy: 12,
      r: 4
    }], ["path", {
      d: "M12 2v2M12 20v2M2 12h2M20 12h2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M19.1 4.9l-1.4 1.4M6.3 17.7l-1.4 1.4"
    }]]
  },
  moon: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M20 13.5A8 8 0 1 1 10.5 4a6.5 6.5 0 0 0 9.5 9.5z"
    }]]
  },
  "arrow-right": {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M5 12h14M13 6l6 6-6 6"
    }]]
  },
  check: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M4 12.5l5 5 11-11"
    }]]
  },
  sparkle: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M12 3l1.8 5.4L19 10l-5.2 1.6L12 17l-1.8-5.4L5 10l5.2-1.6z"
    }]]
  },
  chat: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M4 5h16v11H8l-4 4z"
    }]]
  },
  "sound-on": {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M5 9v6h3l4 4V5L8 9z"
    }], ["path", {
      d: "M15 9.5a4 4 0 0 1 0 5M17.6 7a8 8 0 0 1 0 10"
    }]]
  },
  "sound-off": {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M5 9v6h3l4 4V5L8 9z"
    }], ["path", {
      d: "M16 9.5l4.5 5M20.5 9.5l-4.5 5"
    }]]
  },
  // v2.0 extension — same grammar: line, currentColor, 1.75, round, 24×24
  search: {
    viewBox: "0 0 24 24",
    els: [["circle", {
      cx: 11,
      cy: 11,
      r: 6
    }], ["path", {
      d: "M20 20l-4.2-4.2"
    }]]
  },
  sliders: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M4 6h16M4 12h16M4 18h16"
    }], ["circle", {
      cx: 9,
      cy: 6,
      r: 2
    }], ["circle", {
      cx: 15,
      cy: 12,
      r: 2
    }], ["circle", {
      cx: 7,
      cy: 18,
      r: 2
    }]]
  },
  upload: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M12 15V4M7 8.5L12 4l5 4.5"
    }], ["path", {
      d: "M4 19h16"
    }]]
  },
  download: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M12 4v11M7 10.5L12 15l5-4.5"
    }], ["path", {
      d: "M4 19h16"
    }]]
  },
  calendar: {
    viewBox: "0 0 24 24",
    els: [["rect", {
      x: 4,
      y: 6,
      width: 16,
      height: 14,
      rx: 2
    }], ["path", {
      d: "M4 10.5h16M8 3.5v4M16 3.5v4"
    }]]
  },
  user: {
    viewBox: "0 0 24 24",
    els: [["circle", {
      cx: 12,
      cy: 8.5,
      r: 3.5
    }], ["path", {
      d: "M5 19.5c1.6-3.4 4-5 7-5s5.4 1.6 7 5"
    }]]
  },
  plus: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M12 5v14M5 12h14"
    }]]
  },
  trash: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M5 7h14M9.5 7V4.5h5V7"
    }], ["path", {
      d: "M7 7l.8 12.5h8.4L17 7M10 11v5M14 11v5"
    }]]
  },
  external: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M14 4h6v6M20 4l-8 8"
    }], ["path", {
      d: "M18 13v6H5V6h6"
    }]]
  },
  menu: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M4 7h16M4 12h16M4 17h16"
    }]]
  },
  // v2.13 extension — common chevrons + edit/copy/info/warning (same grammar)
  "chevron-down": {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M6 9l6 6 6-6"
    }]]
  },
  "chevron-up": {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M6 15l6-6 6 6"
    }]]
  },
  "chevron-left": {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M15 6l-6 6 6 6"
    }]]
  },
  "chevron-right": {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M9 6l6 6-6 6"
    }]]
  },
  edit: {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M4 20h4L18.5 9.5a2.12 2.12 0 0 0-3-3L5 17z"
    }], ["path", {
      d: "M13.5 6.5l3 3"
    }]]
  },
  copy: {
    viewBox: "0 0 24 24",
    els: [["rect", {
      x: 8,
      y: 8,
      width: 12,
      height: 12,
      rx: 2
    }], ["path", {
      d: "M4 16V6a2 2 0 0 1 2-2h10"
    }]]
  },
  info: {
    viewBox: "0 0 24 24",
    els: [["circle", {
      cx: 12,
      cy: 12,
      r: 9
    }], ["path", {
      d: "M12 11v5M12 7.75v.01"
    }]]
  },
  "alert-triangle": {
    viewBox: "0 0 24 24",
    els: [["path", {
      d: "M12 3.5l9 16H3z"
    }], ["path", {
      d: "M12 10v4M12 17.5v.01"
    }]]
  }
};
function Icon({
  name,
  size = "md",
  label,
  className,
  strokeWidth = 1.75,
  ...props
}) {
  const def = CS_ICONS[name] || CS_ICONS.sparkle;
  const dim = `var(--cs-icon-${size}, 20px)`;
  const a11y = label ? {
    role: "img",
    "aria-label": label
  } : {
    "aria-hidden": true,
    focusable: false
  };
  return /*#__PURE__*/React.createElement("svg", _extends({}, props, {
    className: className,
    viewBox: def.viewBox,
    style: {
      width: dim,
      height: dim,
      display: "inline-block",
      flex: "none",
      ...(props.style || {})
    },
    fill: "none",
    stroke: "currentColor",
    strokeWidth: strokeWidth,
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, a11y), def.els.map(([tag, attrs], i) => React.createElement(tag, {
    key: i,
    ...attrs
  })));
}
Object.assign(__ds_scope, { CS_ICONS, Icon });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icon/Icon.jsx", error: String((e && e.message) || e) }); }

// components/logo/logo-data.js
try { (() => {
/* Generated from @cyberskill/brand-assets/logo-mark.svg — the official CyberSkill mark.
 * Exact master artwork (1007x1007). Do not hand-edit; replace the source SVG and regenerate. */
const CS_LOGO_VIEWBOX = "0 0 1007 1007";
const CS_LOGO_MARK_INNER = `<path fill="#45210E" opacity="1.000000" stroke="none" 
	d="
M560.000000,1008.000000 
	C373.333344,1008.000000 187.166672,1008.000000 1.000000,1008.000000 
	C1.000000,672.333313 1.000000,336.666656 1.000000,1.000000 
	C336.666656,1.000000 672.333313,1.000000 1008.000000,1.000000 
	C1008.000000,336.666656 1008.000000,672.333313 1008.000000,1008.000000 
	C858.833313,1008.000000 709.666687,1008.000000 560.000000,1008.000000 
M440.743774,105.242386 
	C438.572540,107.309639 436.344940,109.321198 434.238464,111.452431 
	C396.510132,149.623566 372.276398,194.939011 363.356293,247.940109 
	C360.996429,261.961884 358.946777,276.673065 360.603027,290.610352 
	C365.030243,327.864777 394.910004,357.905762 431.834900,364.830811 
	C436.723083,365.747589 441.737213,365.993011 447.763702,366.669525 
	C446.254150,363.858124 445.507843,362.409912 444.709564,360.990906 
	C439.517578,351.761383 436.885193,341.740936 437.455444,331.277863 
	C439.589569,292.120056 458.815063,264.154144 494.094208,247.402267 
	C503.544983,242.914673 513.742554,238.863556 524.373901,240.455948 
	C548.543213,244.076111 568.703491,255.555466 584.000793,274.713074 
	C599.847656,294.559021 607.550598,317.209198 603.764282,342.677429 
	C602.575073,350.677063 598.728027,358.281586 595.936157,366.544159 
	C600.137085,366.074554 604.740540,365.758667 609.275574,365.021576 
	C646.763123,358.928711 678.394348,326.946808 681.791199,289.350922 
	C683.086792,275.012085 681.404419,259.972870 678.550415,245.760666 
	C660.577881,156.262833 608.759338,93.666801 525.888672,56.228947 
	C522.563171,54.726585 519.736023,54.769550 516.419739,56.200459 
	C488.640594,68.186523 463.511292,84.231010 440.743774,105.242386 
M286.676636,492.678345 
	C287.329773,493.793732 287.835724,495.033447 288.657562,496.006378 
	C309.770172,521.000305 334.755707,541.223022 364.045837,555.811401 
	C374.407410,560.972046 385.114655,566.593323 396.291962,568.749573 
	C432.863312,575.804749 469.791931,575.543579 506.608704,569.950623 
	C542.882812,564.440125 577.636902,553.955383 610.255371,536.938660 
	C635.979919,523.518494 665.744080,527.108459 687.312561,546.611755 
	C690.823608,549.786621 693.878845,553.465637 697.214539,556.985291 
	C719.173889,537.685486 738.051208,516.184204 753.920410,491.916840 
	C752.879761,490.957520 752.286987,490.387451 751.669678,489.845367 
	C716.653381,459.096100 676.901917,436.766174 632.377991,423.023193 
	C629.713745,422.200836 626.423889,422.161865 623.725525,422.882690 
	C590.944153,431.639587 558.238403,440.679779 525.448730,449.404907 
	C522.300964,450.242554 518.534851,450.164001 515.372437,449.321808 
	C482.598022,440.593750 449.897797,431.587128 417.141144,422.791412 
	C414.867096,422.180786 412.115295,422.006958 409.909119,422.683990 
	C363.870605,436.812317 322.877258,459.923096 286.676636,492.678345 
M638.193726,649.759216 
	C618.869507,656.015442 599.040955,658.948853 578.743958,658.635498 
	C561.320618,658.366577 544.380371,654.932678 527.565796,650.671387 
	C525.521851,650.153320 523.254150,649.859985 521.196533,650.169861 
	C512.810059,651.432678 504.514069,653.468018 496.093933,654.298462 
	C480.882416,655.798584 465.515686,658.255371 450.375763,657.410278 
	C421.185272,655.780762 394.125427,646.062805 369.145020,630.771667 
	C368.008179,630.075806 366.770874,629.544006 364.879761,628.577820 
	C379.822174,677.662048 402.386169,721.588440 433.601837,761.123535 
	C468.862305,805.781311 475.313782,854.488708 452.707825,906.779114 
	C449.580719,914.012512 445.212494,920.709351 441.423889,927.657288 
	C573.221985,890.897766 675.186523,765.328125 681.092896,630.672180 
	C667.338623,636.799011 653.131897,643.127319 638.193726,649.759216 
M451.496307,582.220520 
	C442.351196,581.799133 433.142700,581.909058 424.076202,580.828186 
	C411.400055,579.317017 398.218109,578.681030 386.332123,574.558289 
	C354.337128,563.460327 326.346069,545.236816 301.709656,521.949341 
	C300.020294,520.352478 298.291534,518.797302 295.873596,516.571472 
	C296.025665,518.456909 295.980042,519.134644 296.144836,519.756653 
	C304.310333,550.579102 319.447479,577.592346 342.608459,599.413086 
	C382.985504,637.453735 430.264038,654.771729 486.434967,647.405457 
	C555.692078,638.323059 618.847046,614.520935 675.700134,573.984924 
	C680.675537,570.437500 685.476868,566.646118 690.581665,562.800293 
	C677.023743,544.661377 659.575134,536.325806 637.562927,537.450195 
	C626.043884,538.038574 616.340271,543.514893 606.234131,548.288513 
	C557.596436,571.262329 506.255249,582.294250 451.496307,582.220520 
M539.676270,436.107300 
	C577.955139,425.690460 616.233948,415.273621 655.757202,404.518158 
	C635.089294,395.015900 615.544373,386.029968 595.877441,376.987946 
	C594.662292,379.021637 593.749573,380.591156 592.797180,382.136292 
	C586.595276,392.197998 578.925293,400.845184 568.666138,406.952454 
	C534.791809,427.117798 492.288269,422.383606 462.337891,394.876129 
	C456.381653,389.405701 451.112213,383.187500 445.401093,377.182709 
	C426.206024,386.105682 406.958923,395.052887 386.824615,404.412476 
	C389.268799,405.156464 390.645874,405.614380 392.043884,405.995544 
	C429.426819,416.187622 466.920624,425.996552 504.132446,436.778748 
	C515.781982,440.154175 526.683472,441.361206 537.944458,436.636871 
	C538.251221,436.508148 538.570312,436.408813 539.676270,436.107300 
M636.169495,641.720032 
	C693.203857,621.317932 729.752136,581.965942 745.785767,523.547119 
	C689.811462,583.441650 622.343140,624.064514 542.504456,645.372620 
	C570.121521,652.872437 603.639343,651.544189 636.169495,641.720032 
z"/>
<path fill="#F4BA17" opacity="1.000000" stroke="none" 
	d="
M441.015747,105.015450 
	C463.511292,84.231010 488.640594,68.186523 516.419739,56.200459 
	C519.736023,54.769550 522.563171,54.726585 525.888672,56.228947 
	C608.759338,93.666801 660.577881,156.262833 678.550415,245.760666 
	C681.404419,259.972870 683.086792,275.012085 681.791199,289.350922 
	C678.394348,326.946808 646.763123,358.928711 609.275574,365.021576 
	C604.740540,365.758667 600.137085,366.074554 595.936157,366.544159 
	C598.728027,358.281586 602.575073,350.677063 603.764282,342.677429 
	C607.550598,317.209198 599.847656,294.559021 584.000793,274.713074 
	C568.703491,255.555466 548.543213,244.076111 524.373901,240.455948 
	C513.742554,238.863556 503.544983,242.914673 494.094208,247.402267 
	C458.815063,264.154144 439.589569,292.120056 437.455444,331.277863 
	C436.885193,341.740936 439.517578,351.761383 444.709564,360.990906 
	C445.507843,362.409912 446.254150,363.858124 447.763702,366.669525 
	C441.737213,365.993011 436.723083,365.747589 431.834900,364.830811 
	C394.910004,357.905762 365.030243,327.864777 360.603027,290.610352 
	C358.946777,276.673065 360.996429,261.961884 363.356293,247.940109 
	C372.276398,194.939011 396.510132,149.623566 434.238464,111.452431 
	C436.344940,109.321198 438.572540,107.309639 441.015747,105.015450 
M523.796143,200.964203 
	C522.979370,200.806061 522.153381,200.684708 521.347168,200.484161 
	C511.964752,198.150375 505.739960,190.440384 506.015442,181.511810 
	C506.292053,172.544968 512.964478,165.113678 522.410034,163.391052 
	C528.999146,162.189362 534.425354,164.852509 539.153198,168.766846 
	C541.656067,166.404358 544.030029,164.163544 546.391113,161.934906 
	C535.207764,149.868927 515.326111,149.927109 503.764648,161.836975 
	C492.335419,173.610657 493.100830,193.056198 505.417236,203.820190 
	C517.457336,214.342712 537.192993,213.508774 546.093628,202.189407 
	C543.863281,199.842941 541.645081,197.509277 539.426880,195.175613 
	C539.477722,195.664429 539.528503,196.153259 539.579346,196.642075 
	C534.598694,198.089890 529.617981,199.537689 523.796143,200.964203 
z"/>
<path fill="#F4BA17" opacity="1.000000" stroke="none" 
	d="
M286.857971,492.402985 
	C322.877258,459.923096 363.870605,436.812317 409.909119,422.683990 
	C412.115295,422.006958 414.867096,422.180786 417.141144,422.791412 
	C449.897797,431.587128 482.598022,440.593750 515.372437,449.321808 
	C518.534851,450.164001 522.300964,450.242554 525.448730,449.404907 
	C558.238403,440.679779 590.944153,431.639587 623.725525,422.882690 
	C626.423889,422.161865 629.713745,422.200836 632.377991,423.023193 
	C676.901917,436.766174 716.653381,459.096100 751.669678,489.845367 
	C752.286987,490.387451 752.879761,490.957520 753.920410,491.916840 
	C738.051208,516.184204 719.173889,537.685486 697.214539,556.985291 
	C693.878845,553.465637 690.823608,549.786621 687.312561,546.611755 
	C665.744080,527.108459 635.979919,523.518494 610.255371,536.938660 
	C577.636902,553.955383 542.882812,564.440125 506.608704,569.950623 
	C469.791931,575.543579 432.863312,575.804749 396.291962,568.749573 
	C385.114655,566.593323 374.407410,560.972046 364.045837,555.811401 
	C334.755707,541.223022 309.770172,521.000305 288.657562,496.006378 
	C287.835724,495.033447 287.329773,493.793732 286.857971,492.402985 
z"/>
<path fill="#F4BA17" opacity="1.000000" stroke="none" 
	d="
M638.559448,649.607422 
	C653.131897,643.127319 667.338623,636.799011 681.092896,630.672180 
	C675.186523,765.328125 573.221985,890.897766 441.421356,927.655945 
	C445.212494,920.709351 449.580719,914.012512 452.707825,906.779114 
	C475.313782,854.488708 468.862305,805.781311 433.601837,761.123535 
	C402.386169,721.588440 379.822174,677.662048 364.879761,628.577820 
	C366.770874,629.544006 368.008179,630.075806 369.145020,630.771667 
	C394.125427,646.062805 421.185272,655.780762 450.375763,657.410278 
	C465.515686,658.255371 480.882416,655.798584 496.093933,654.298462 
	C504.514069,653.468018 512.810059,651.432678 521.196533,650.169861 
	C523.254150,649.859985 525.521851,650.153320 527.565796,650.671387 
	C544.380371,654.932678 561.320618,658.366577 578.743958,658.635498 
	C599.040955,658.948853 618.869507,656.015442 638.559448,649.607422 
z"/>
<path fill="#F3B917" opacity="1.000000" stroke="none" 
	d="
M451.996887,582.225220 
	C506.255249,582.294250 557.596436,571.262329 606.234131,548.288513 
	C616.340271,543.514893 626.043884,538.038574 637.562927,537.450195 
	C659.575134,536.325806 677.023743,544.661377 690.581665,562.800293 
	C685.476868,566.646118 680.675537,570.437500 675.700134,573.984924 
	C618.847046,614.520935 555.692078,638.323059 486.434967,647.405457 
	C430.264038,654.771729 382.985504,637.453735 342.608459,599.413086 
	C319.447479,577.592346 304.310333,550.579102 296.144836,519.756653 
	C295.980042,519.134644 296.025665,518.456909 295.873596,516.571472 
	C298.291534,518.797302 300.020294,520.352478 301.709656,521.949341 
	C326.346069,545.236816 354.337128,563.460327 386.332123,574.558289 
	C398.218109,578.681030 411.400055,579.317017 424.076202,580.828186 
	C433.142700,581.909058 442.351196,581.799133 451.996887,582.225220 
z"/>
<path fill="#F2B817" opacity="1.000000" stroke="none" 
	d="
M539.279968,436.201538 
	C538.570312,436.408813 538.251221,436.508148 537.944458,436.636871 
	C526.683472,441.361206 515.781982,440.154175 504.132446,436.778748 
	C466.920624,425.996552 429.426819,416.187622 392.043884,405.995544 
	C390.645874,405.614380 389.268799,405.156464 386.824615,404.412476 
	C406.958923,395.052887 426.206024,386.105682 445.401093,377.182709 
	C451.112213,383.187500 456.381653,389.405701 462.337891,394.876129 
	C492.288269,422.383606 534.791809,427.117798 568.666138,406.952454 
	C578.925293,400.845184 586.595276,392.197998 592.797180,382.136292 
	C593.749573,380.591156 594.662292,379.021637 595.877441,376.987946 
	C615.544373,386.029968 635.089294,395.015900 655.757202,404.518158 
	C616.233948,415.273621 577.955139,425.690460 539.279968,436.201538 
z"/>
<path fill="#F2B817" opacity="1.000000" stroke="none" 
	d="
M635.789856,641.836487 
	C603.639343,651.544189 570.121521,652.872437 542.504456,645.372620 
	C622.343140,624.064514 689.811462,583.441650 745.785767,523.547119 
	C729.752136,581.965942 693.203857,621.317932 635.789856,641.836487 
z"/>
<path fill="#48240F" opacity="1.000000" stroke="none" 
	d="
M524.216736,200.974854 
	C529.617981,199.537689 534.598694,198.089890 539.579346,196.642075 
	C539.528503,196.153259 539.477722,195.664429 539.426880,195.175613 
	C541.645081,197.509277 543.863281,199.842941 546.093628,202.189407 
	C537.192993,213.508774 517.457336,214.342712 505.417236,203.820190 
	C493.100830,193.056198 492.335419,173.610657 503.764648,161.836975 
	C515.326111,149.927109 535.207764,149.868927 546.391113,161.934906 
	C544.030029,164.163544 541.656067,166.404358 539.153198,168.766846 
	C534.425354,164.852509 528.999146,162.189362 522.410034,163.391052 
	C512.964478,165.113678 506.292053,172.544968 506.015442,181.511810 
	C505.739960,190.440384 511.964752,198.150375 521.347168,200.484161 
	C522.153381,200.684708 522.979370,200.806061 524.216736,200.974854 
z"/>`;
Object.assign(__ds_scope, { CS_LOGO_VIEWBOX, CS_LOGO_MARK_INNER, __ds_default_components_logo_logo_data_19wwt6a: CS_LOGO_MARK_INNER });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/logo/logo-data.js", error: String((e && e.message) || e) }); }

// components/logo/Logo.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/**
 * CyberSkill brand mark. Renders the EXACT official master artwork (Umber ground,
 * Ochre genie figure). Use whenever a product is for or owned by CyberSkill.
 * Do not recreate or recolour the mark.
 */
function Logo({
  size = 32,
  title = "CyberSkill",
  decorative = false,
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("svg", _extends({}, props, {
    className: cx("cs-logo", className),
    width: size,
    height: size,
    viewBox: __ds_scope.CS_LOGO_VIEWBOX,
    xmlns: "http://www.w3.org/2000/svg",
    role: decorative ? undefined : "img",
    "aria-hidden": decorative ? true : undefined,
    "aria-label": decorative ? undefined : title,
    dangerouslySetInnerHTML: {
      __html: (decorative ? "" : `<title>${title}</title>`) + __ds_scope.CS_LOGO_MARK_INNER
    }
  }));
}
Object.assign(__ds_scope, { Logo });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/logo/Logo.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Breadcrumb.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Breadcrumb — hierarchical trail; last item is current. */
function Breadcrumb({
  items = [],
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("nav", _extends({
    "aria-label": "Breadcrumb"
  }, props), /*#__PURE__*/React.createElement("ol", {
    className: cx("cs-breadcrumb", className)
  }, items.map((it, i) => {
    const last = i === items.length - 1;
    return /*#__PURE__*/React.createElement("li", {
      key: i
    }, last ? /*#__PURE__*/React.createElement("span", {
      className: "cs-breadcrumb__current",
      "aria-current": "page"
    }, it.label) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("a", {
      href: it.href || "#"
    }, it.label), /*#__PURE__*/React.createElement("span", {
      className: "cs-breadcrumb__sep",
      "aria-hidden": "true"
    }, "/")));
  })));
}
Object.assign(__ds_scope, { Breadcrumb });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Breadcrumb.jsx", error: String((e && e.message) || e) }); }

// components/navigation/CommandPalette.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill CommandPalette — ⌘K overlay with search + grouped actions. Controlled via open/onClose. */
function CommandPalette({
  open,
  onClose,
  placeholder = "Type a command or search…",
  groups = [],
  className
}) {
  const [q, setQ] = React.useState("");
  React.useEffect(() => {
    if (!open) return;
    const onKey = e => {
      if (e.key === "Escape") onClose && onClose();
    };
    document.addEventListener("keydown", onKey);
    return () => document.removeEventListener("keydown", onKey);
  }, [open]);
  if (!open) return null;
  const needle = q.trim().toLowerCase();
  return /*#__PURE__*/React.createElement("div", {
    className: "cs-cmdk-scrim",
    onClick: onClose
  }, /*#__PURE__*/React.createElement("div", {
    className: cx("cs-cmdk", className),
    role: "dialog",
    "aria-modal": "true",
    "aria-label": "Command palette",
    onClick: e => e.stopPropagation()
  }, /*#__PURE__*/React.createElement("div", {
    className: "cs-cmdk__search"
  }, /*#__PURE__*/React.createElement("svg", {
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "11",
    cy: "11",
    r: "7"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M21 21l-4.3-4.3"
  })), /*#__PURE__*/React.createElement("input", {
    autoFocus: true,
    value: q,
    onChange: e => setQ(e.target.value),
    placeholder: placeholder
  }), /*#__PURE__*/React.createElement("span", {
    className: "cs-kbd"
  }, "Esc")), /*#__PURE__*/React.createElement("div", {
    className: "cs-cmdk__list"
  }, groups.map((g, gi) => {
    const items = (g.items || []).filter(it => !needle || String(it.label).toLowerCase().includes(needle));
    if (!items.length) return null;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: gi
    }, g.label ? /*#__PURE__*/React.createElement("div", {
      className: "cs-cmdk__label"
    }, g.label) : null, items.map((it, ii) => /*#__PURE__*/React.createElement("button", {
      key: ii,
      type: "button",
      className: "cs-cmdk__item",
      onClick: () => {
        it.onSelect && it.onSelect();
        onClose && onClose();
      }
    }, it.icon ? /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true"
    }, it.icon) : null, /*#__PURE__*/React.createElement("span", null, it.label), it.shortcut ? /*#__PURE__*/React.createElement("span", {
      className: "cs-kbd"
    }, it.shortcut) : null)));
  }))));
}
Object.assign(__ds_scope, { CommandPalette });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/CommandPalette.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Menu.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Menu — dropdown. Provide a `trigger` element; compose MenuItem children. */
function Menu({
  trigger,
  children,
  align = "start",
  open: controlledOpen,
  onOpenChange,
  className
}) {
  const [uOpen, setUOpen] = React.useState(false);
  const open = controlledOpen != null ? controlledOpen : uOpen;
  const set = v => {
    onOpenChange ? onOpenChange(v) : setUOpen(v);
  };
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!open) return;
    const onDoc = e => {
      if (ref.current && !ref.current.contains(e.target)) set(false);
    };
    const onKey = e => {
      if (e.key === "Escape") set(false);
    };
    document.addEventListener("mousedown", onDoc);
    document.addEventListener("keydown", onKey);
    return () => {
      document.removeEventListener("mousedown", onDoc);
      document.removeEventListener("keydown", onKey);
    };
  }, [open]);
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-menu", className),
    ref: ref
  }, /*#__PURE__*/React.createElement("span", {
    onClick: () => set(!open),
    "aria-haspopup": "menu",
    "aria-expanded": open
  }, trigger), open ? /*#__PURE__*/React.createElement("div", {
    className: cx("cs-menu__list", align === "end" && "cs-menu__list--end"),
    role: "menu"
  }, children) : null);
}

/** An item inside a Menu. */
function MenuItem({
  danger = false,
  icon,
  children,
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    role: "menuitem",
    className: cx("cs-menu__item", danger && "cs-menu__item--danger", className)
  }, props), icon ? /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true"
  }, icon) : null, children);
}
Object.assign(__ds_scope, { Menu, MenuItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Menu.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Pagination.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}
function pages(page, count) {
  if (count <= 7) return Array.from({
    length: count
  }, (_, i) => i + 1);
  const out = [1];
  const lo = Math.max(2, page - 1),
    hi = Math.min(count - 1, page + 1);
  if (lo > 2) out.push("…");
  for (let p = lo; p <= hi; p++) out.push(p);
  if (hi < count - 1) out.push("…");
  out.push(count);
  return out;
}

/** CyberSkill Pagination — page controls with prev/next and ellipsis. */
function Pagination({
  page = 1,
  pageCount = 1,
  onChange,
  className,
  ...props
}) {
  const go = p => onChange && p >= 1 && p <= pageCount && p !== page && onChange(p);
  return /*#__PURE__*/React.createElement("nav", _extends({
    "aria-label": "Pagination",
    className: cx("cs-pagination", className)
  }, props), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => go(page - 1),
    disabled: page <= 1,
    "aria-label": "Previous page"
  }, "\u2039"), pages(page, pageCount).map((p, i) => p === "…" ? /*#__PURE__*/React.createElement("span", {
    key: "e" + i,
    className: "cs-pagination__ellipsis",
    "aria-hidden": "true"
  }, "\u2026") : /*#__PURE__*/React.createElement("button", {
    key: p,
    type: "button",
    "aria-current": p === page ? "page" : undefined,
    onClick: () => go(p)
  }, p)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => go(page + 1),
    disabled: page >= pageCount,
    "aria-label": "Next page"
  }, "\u203A"));
}
Object.assign(__ds_scope, { Pagination });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Pagination.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Sidebar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Sidebar — vertical app-shell navigation. Compose NavItem children; optional label groups. */
function Sidebar({
  label,
  children,
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("nav", _extends({
    className: cx("cs-sidebar", className)
  }, props), label ? /*#__PURE__*/React.createElement("div", {
    className: "cs-sidebar__label"
  }, label) : null, children);
}

/** A row in a Sidebar. Renders an <a> when href is set, else a <button>. */
function NavItem({
  icon,
  active = false,
  trail,
  href,
  onClick,
  children,
  className,
  ...props
}) {
  const Tag = href ? "a" : "button";
  return /*#__PURE__*/React.createElement(Tag, _extends({
    className: cx("cs-nav-item", active && "is-active", className),
    href: href,
    "aria-current": active ? "page" : undefined,
    onClick: onClick
  }, props), icon ? /*#__PURE__*/React.createElement("span", {
    className: "cs-nav-item__icon"
  }, icon) : null, /*#__PURE__*/React.createElement("span", null, children), trail != null ? /*#__PURE__*/React.createElement("span", {
    className: "cs-nav-item__trail"
  }, trail) : null);
}
Object.assign(__ds_scope, { Sidebar, NavItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Sidebar.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Steps.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Steps — horizontal progress stepper. Steps before `current` are done, `current` is active. */
function Steps({
  steps = [],
  current = 0,
  className
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: cx("cs-steps", className)
  }, steps.map((s, i) => {
    const state = i < current ? "done" : i === current ? "active" : "todo";
    return /*#__PURE__*/React.createElement("div", {
      key: i,
      className: cx("cs-step", `cs-step--${state}`)
    }, /*#__PURE__*/React.createElement("span", {
      className: "cs-step__marker"
    }, state === "done" ? "✓" : s.n || i + 1), /*#__PURE__*/React.createElement("span", {
      className: "cs-step__title"
    }, s.title), s.body ? /*#__PURE__*/React.createElement("span", {
      className: "cs-step__body"
    }, s.body) : null);
  }));
}
Object.assign(__ds_scope, { Steps });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Steps.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Tabs.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Tabs — data-driven tablist, controlled by value/onChange. Ochre underline on active. */
function Tabs({
  tabs = [],
  value,
  onChange,
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    role: "tablist",
    className: cx("cs-tabs", className)
  }, props), tabs.map(t => /*#__PURE__*/React.createElement("button", {
    key: t.value,
    type: "button",
    role: "tab",
    "aria-selected": value === t.value,
    className: "cs-tab",
    onClick: () => onChange && onChange(t.value)
  }, t.label, t.count != null ? /*#__PURE__*/React.createElement("span", {
    className: "cs-tab__count"
  }, t.count) : null)));
}

/** A single tab button, for hand-composed tablists. */
function Tab({
  selected = false,
  count,
  children,
  className,
  ...props
}) {
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    role: "tab",
    "aria-selected": selected,
    className: cx("cs-tab", className)
  }, props), children, count != null ? /*#__PURE__*/React.createElement("span", {
    className: "cs-tab__count"
  }, count) : null);
}
Object.assign(__ds_scope, { Tabs, Tab });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Tabs.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Drawer.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Drawer — slide-in side panel with scrim. side: right (default) | left. */
function Drawer({
  open,
  onClose,
  title,
  side = "right",
  children,
  actions,
  className
}) {
  React.useEffect(() => {
    if (!open) return;
    const k = e => {
      if (e.key === "Escape") onClose && onClose();
    };
    document.addEventListener("keydown", k);
    return () => document.removeEventListener("keydown", k);
  }, [open]);
  if (!open) return null;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "cs-drawer-scrim",
    onClick: onClose,
    "aria-hidden": "true"
  }), /*#__PURE__*/React.createElement("aside", {
    className: cx("cs-drawer", side === "left" && "cs-drawer--left", className),
    role: "dialog",
    "aria-modal": "true",
    "aria-label": typeof title === "string" ? title : "Panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cs-drawer__header"
  }, title ? /*#__PURE__*/React.createElement("h2", {
    className: "cs-drawer__title"
  }, title) : null, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cs-drawer__close",
    "aria-label": "Close",
    onClick: onClose
  }, /*#__PURE__*/React.createElement("svg", {
    width: "20",
    height: "20",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 6l12 12M18 6L6 18"
  })))), /*#__PURE__*/React.createElement("div", {
    className: "cs-drawer__body"
  }, children), actions ? /*#__PURE__*/React.createElement("div", {
    className: "cs-drawer__footer"
  }, actions) : null));
}
Object.assign(__ds_scope, { Drawer });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Drawer.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Popover.jsx
try { (() => {
function cx(...c) {
  return c.filter(Boolean).join(" ");
}

/** CyberSkill Popover — anchored floating panel. Uncontrolled (click toggles) or controlled. */
function Popover({
  trigger,
  children,
  align = "start",
  open: controlled,
  onOpenChange,
  className
}) {
  const [u, setU] = React.useState(false);
  const open = controlled != null ? controlled : u;
  const set = v => onOpenChange ? onOpenChange(v) : setU(v);
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!open) return;
    const d = e => {
      if (ref.current && !ref.current.contains(e.target)) set(false);
    };
    const k = e => {
      if (e.key === "Escape") set(false);
    };
    document.addEventListener("mousedown", d);
    document.addEventListener("keydown", k);
    return () => {
      document.removeEventListener("mousedown", d);
      document.removeEventListener("keydown", k);
    };
  }, [open]);
  return /*#__PURE__*/React.createElement("span", {
    className: cx("cs-popover", className),
    ref: ref
  }, /*#__PURE__*/React.createElement("span", {
    onClick: () => set(!open),
    "aria-haspopup": "dialog",
    "aria-expanded": open
  }, trigger), open ? /*#__PURE__*/React.createElement("div", {
    className: cx("cs-popover__panel", align === "end" && "cs-popover__panel--end"),
    role: "dialog"
  }, children) : null);
}
Object.assign(__ds_scope, { Popover });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Popover.jsx", error: String((e && e.message) || e) }); }

// components/textfield/TextField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function cx(...c) {
  return c.filter(Boolean).join(" ");
}
let uid = 0;
function useId(prefix) {
  const [id] = React.useState(() => `${prefix}-${++uid}`);
  return id;
}

/**
 * CyberSkill TextField — label + input with optional description and error.
 * Programmatic label association and aria-describedby wiring; IME-safe.
 */
function TextField({
  id,
  label,
  description,
  error,
  disabled = false,
  readOnly = false,
  className,
  children,
  ...props
}) {
  const generatedId = useId("cs-textfield");
  const inputId = id ?? generatedId;
  const descriptionId = description ? `${inputId}-description` : undefined;
  const errorId = error ? `${inputId}-error` : undefined;
  const describedBy = [descriptionId, errorId].filter(Boolean).join(" ") || undefined;
  return /*#__PURE__*/React.createElement("label", {
    className: cx("cs-field", disabled && "is-disabled", error && "is-invalid", className),
    htmlFor: inputId
  }, /*#__PURE__*/React.createElement("span", {
    className: "cs-field__label"
  }, label), description ? /*#__PURE__*/React.createElement("span", {
    id: descriptionId,
    className: "cs-field__description"
  }, description) : null, /*#__PURE__*/React.createElement("input", _extends({}, props, {
    id: inputId,
    disabled: disabled,
    readOnly: readOnly,
    "aria-invalid": error ? true : undefined,
    "aria-describedby": describedBy,
    className: "cs-field__control"
  })), error ? /*#__PURE__*/React.createElement("span", {
    id: errorId,
    className: "cs-field__error",
    role: "alert"
  }, error) : null);
}
Object.assign(__ds_scope, { TextField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/textfield/TextField.jsx", error: String((e && e.message) || e) }); }

// image-slot.js
try { (() => {
// @ds-adherence-ignore -- omelette starter scaffold (raw elements/hex/px by design)
// Copied omelette starter. Re-running copy_starter_component with this kind overwrites this file with the latest version (page content is unaffected).
/* BEGIN USAGE */
/**
 * <image-slot> — user-fillable image placeholder.
 *
 * Drop this into a deck, mockup, or page wherever a design needs an image.
 * You control the slot's shape; it sizes to its container by default. When the search_stock_photos tool
 * is available, prefill the slot by default — write the photo's URL into
 * src (with credit/credit-href); the user can still fill or replace it
 * by dragging an image file onto it (or clicking to browse). The dropped
 * image persists across reloads via a .image-slots.state.json sidecar —
 * same read-via-fetch / write-via-window.omelette pattern as
 * design_canvas.jsx, so the filled slot shows on share links, downloaded
 * zips, and PPTX export. Outside the omelette runtime the slot is read-only.
 *
 * The sidecar is a SIBLING of the HTML file that uses this component: the
 * read is a document-relative fetch, and the host resolves the bridge's
 * sidecar writes into the previewed file's directory to match (same
 * contract as design_canvas.jsx). Pages in the same directory share one
 * sidecar; keep slot ids distinct across them.
 *
 * Attributes:
 *   id           Persistence key. REQUIRED for the drop to survive reload —
 *                every slot on the page needs a distinct id.
 *   shape        'rect' | 'rounded' | 'circle' | 'pill'   (default 'rounded')
 *                'circle' applies 50% border-radius; on a non-square slot
 *                that's an ellipse — set equal width and height for a true
 *                circle.
 *   radius       Corner radius in px for 'rounded'.       (default 12)
 *   mask         Any CSS clip-path value. Overrides `shape` — use this for
 *                hexagons, blobs, arbitrary polygons.
 *   fit          Initial framing baseline: cover | contain.   (default 'cover')
 *                cover starts the image filling the frame (overflow cropped);
 *                contain starts it fully visible (letterboxed). Either way the
 *                user can always pan/scale from there — double-click, or the
 *                Edit control, enters reframe mode (drag to move, scroll or
 *                corner-handles to scale; Escape / click-out commits). The
 *                crop persists alongside the image in the sidecar.
 *   placeholder  Empty-state caption.                      (default 'Drop an image')
 *   src          Optional initial/fallback image URL. Prefill it with a real
 *                photo via search_stock_photos when that tool is available
 *                (set credit/credit-href from the result). A user drop
 *                overrides it; clearing the drop reveals src again.
 *   credit       Attribution text shown as a small overlay at the
 *                bottom-left of the filled slot. REQUIRED whenever src
 *                points at any Unsplash host (images.unsplash.com,
 *                plus.unsplash.com, …): an Unsplash src with no credit
 *                renders an error tile INSTEAD of the photo (Unsplash
 *                terms forbid showing their photos unattributed). Use the
 *                exact form 'Photo by {photographer name} on Unsplash' —
 *                the overlay then links the name to credit-href and
 *                'Unsplash' to the Unsplash homepage, and links back to
 *                unsplash.com automatically get the required utm referral
 *                params appended at render time. The credit belongs to
 *                the src image, so it only shows while src is what's
 *                displayed — a user-dropped image hides it.
 *   credit-href  Link for the photographer's name in the credit overlay
 *                (their Unsplash profile URL from the stock-photo search
 *                results). http(s) URLs only — anything else renders the
 *                name as plain text.
 *
 * Sizing: the slot fills its container by default (width/height 100%).
 * Put it in a sized wrapper — absolutely positioned, a grid cell, a fixed
 * frame — and it takes exactly that box. When the parent's height is
 * indefinite (ordinary flow), it falls back to full width at a 3:2 aspect
 * ratio instead of collapsing. In a shrink-to-fit parent (a float,
 * width:max-content, an unsized absolute wrapper), percentages have
 * nothing to resolve against — size the slot or its wrapper explicitly
 * there. For a fixed-size slot, set
 * width/height on the element itself (inline style), which overrides the
 * default. When
 * layering content above a slot (full-bleed layouts), make the overlay
 * click-through — pointer-events: none on scrims/text plates, re-enabled
 * on interactive children — so the slot's hover controls stay reachable.
 * Keep the slot's bottom-left corner visually clear as well: the credit
 * overlay renders there, and a dark fade or text plate covering it hides
 * the attribution Unsplash's terms require — end the fade above that
 * corner, or keep it nearly transparent where the credit sits.
 *
 * Usage:
 *   <div style="position:relative;width:100%;height:100%">      <!-- full-bleed: -->
 *     <image-slot id="bg" shape="rect"></image-slot>            <!-- fills the wrapper -->
 *   </div>
 *   <image-slot id="hero"   style="width:800px;height:450px" shape="rounded" radius="20"
 *               placeholder="Drop a hero image"></image-slot>
 *   <image-slot id="avatar" style="width:120px;height:120px" shape="circle"></image-slot>
 *   <image-slot id="kite"   style="width:300px;height:300px"
 *               mask="polygon(50% 0, 100% 50%, 50% 100%, 0 50%)"></image-slot>
 */
/* END USAGE */

(() => {
  const STATE_FILE = '.image-slots.state.json';

  // Unsplash terms require visible attribution wherever their photos
  // display, and every link back to unsplash.com must carry utm referral
  // params. Two render-time rules enforce that here:
  //  - an Unsplash-src slot with NO credit attribute renders an error
  //    tile INSTEAD of the photo (an uncredited Unsplash photo on screen
  //    is itself the terms violation, so it never renders bare);
  //  - rendered credit links pointing at unsplash.com get the referral
  //    params appended when absent (credit-href values live in page
  //    content that can't be edited after the fact).
  // Keep the utm_source value in sync with UTM_SOURCE in
  // platform/web-agent/unsplash.ts — this file is a project-local
  // artifact and cannot import it (equality is pinned by tests).
  const UNSPLASH_HOMEPAGE_HREF = 'https://unsplash.com/?utm_source=claude_design&utm_medium=referral';
  // Host rule mirrors the hotlink validator that admits Unsplash srcs into
  // pages in the first place (cdn$ in unsplash.ts: apex or any subdomain)
  // — Unsplash+ results serve from plus.unsplash.com, not just images.*,
  // and an admitted-but-uncredited photo must error whatever unsplash
  // host it rides on.
  // Trailing-dot FQDNs (images.unsplash.com.) are the same host to the
  // browser but would miss the regex — strip one dot so the check fails
  // CLOSED (unrecognized-but-real Unsplash srcs must error, not render).
  const isUnsplashHost = u => {
    try {
      return /(^|\.)unsplash\.com$/.test(new URL(u, document.baseURI).hostname.replace(/\.$/, ''));
    } catch {
      return false;
    }
  };
  // Render-time referral normalization for links back to Unsplash:
  // appends utm_source/utm_medium when absent, preserves every existing
  // query param, never overwrites an existing utm_source, and passes
  // non-Unsplash URLs through untouched. Input is an ABSOLUTE validated
  // http(s) URL (the credit render funnel resolves + validates first).
  const withReferral = href => {
    try {
      const u = new URL(href);
      if (!/(^|\.)unsplash\.com$/.test(u.hostname.replace(/\.$/, ''))) {
        return href;
      }
      if (!u.searchParams.has('utm_source')) {
        u.searchParams.set('utm_source', 'claude_design');
      }
      if (!u.searchParams.has('utm_medium')) {
        u.searchParams.set('utm_medium', 'referral');
      }
      return u.toString();
    } catch (e) {
      return href;
    }
  };
  // 2× a ~600px slot in a 1920-wide deck — retina-sharp without making the
  // sidecar enormous. A 1200px WebP at q=0.85 is ~150-300KB.
  const MAX_DIM = 1200;
  // Raster formats only. SVG is excluded (can carry script; createImageBitmap
  // on SVG blobs is inconsistent). GIF is excluded because the canvas
  // re-encode keeps only the first frame, so an animated GIF would silently
  // go still — better to reject than surprise.
  const ACCEPT = ['image/png', 'image/jpeg', 'image/webp', 'image/avif'];

  // ── Shared sidecar store ────────────────────────────────────────────────
  // One fetch + immediate write-on-change for every <image-slot> on the
  // page. Reads via fetch() so viewing works anywhere the HTML and sidecar
  // are served together; writes go through window.omelette.writeFile, which
  // the host allowlists to *.state.json basenames only.
  const subs = new Set();
  let slots = {};
  // ids explicitly cleared before the sidecar fetch resolved — otherwise
  // the merge below can't tell "never set" from "just deleted" and would
  // resurrect the sidecar's stale value.
  const tombstones = new Set();
  let loaded = false;
  let loadP = null;
  function load() {
    if (loadP) return loadP;
    loadP = fetch(STATE_FILE).then(r => r.ok ? r.json() : null).then(j => {
      // Merge: sidecar loses to any in-memory change that raced ahead of
      // the fetch (drop or clear) so neither is clobbered by hydration.
      if (j && typeof j === 'object') {
        const merged = Object.assign({}, j, slots);
        // A framing-only write that raced ahead of hydration must not
        // drop a user image that's only on disk — inherit u from the
        // sidecar for any in-memory entry that lacks one.
        for (const k in slots) {
          if (merged[k] && !merged[k].u && j[k]) {
            merged[k].u = typeof j[k] === 'string' ? j[k] : j[k].u;
          }
        }
        for (const id of tombstones) delete merged[id];
        slots = merged;
      }
      tombstones.clear();
    }).catch(() => {}).then(() => {
      loaded = true;
      subs.forEach(fn => fn());
    });
    return loadP;
  }

  // Serialize writes so two near-simultaneous drops on different slots
  // can't reorder at the backend and leave the sidecar with only the
  // first. A save requested mid-flight just marks dirty and re-fires on
  // completion with the then-current slots.
  let saving = false;
  let saveDirty = false;
  // Unload-time flush: save()'s serialization defers a mid-RTT re-fire to a
  // .then that never runs in an unloading document, silently dropping a
  // pagehide commit. Post the current slots immediately instead — content
  // is a superset snapshot of any in-flight save's, the write is a
  // whole-file last-writer-wins replace, and postMessage FIFO delivers it
  // to the host after the in-flight one, so a backend-side reorder at
  // worst reproduces the dropped-commit outcome this flush improves on.
  // Guarded on the initial sidecar read: pre-hydration slots can miss
  // other slots' persisted entries, and flushing it would clobber them —
  // that narrow case stays best-effort (the in-memory merge in load()
  // cannot happen in an unloading document anyway).
  function flushNow() {
    if (!loaded) return;
    const w = window.omelette && window.omelette.writeFile;
    if (!w) return;
    try {
      Promise.resolve(w(STATE_FILE, JSON.stringify(slots))).catch(() => {});
    } catch (e) {}
  }
  function save() {
    if (saving) {
      saveDirty = true;
      return;
    }
    const w = window.omelette && window.omelette.writeFile;
    if (!w) return;
    saving = true;
    Promise.resolve(w(STATE_FILE, JSON.stringify(slots))).catch(() => {}).then(() => {
      saving = false;
      if (saveDirty) {
        saveDirty = false;
        save();
      }
    });
  }
  const S_MAX = 5;
  const clampS = s => Math.max(1, Math.min(S_MAX, s));

  // Normalize a stored slot value. Pre-reframe sidecars stored a bare
  // data-URL string; newer ones store {u, s, x, y}. Either shape is valid.
  function getSlot(id) {
    const v = slots[id];
    if (!v) return null;
    return typeof v === 'string' ? {
      u: v,
      s: 1,
      x: 0,
      y: 0
    } : v;
  }
  function setSlot(id, val) {
    if (!id) return;
    if (val) {
      slots[id] = val;
      tombstones.delete(id);
    } else {
      delete slots[id];
      if (!loaded) tombstones.add(id);
    }
    subs.forEach(fn => fn());
    // A drop is rare + high-value — write immediately so nav-away can't lose
    // it. Gate on the initial read so we don't overwrite a sidecar we haven't
    // merged yet; the merge in load() keeps this change once the read lands.
    if (loaded) save();else load().then(save);
  }

  // ── Image downscale ─────────────────────────────────────────────────────
  // Encode through a canvas so the sidecar carries resized bytes, not the
  // raw upload. Longest side is capped at 2× the slot's rendered width
  // (retina) and at MAX_DIM. WebP keeps alpha and is ~10× smaller than PNG
  // for photos, so there's no need for per-image format picking.
  async function toDataUrl(file, targetW) {
    const bitmap = await createImageBitmap(file);
    try {
      const cap = Math.min(MAX_DIM, Math.max(1, Math.round(targetW * 2)) || MAX_DIM);
      const scale = Math.min(1, cap / Math.max(bitmap.width, bitmap.height));
      const w = Math.max(1, Math.round(bitmap.width * scale));
      const h = Math.max(1, Math.round(bitmap.height * scale));
      const canvas = document.createElement('canvas');
      canvas.width = w;
      canvas.height = h;
      canvas.getContext('2d').drawImage(bitmap, 0, 0, w, h);
      return canvas.toDataURL('image/webp', 0.85);
    } finally {
      bitmap.close && bitmap.close();
    }
  }

  // ── Custom element ──────────────────────────────────────────────────────
  const stylesheet =
  // Fill the container by default: slots are usually placed inside a
  // sized wrapper (a hero frame, a grid cell, an inset:0 layer) and are
  // expected to take that box — a fixed intrinsic size would render as
  // a small tile in the corner of a full-bleed wrapper instead.
  // aspect-ratio is the companion fallback that keeps a bare slot
  // visible when the parent's height is indefinite: height:100%
  // resolves to auto there, and the ratio then derives height from
  // width instead of letting the slot collapse to zero height.
  // Explicit width/height on the element override all of this.
  ':host{display:block;position:relative;' + '  font:13px/1.3 system-ui,-apple-system,sans-serif;color:rgba(0,0,0,.55);' + '  width:100%;height:100%;aspect-ratio:3/2}' + '.frame{position:absolute;inset:0;overflow:hidden;background:rgba(0,0,0,.04)}' +
  // .frame img (clipped) and .spill (unclipped ghost + handles) share the
  // same left/top/width/height in frame-%, computed by _applyView(), so the
  // inside-mask crop and the outside-mask spill stay pixel-aligned.
  '.frame img{position:absolute;max-width:none;transform:translate(-50%,-50%);' + '  -webkit-user-drag:none;user-select:none;touch-action:none}' +
  // Reframe mode (double-click): the full image spills past the mask. The
  // spill layer is sized to the IMAGE bounds so its corners are where the
  // resize handles belong. The ghost <img> inside is translucent; the real
  // clipped <img> underneath shows the opaque in-mask crop.
  // popover=manual promotes the spill to the top layer on reframe, so it is
  // not clipped by any overflow:hidden / clip-path / scroll-container
  // ancestor (a plain z-index can't escape overflow clipping). UA popover
  // defaults (inset:0;margin:auto) are reset; _applyView sets viewport px.
  '.spill{position:fixed;margin:0;inset:auto;border:0;padding:0;background:transparent;' + '  overflow:visible;transform:translate(-50%,-50%);z-index:1;cursor:grab;touch-action:none}' + ':host([data-panning]) .spill{cursor:grabbing}' + '.spill .ghost{position:absolute;inset:0;width:100%;height:100%;opacity:.35;' + '  pointer-events:none;-webkit-user-drag:none;user-select:none;' + '  box-shadow:0 0 0 1px rgba(0,0,0,.2),0 12px 32px rgba(0,0,0,.2)}' + '.spill .handle{position:absolute;width:12px;height:12px;border-radius:50%;' + '  background:#fff;box-shadow:0 0 0 1.5px #c96442,0 1px 3px rgba(0,0,0,.3);' + '  transform:translate(-50%,-50%)}' + '.spill .handle[data-c=nw]{left:0;top:0;cursor:nwse-resize}' + '.spill .handle[data-c=ne]{left:100%;top:0;cursor:nesw-resize}' + '.spill .handle[data-c=sw]{left:0;top:100%;cursor:nesw-resize}' + '.spill .handle[data-c=se]{left:100%;top:100%;cursor:nwse-resize}' + ':host([data-reframe]){z-index:10}' + ':host([data-reframe]) .frame{box-shadow:0 0 0 2px #c96442}' + '.empty{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;' + '  justify-content:center;gap:6px;text-align:center;padding:12px;box-sizing:border-box;' + '  cursor:pointer;user-select:none}' + '.empty svg{opacity:.45}' + '.empty .cap{max-width:90%;font-weight:500;letter-spacing:.01em}' + '.empty .sub{font-size:11px}' + '.empty .sub u{text-underline-offset:2px;text-decoration-color:rgba(0,0,0,.25)}' + '.empty:hover .sub u{color:rgba(0,0,0,.75);text-decoration-color:currentColor}' + ':host([data-over]) .frame{outline:2px solid #c96442;outline-offset:-2px;' + '  background:rgba(201,100,66,.10)}' + '.ring{position:absolute;inset:0;pointer-events:none;border:1.5px dashed rgba(0,0,0,.25);' + '  transition:border-color .12s}' + ':host([data-over]) .ring{border-color:#c96442}' + ':host([data-filled]) .ring{display:none}' +
  // Controls overlay INSIDE the frame, pinned to the top-right corner, so
  // a full-bleed slot in an overflow:hidden container still shows them
  // (the old below-mask placement got clipped). Credit sits bottom-left,
  // so top-right avoids collision. The blurred pill background keeps them
  // legible over the image.
  // The UA [popover] base rule styles the element in EVERY state (only
  // display:none is gated on :not(:popover-open), and the display:flex
  // below overrides that) — so the UA resets live HERE, like .spill's,
  // or the ordinary hover-state strip renders as a bordered Canvas box
  // centered by margin:auto. inset:auto precedes top/right (shorthand).
  '.ctl{position:absolute;inset:auto;top:8px;right:8px;margin:0;border:0;padding:0;' + '  background:transparent;overflow:visible;' + '  display:flex;gap:6px;opacity:0;pointer-events:none;transition:opacity .12s;z-index:2;' + '  white-space:nowrap}' +
  // While reframing, the spill owns the top layer and would swallow every
  // click on the in-frame controls. Promoting .ctl into the top layer
  // ABOVE the spill (shown after it — later popovers stack higher) keeps
  // Edit-as-toggle and Replace clickable mid-reframe. _applyView pins it
  // to the frame's top-right in viewport px (translateX(-100%)
  // right-aligns against the computed left edge); inset:auto clears the
  // base rule's top/right so the inline left/top position it alone.
  '.ctl:popover-open{position:fixed;inset:auto;transform:translateX(-100%)}' + ':host([data-filled][data-editable]:hover) .ctl,:host([data-reframe]) .ctl' + '  {opacity:1;pointer-events:auto}' + '.ctl button{appearance:none;border:0;border-radius:6px;padding:5px 10px;cursor:pointer;' + '  background:rgba(0,0,0,.65);color:#fff;font:11px/1 system-ui,-apple-system,sans-serif;' + '  backdrop-filter:blur(6px)}' + '.ctl button:hover{background:rgba(0,0,0,.8)}' + '.err{position:absolute;left:8px;bottom:8px;right:8px;color:#b3261e;font-size:11px;' + '  background:rgba(255,255,255,.85);padding:4px 6px;border-radius:5px;pointer-events:none}' +
  // Replacement in flight: after a src swap the browser keeps painting
  // the PREVIOUS image until the new one decodes, so a Replace would
  // flash the old photo and then pop. Hide the stale frame (visibility,
  // not display — _applyView geometry still applies) and spin until the
  // new image reports in (load/error clears data-swapping).
  ':host([data-swapping]) .frame img{visibility:hidden}' + '.loading{position:absolute;inset:0;display:none;align-items:center;' + '  justify-content:center;pointer-events:none}' + ':host([data-swapping]) .loading{display:flex}' + '.loading::after{content:"";width:22px;height:22px;border-radius:50%;' + '  border:2px solid rgba(0,0,0,.12);border-top-color:rgba(0,0,0,.45);' + '  animation:om-slot-spin .7s linear infinite}' + '@keyframes om-slot-spin{to{transform:rotate(360deg)}}' +
  // Reduced motion: the static two-tone ring still reads as "working".
  '@media (prefers-reduced-motion:reduce){.loading::after{animation:none}}' + '.credit{position:absolute;left:6px;bottom:6px;max-width:calc(100% - 12px);display:none;' + '  padding:3px 7px;border-radius:5px;background:rgba(0,0,0,.55);color:#fff;' + '  font:10px/1.2 system-ui,-apple-system,sans-serif;text-decoration:none;' + '  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;backdrop-filter:blur(6px)}' +
  // The credit is a SPAN holding one or two <a>s (Unsplash's prescribed
  // form links the photographer AND Unsplash) — anchors style inline so
  // the overlay reads as one line of text.
  '.credit a{color:inherit;text-decoration:none}' + '.credit a:hover,.credit a:focus-visible{text-decoration:underline}' + ':host([data-filled][data-credit]) .credit{display:block}' +
  // Exports must ship JUST the image — no hover controls, no credit chip
  // (the host marks <html data-om-exporting> for the capture window; the
  // page-level hide script can't reach shadow DOM, this rule can).
  ':host-context([data-om-exporting]) .ctl,' + ':host-context([data-om-exporting]) .credit{display:none !important}' +
  // No export-window mask rules here on purpose: the export capture
  // releases the replacement mask by REMOVING data-swapping (the
  // shadow-root pass in pages/export/shared.ts HIDE_EXPORT_CHROME_SCRIPT)
  // — attribute removal works in every engine (:host-context is
  // Chromium-only), is scoped by construction to slots actually
  // mid-swap, and hides the spinner through the same gate. A masked img
  // would otherwise be silently dropped from PPTX decks (the capture
  // walk skips visibility:hidden imgs).
  // Attribution error tile: REPLACES the photo when an Unsplash src has
  // no credit attribute — rendering the photo uncredited is the terms
  // violation, so the photo must not appear at all.
  // Calm and neutral on purpose (review feedback): the tile informs the
  // user; the fix instructions are machine-facing (usage docblock, tool
  // description, and the turn-end scan's bounce copy name the attributes
  // for the agent).
  '.attr-error{position:absolute;inset:0;display:none;flex-direction:column;align-items:center;' + '  justify-content:center;gap:6px;text-align:center;padding:12px;box-sizing:border-box;' + '  background:#f2f1ef;color:#6e6c66;user-select:none;' + '  font:13px/1.45 system-ui,-apple-system,sans-serif}' + '.attr-error svg{opacity:.55}' + '.attr-error .cap{max-width:92%;font-weight:500;letter-spacing:.01em}' + ':host([data-attribution-error]) .attr-error{display:flex}' + ':host([data-attribution-error]) .ring{display:none}';
  const icon = '<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" ' + 'stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">' + '<rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/>' + '<path d="m21 15-5-5L5 21"/></svg>';
  const warnIcon = '<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" ' + 'stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">' + '<path d="m21.73 18-8-14a2 2 0 0 0-3.46 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3"/>' + '<path d="M12 9v4"/><path d="M12 17h.01"/></svg>';
  class ImageSlot extends HTMLElement {
    static get observedAttributes() {
      return ['shape', 'radius', 'mask', 'fit', 'placeholder', 'src', 'id', 'credit', 'credit-href'];
    }

    /** Duplicate-slide hook (called by deck-stage, see its
     *  _remintDuplicateIds): copy this id's stored image, if any, under a
     *  freshly minted key and return that key — so a duplicated slide's
     *  slot keeps its dropped photo instead of reverting to the
     *  placeholder. 'isFree' is the caller's uniqueness check (document
     *  ids); candidates must ALSO be unused in the sidecar, which can
     *  hold keys from other pages sharing the project root. (An EMPTY
     *  slot on another page leaves no sidecar entry, so its id is not
     *  detectable here — a minted key can collide with it and that slot
     *  would show this photo. Same blast radius as two pages reusing an
     *  id by hand, which the shared sidecar already permits.) Returns null
     *  when no id could be minted (caller strips the id, today's
     *  behavior). */
    static cloneSlot(fromId, isFree) {
      if (typeof fromId !== 'string' || !fromId) return null;
      // Pre-hydration the store can't veto candidates or source the copy
      // — degrade to the strip (today's behavior) rather than mint
      // against keys we can't see yet. Any rendered (= droppable) slot
      // means load() has already settled.
      if (!loaded) return null;
      const stem = fromId.replace(/-\d+$/, '') || fromId;
      for (let n = 2; n < 100; n++) {
        const toId = stem + '-' + n;
        if (toId === fromId) continue;
        if (slots[toId] !== undefined) {
          // Reuse a key holding this exact value (bytes AND crop) if no
          // live element here owns it — a duplicate op the host refused
          // after minting leaves such a key behind, and reusing keeps
          // refused retries from accumulating one orphaned copy per
          // attempt. Full equality (not just bytes) so a byte-identical
          // key another PAGE owns with its own crop is stepped past, not
          // adopted or rewritten. (Entries without .u never match.)
          const prev = getSlot(toId);
          const cur = getSlot(fromId);
          if (!(prev && cur && prev.u && prev.u === cur.u && prev.s === cur.s && prev.x === cur.x && prev.y === cur.y && (typeof isFree !== 'function' || isFree(toId)))) continue;
          return toId;
        }
        if (typeof isFree === 'function' && !isFree(toId)) continue;
        const v = getSlot(fromId);
        if (v) setSlot(toId, Object.assign({}, v));
        return toId;
      }
      return null;
    }
    constructor() {
      super();
      // clonable: rail thumbnails deep-clone slides and carry this shadow
      // along; reuse an already-cloned root so upgrade-after-clone works.
      // (Deliberately NOT serializable — a getHTML consumer would embed
      // multi-MB sidecar data-URLs into serialized page HTML.)
      const root = this.shadowRoot || this.attachShadow({
        mode: 'open',
        clonable: true
      });
      // .spill and .ctl sit OUTSIDE .frame so overflow:hidden + border-radius
      // on the frame (circle, pill, rounded) can't clip them.
      root.innerHTML = '<style>' + stylesheet + '</style>' + '<div class="frame" part="frame">' + '  <img part="image" alt="" draggable="false" style="display:none">' + '  <div class="empty" part="empty">' + icon + '    <div class="cap"></div>' + '    <div class="sub">or <u>browse files</u></div></div>' + '  <div class="attr-error" part="attribution-error">' + warnIcon + '    <div class="cap">This photo needs attribution</div></div>' + '  <div class="loading" part="loading"></div>' + '  <div class="ring" part="ring"></div>' + '</div>' +
      // Outside .frame, like .spill/.ctl — the frame's overflow:hidden +
      // border-radius/clip-path would cut the credit off on circle/pill/mask.
      // A SPAN, not an <a>: the prescribed Unsplash credit holds two links
      // (photographer + Unsplash), built per-render in _render().
      '<span class="credit" part="credit"></span>' + '<div class="spill" popover="manual" data-dc-edit-transparent>' + '  <img class="ghost" alt="" draggable="false">' + '  <div class="handle" data-c="nw"></div><div class="handle" data-c="ne"></div>' + '  <div class="handle" data-c="sw"></div><div class="handle" data-c="se"></div>' + '</div>' +
      // data-dc-edit-transparent: the DC editor's edit-mode picker lets
      // clicks through for chrome marked with it (EDIT_TRANSPARENT_SEL)
      // — without it, Replace/Edit clicks in Edit mode are swallowed by
      // element selection and the controls look dead.
      '<div class="ctl" popover="manual" data-dc-edit-transparent><button data-act="replace" title="Replace image">Replace</button>' + '  <button data-act="edit" title="Reframe image">Edit</button></div>' + '<input type="file" accept="' + ACCEPT.join(',') + '" hidden>';
      this._frame = root.querySelector('.frame');
      this._ring = root.querySelector('.ring');
      this._img = root.querySelector('.frame img');
      this._empty = root.querySelector('.empty');
      this._cap = root.querySelector('.cap');
      this._sub = root.querySelector('.sub');
      this._spill = root.querySelector('.spill');
      this._ctl = root.querySelector('.ctl');
      this._credit = root.querySelector('.credit');
      this._attrError = root.querySelector('.attr-error');
      // Credit clicks open the link, not browse/reframe.
      this._credit.addEventListener('click', e => e.stopPropagation());
      this._credit.addEventListener('dblclick', e => e.stopPropagation());
      this._ghost = root.querySelector('.ghost');
      this._err = null;
      this._input = root.querySelector('input');
      this._depth = 0;
      this._gen = 0;
      // Encode-in-flight marker (the owning _ingest generation): while set,
      // the same-src "nothing in flight" clear in _render must not fire —
      // the stored value still points at the OLD image until the encode
      // lands, so that clear would unmask the stale image mid-replace.
      this._swapGen = 0;
      // Render-owned swap in flight: set when _render assigns a new src,
      // cleared only by the img's own load/error (or the empty branch).
      // img.complete CANNOT stand in for this — setting src only QUEUES
      // the current-request swap (a microtask), so synchronously after an
      // assignment, complete still reports the OLD settled request. The
      // pick path does exactly that: the host sets src, credit, and
      // credit-href back-to-back in one task, and renders #2/#3 would
      // read the stale complete === true and drop the mask one render
      // after it was set.
      this._loadPending = false;
      // See _render's empty branch: a transient attribution-error wipe of a
      // showing image must make the follow-up render a replacement (spinner),
      // not a first fill (blank frame).
      this._hidShowing = false;
      this._view = {
        s: 1,
        x: 0,
        y: 0
      };
      this._subFn = () => this._render();
      // Shadow-DOM listeners live with the shadow DOM — bound once here so
      // disconnect/reconnect (e.g. React remount) doesn't stack handlers.
      this._empty.addEventListener('click', () => this._input.click());
      root.addEventListener('click', e => {
        const act = e.target && e.target.getAttribute && e.target.getAttribute('data-act');
        if (!act) return;
        // The hidden controls are opacity-0 but still tabbable — without
        // this gate a keyboard user could drive them on a read-only share
        // link (mirrors the dblclick handler's editable gate).
        if (!this.hasAttribute('data-editable')) return;
        if (act === 'replace') {
          this._exitReframe(true);
          // Host-owned picker (Unsplash modal; it also offers local import).
          this.dispatchEvent(new CustomEvent('image-slot:pick', {
            bubbles: true,
            composed: true,
            detail: {
              id: this.id || null
            }
          }));
        }
        if (act === 'edit') {
          if (!this._reframes()) return;
          if (this.hasAttribute('data-reframe')) this._exitReframe(true);else this._enterReframe();
        }
      });
      this._input.addEventListener('change', () => {
        const f = this._input.files && this._input.files[0];
        if (f) this._ingest(f);
        this._input.value = '';
      });
      // naturalWidth/Height aren't known until load — re-apply so the cover
      // baseline is computed from real dimensions, not the 100%×100% fallback.
      // load/error also release the replacement-in-flight mask (via the
      // single discipline in _releaseMask): the swap is only revealed once
      // the new image can actually paint (on error the frame shows its
      // background, same as a fresh slot with a broken src).
      this._img.addEventListener('load', () => {
        this._loadPending = false;
        this._releaseMask(true);
        this._applyView();
      });
      this._img.addEventListener('error', () => {
        this._loadPending = false;
        this._releaseMask(true);
      });
      // Gated only on editable — any filled slot can be repositioned/scaled,
      // regardless of fit. Share links (no writeFile) stay static.
      this.addEventListener('dblclick', e => {
        if (!this.hasAttribute('data-editable') || !this._reframes()) return;
        e.preventDefault();
        if (this.hasAttribute('data-reframe')) this._exitReframe(true);else this._enterReframe();
      });
      // Pan + resize both originate on the spill layer. A handle pointerdown
      // drives an aspect-locked resize anchored at the opposite corner; any
      // other pointerdown on the spill pans. Offsets are frame-% so a
      // reframed slot survives responsive resize / PPTX export.
      this._spill.addEventListener('pointerdown', e => {
        if (e.button !== 0 || !this.hasAttribute('data-reframe')) return;
        e.preventDefault();
        e.stopPropagation();
        this._spill.setPointerCapture(e.pointerId);
        const rect = this.getBoundingClientRect();
        const fw = rect.width || 1,
          fh = rect.height || 1;
        const corner = e.target.getAttribute && e.target.getAttribute('data-c');
        let move;
        if (corner) {
          // Resize about the OPPOSITE corner. Viewport-px throughout (rect
          // fw/fh, not clientWidth) so the math survives a transform:scale()
          // ancestor — deck_stage renders slides scaled-to-fit.
          const iw = this._img.naturalWidth || 1,
            ih = this._img.naturalHeight || 1;
          const contain = (this.getAttribute('fit') || 'cover').toLowerCase() === 'contain';
          const base = contain ? Math.min(fw / iw, fh / ih) : Math.max(fw / iw, fh / ih);
          const sx = corner.includes('e') ? 1 : -1;
          const sy = corner.includes('s') ? 1 : -1;
          const s0 = this._view.s;
          const w0 = iw * base * s0,
            h0 = ih * base * s0;
          const cx0 = (50 + this._view.x) / 100 * fw;
          const cy0 = (50 + this._view.y) / 100 * fh;
          const ox = cx0 - sx * w0 / 2,
            oy = cy0 - sy * h0 / 2;
          const diag0 = Math.hypot(w0, h0);
          const ux = sx * w0 / diag0,
            uy = sy * h0 / diag0;
          move = ev => {
            const proj = (ev.clientX - rect.left - ox) * ux + (ev.clientY - rect.top - oy) * uy;
            const s = clampS(s0 * proj / diag0);
            const d = diag0 * s / s0;
            this._view.s = s;
            this._view.x = (ox + ux * d / 2) / fw * 100 - 50;
            this._view.y = (oy + uy * d / 2) / fh * 100 - 50;
            this._clampView();
            this._applyView();
          };
        } else {
          this.setAttribute('data-panning', '');
          const start = {
            px: e.clientX,
            py: e.clientY,
            x: this._view.x,
            y: this._view.y
          };
          move = ev => {
            this._view.x = start.x + (ev.clientX - start.px) / fw * 100;
            this._view.y = start.y + (ev.clientY - start.py) / fh * 100;
            this._clampView();
            this._applyView();
          };
        }
        const up = () => {
          try {
            this._spill.releasePointerCapture(e.pointerId);
          } catch {}
          this._spill.removeEventListener('pointermove', move);
          this._spill.removeEventListener('pointerup', up);
          this._spill.removeEventListener('pointercancel', up);
          this.removeAttribute('data-panning');
          this._dragUp = null;
        };
        // Stashed so _exitReframe (Escape / outside-click mid-drag) can
        // tear the capture + listeners down synchronously.
        this._dragUp = up;
        this._spill.addEventListener('pointermove', move);
        this._spill.addEventListener('pointerup', up);
        this._spill.addEventListener('pointercancel', up);
      });
      // Wheel zoom stays available inside reframe mode as a trackpad nicety —
      // zooms toward the cursor (offset' = cursor·(1-k) + offset·k).
      this.addEventListener('wheel', e => {
        if (!this.hasAttribute('data-reframe')) return;
        e.preventDefault();
        const r = this.getBoundingClientRect();
        const cx = (e.clientX - r.left) / r.width * 100 - 50;
        const cy = (e.clientY - r.top) / r.height * 100 - 50;
        const prev = this._view.s;
        const next = clampS(prev * Math.pow(1.0015, -e.deltaY));
        if (next === prev) return;
        const k = next / prev;
        this._view.s = next;
        this._view.x = cx * (1 - k) + this._view.x * k;
        this._view.y = cy * (1 - k) + this._view.y * k;
        this._clampView();
        this._applyView();
      }, {
        passive: false
      });
    }
    connectedCallback() {
      // Warn once per page — an id-less slot works for the session but
      // cannot persist, and two id-less slots would share nothing.
      if (!this.id && !ImageSlot._warned) {
        ImageSlot._warned = true;
        console.warn('<image-slot> without an id will not persist its dropped image.');
      }
      this.addEventListener('dragenter', this);
      this.addEventListener('dragover', this);
      this.addEventListener('dragleave', this);
      this.addEventListener('drop', this);
      subs.add(this._subFn);
      // The host may inject window.omelette.writeFile AFTER the first render;
      // re-render on hover so the editable-gated controls reliably appear.
      this.addEventListener('pointerenter', this._subFn);
      // width%/height% in _applyView encode the frame aspect at call time —
      // a host resize (responsive grid, pane divider) would stretch the
      // image until the next _render. Re-render on size change: _render()
      // re-seeds _view from stored before clamp/apply, so a shrink→grow
      // cycle round-trips instead of ratcheting x/y toward the narrower
      // frame's clamp range.
      this._ro = new ResizeObserver(() => this._render());
      this._ro.observe(this);
      load();
      this._render();
    }
    disconnectedCallback() {
      subs.delete(this._subFn);
      this.removeEventListener('pointerenter', this._subFn);
      this.removeEventListener('dragenter', this);
      this.removeEventListener('dragover', this);
      this.removeEventListener('dragleave', this);
      this.removeEventListener('drop', this);
      if (this._ro) {
        this._ro.disconnect();
        this._ro = null;
      }
      // commit=false: a disconnect is not a user intent — committing here
      // would persist whatever half-finished drag a React remount or DOM
      // splice happened to interrupt. Deliberate exits commit on their own
      // paths (Escape/click-out/toggle), and unloads commit via pagehide.
      this._exitReframe(false);
    }
    _enterReframe() {
      if (this.hasAttribute('data-reframe')) return;
      this.setAttribute('data-reframe', '');
      this._signalReframe(true);
      // Best-effort commit when the document unloads mid-reframe (a host
      // navigation racing the enter signal, a manual reload, tab close):
      // the sidecar write rides the host bridge, which outlives this
      // document, so the crop survives even though the mode dies with the
      // DOM. Held on the instance so _exitReframe detaches exactly what
      // was attached.
      this._pagehide = () => {
        this._exitReframe(true);
        flushNow();
      };
      window.addEventListener('pagehide', this._pagehide);
      // Promote spill to the top layer, then keep it pinned over the frame:
      // scroll/resize cover the common cases, and a per-frame rect check
      // catches layout shifts that fire neither (an image above finishing
      // load, streamed DOM pushing the slot down, an ancestor transform
      // change) so the overlay can't detach from the frame.
      try {
        this._spill.showPopover();
      } catch {}
      // After the spill, so the controls stack above it in the top layer.
      try {
        this._ctl.showPopover();
      } catch {}
      this._reposition = () => {
        if (this.hasAttribute('data-reframe')) this._applyView();
      };
      window.addEventListener('scroll', this._reposition, true);
      window.addEventListener('resize', this._reposition);
      this._lastRect = '';
      this._watch = () => {
        if (!this.hasAttribute('data-reframe')) return;
        const r = this.getBoundingClientRect();
        const key = r.left + ',' + r.top + ',' + r.width + ',' + r.height;
        if (key !== this._lastRect) {
          this._lastRect = key;
          this._applyView();
        }
        this._watchId = requestAnimationFrame(this._watch);
      };
      this._watchId = requestAnimationFrame(this._watch);
      this._applyView();
      // Close on click outside (the spill handler stopPropagation()s so
      // in-image drags don't reach this) and on Escape. Listeners are held
      // on the instance so _exitReframe / disconnectedCallback can detach
      // exactly what was attached.
      this._outside = e => {
        if (e.composedPath && e.composedPath().includes(this)) return;
        this._exitReframe(true);
      };
      this._esc = e => {
        if (e.key === 'Escape') this._exitReframe(true);
      };
      document.addEventListener('pointerdown', this._outside, true);
      document.addEventListener('keydown', this._esc, true);
    }
    _exitReframe(commit) {
      if (!this.hasAttribute('data-reframe')) return;
      if (this._dragUp) this._dragUp();
      this.removeAttribute('data-reframe');
      this.removeAttribute('data-panning');
      if (this._outside) document.removeEventListener('pointerdown', this._outside, true);
      if (this._esc) document.removeEventListener('keydown', this._esc, true);
      this._outside = this._esc = null;
      if (this._reposition) {
        window.removeEventListener('scroll', this._reposition, true);
        window.removeEventListener('resize', this._reposition);
        this._reposition = null;
      }
      if (this._watchId) {
        cancelAnimationFrame(this._watchId);
        this._watchId = 0;
      }
      if (this._pagehide) {
        window.removeEventListener('pagehide', this._pagehide);
        this._pagehide = null;
      }
      try {
        this._spill.hidePopover();
      } catch {}
      try {
        this._ctl.hidePopover();
      } catch {}
      this._ctl.style.left = '';
      this._ctl.style.top = '';
      if (commit) this._commitView();
      this._signalReframe(false);
    }

    // Reframe state lives only in this DOM until commit, invisible to the
    // host's dirty signals — announce enter/exit so the host can hold
    // auto-reloads for exactly the gesture (the guest bundle forwards
    // image-slot:reframe to the host as imageSlotReframe). Dispatched on
    // the element (composed, so it escapes shadow roots) while connected;
    // a disconnected exit (disconnectedCallback) falls back to document so
    // the host still hears it.
    _signalReframe(active) {
      const target = this.isConnected ? this : document;
      target.dispatchEvent(new CustomEvent('image-slot:reframe', {
        bubbles: true,
        composed: true,
        detail: {
          active: active,
          id: this.id || null
        }
      }));
    }

    // Public: host's "Import from computer" calls this to run local browse.
    openFilePicker() {
      this._exitReframe(true);
      this._input.click();
    }
    attributeChangedCallback() {
      if (this.shadowRoot) this._render();
    }

    // handleEvent — one listener object for all four drag events keeps the
    // add/remove symmetric and the depth counter correct.
    handleEvent(e) {
      if (e.type === 'dragenter' || e.type === 'dragover') {
        // Without preventDefault the browser never fires 'drop'.
        e.preventDefault();
        e.stopPropagation();
        if (e.dataTransfer) e.dataTransfer.dropEffect = 'copy';
        if (e.type === 'dragenter') this._depth++;
        this.setAttribute('data-over', '');
      } else if (e.type === 'dragleave') {
        // dragenter/leave fire for every descendant crossing — count depth
        // so hovering the icon inside the empty state doesn't flicker.
        if (--this._depth <= 0) {
          this._depth = 0;
          this.removeAttribute('data-over');
        }
      } else if (e.type === 'drop') {
        e.preventDefault();
        e.stopPropagation();
        this._depth = 0;
        this.removeAttribute('data-over');
        const f = e.dataTransfer && e.dataTransfer.files && e.dataTransfer.files[0];
        if (f) this._ingest(f);
      }
    }
    async _ingest(file) {
      this._setError(null);
      if (!file || ACCEPT.indexOf(file.type) < 0) {
        this._setError('Drop a PNG, JPEG, WebP, or AVIF image.');
        return;
      }
      // toDataUrl can take hundreds of ms on a large photo. A Clear or a
      // newer drop during that window would be clobbered when this await
      // resumes — bump + capture a generation so stale encodes bail.
      const gen = ++this._gen;
      // Replacing a shown image: surface the swap through the encode too,
      // not just the decode — otherwise the old photo sits there with no
      // feedback while the canvas re-encode runs. An empty slot keeps its
      // placeholder (no spinner) until the encode lands, as before.
      // _swapGen guards the mask against re-renders DURING the encode
      // (pointerenter, ResizeObserver, another slot's store write): the
      // stored value still resolves to the old image there, so _render's
      // same-src clear would otherwise unmask it mid-replace.
      if (this.hasAttribute('data-filled')) {
        this.setAttribute('data-swapping', '');
        this._swapGen = gen;
      }
      try {
        const w = this.clientWidth || this.offsetWidth || MAX_DIM;
        const url = await toDataUrl(file, w);
        if (gen !== this._gen) return;
        // Only exit reframe once the new image is in hand — a rejected type
        // or decode failure leaves the in-progress crop untouched.
        this._exitReframe(false);
        // Clear BEFORE setSlot: its synchronous re-render must see no
        // pending encode, so a byte-identical re-upload (same data URL, no
        // load event coming) still clears the mask via the complete branch.
        this._swapGen = 0;
        const val = {
          u: url,
          s: 1,
          x: 0,
          y: 0
        };
        setSlot(this.id || '', val);
        // Keep a session-local copy for id-less slots so the drop still
        // shows, even though it cannot persist.
        if (!this.id) {
          this._local = val;
          this._render();
        }
      } catch (err) {
        if (gen !== this._gen) return;
        this._swapGen = 0;
        // Reveal the kept old image — unless another replacement (a
        // remote pick's src swap) is still in flight, in which case the
        // mask stays until THAT image settles (its load/error releases).
        this._releaseMask();
        this._setError('Could not read that image.');
        console.warn('<image-slot> ingest failed:', err);
      }
    }
    _setError(msg) {
      if (this._err) {
        this._err.remove();
        this._err = null;
      }
      if (!msg) return;
      const d = document.createElement('div');
      d.className = 'err';
      d.textContent = msg;
      this.shadowRoot.appendChild(d);
      this._err = d;
      setTimeout(() => {
        if (this._err === d) {
          d.remove();
          this._err = null;
        }
      }, 3000);
    }

    // Reframing (pan/resize) is available on any filled slot — the user can
    // always reposition/scale. `fit` only sets the initial baseline (see
    // _geom): contain starts fully-visible, cover starts frame-filling.
    _reframes() {
      return this.hasAttribute('data-filled');
    }

    // The single release discipline for the replacement-in-flight mask
    // (data-swapping). The mask comes off only when BOTH hold:
    //  - no encode is pending (_swapGen) — mid-encode the stored value
    //    still resolves to the old image, so any reveal paints it;
    //  - the frame img has settled on its current src — an unsettled src
    //    means some replacement is still in flight (e.g. a remote pick),
    //    whoever started it, and revealing would paint the previous
    //    frame. The load/error listeners pass settled=true (the event IS
    //    the settlement signal, per spec complete is true by then);
    //    other callers rely on the complete flag (covers loaded AND
    //    failed).
    // Every release path funnels through here EXCEPT _render's empty
    // branch (the img is being cleared — nothing will ever settle).
    _releaseMask(settled) {
      if (!this._swapGen && !this._loadPending && (settled || this._img.complete)) {
        this.removeAttribute('data-swapping');
      }
    }

    // Baseline geometry, shared by clamp/apply/resize. `base` is the scale at
    // view-scale s=1: cover = fill the frame (overflow on the looser axis),
    // contain = fit fully inside (letterboxed). Zooming a contain image past
    // s where it overflows naturally becomes a crop. Null until the img has
    // loaded (naturalWidth is 0 before that) or when the slot has no layout
    // box — ResizeObserver fires with a 0×0 rect under display:none, and
    // clamping against a degenerate 1×1 frame would silently pull the stored
    // pan toward zero.
    _geom() {
      const iw = this._img.naturalWidth,
        ih = this._img.naturalHeight;
      const fw = this.clientWidth,
        fh = this.clientHeight;
      if (!iw || !ih || !fw || !fh) return null;
      const contain = (this.getAttribute('fit') || 'cover').toLowerCase() === 'contain';
      const base = contain ? Math.min(fw / iw, fh / ih) : Math.max(fw / iw, fh / ih);
      return {
        iw,
        ih,
        fw,
        fh,
        base
      };
    }
    _clampView() {
      // Pan range on each axis is half the overflow past the frame edge.
      const g = this._geom();
      if (!g) return;
      const mx = Math.max(0, (g.iw * g.base * this._view.s / g.fw - 1) * 50);
      const my = Math.max(0, (g.ih * g.base * this._view.s / g.fh - 1) * 50);
      this._view.x = Math.max(-mx, Math.min(mx, this._view.x));
      this._view.y = Math.max(-my, Math.min(my, this._view.y));
    }
    _applyView() {
      const g = this._geom();
      // Top-layer controls: pin to the frame's top-right in viewport px
      // (the same 8px inset as the in-frame layout; unscaled — top-layer UI
      // reads as chrome, not page content). BEFORE the geometry branch:
      // placement needs only the frame rect, and a not-yet-loaded or broken
      // src must not leave the promoted strip floating unpositioned. Gated
      // on the popover actually being open: without the Popover API,
      // showPopover() threw (swallowed in _enterReframe), .ctl stays in
      // its in-frame absolute layout, and viewport-px coordinates would
      // shove it off-frame — and matches(':popover-open') itself throws
      // there (unknown pseudo-class), hence the try/catch.
      if (this.hasAttribute('data-reframe')) {
        let onTop = false;
        try {
          onTop = this._ctl.matches(':popover-open');
        } catch {}
        if (onTop) {
          const r = this.getBoundingClientRect();
          this._ctl.style.left = r.right - 8 + 'px';
          this._ctl.style.top = r.top + 8 + 'px';
        }
      }
      if (!g) {
        // Dimensions not known yet (before img load) — centered fit so there
        // is no flash of an unpositioned image before the geometry lands.
        const contain = (this.getAttribute('fit') || 'cover').toLowerCase() === 'contain';
        this._img.style.width = '100%';
        this._img.style.height = '100%';
        this._img.style.left = '50%';
        this._img.style.top = '50%';
        this._img.style.objectFit = contain ? 'contain' : 'cover';
        return;
      }
      // Baseline (cover-fill or contain-fit) × view scale. Width/height and
      // left/top are all frame-% — depends only on the frame aspect ratio, so
      // a responsive resize keeps the same crop. The spill layer mirrors the
      // same box so its corners = image corners.
      const k = g.base * this._view.s;
      const w = g.iw * k / g.fw * 100 + '%';
      const h = g.ih * k / g.fh * 100 + '%';
      const l = 50 + this._view.x + '%';
      const t = 50 + this._view.y + '%';
      this._img.style.width = w;
      this._img.style.height = h;
      this._img.style.left = l;
      this._img.style.top = t;
      this._img.style.objectFit = '';
      if (this.hasAttribute('data-reframe')) {
        // Top-layer spill: position in viewport px over the frame. The top
        // layer escapes ancestor transforms entirely, so EVERY term must be
        // in viewport units: getBoundingClientRect gives the frame's scaled
        // origin AND size, and the rect/layout ratio rescales the ghost —
        // sizing from layout px alone renders it 1/scale too large under a
        // scaled deck slide. Inner ghost + handles stay box-relative.
        const r = this.getBoundingClientRect();
        const sx = g.fw ? r.width / g.fw : 1;
        const sy = g.fh ? r.height / g.fh : 1;
        this._spill.style.width = g.iw * k * sx + 'px';
        this._spill.style.height = g.ih * k * sy + 'px';
        this._spill.style.left = r.left + (50 + this._view.x) / 100 * r.width + 'px';
        this._spill.style.top = r.top + (50 + this._view.y) / 100 * r.height + 'px';
      }
    }
    _commitView() {
      const v = {
        s: this._view.s,
        x: this._view.x,
        y: this._view.y
      };
      if (this._userUrl) v.u = this._userUrl;
      // Framing-only (no u) persists too so an author-src slot remembers its
      // crop; clearing the sidecar still falls through to src=.
      if (this.id) setSlot(this.id, v);else {
        this._local = v;
      }
    }
    _render() {
      // Shape / mask. Presets use border-radius so the dashed ring can
      // follow the rounded outline; clip-path is only applied for an
      // explicit `mask` (the ring is hidden there since a rectangle
      // dashed border chopped by an arbitrary polygon looks broken).
      const mask = this.getAttribute('mask');
      const shape = (this.getAttribute('shape') || 'rounded').toLowerCase();
      let radius = '';
      if (shape === 'circle') radius = '50%';else if (shape === 'pill') radius = '9999px';else if (shape === 'rounded') {
        const n = parseFloat(this.getAttribute('radius'));
        radius = (Number.isFinite(n) ? n : 12) + 'px';
      }
      this._frame.style.borderRadius = mask ? '' : radius;
      this._frame.style.clipPath = mask || '';
      this._ring.style.borderRadius = mask ? '' : radius;
      this._ring.style.display = mask ? 'none' : '';

      // Controls and reframe entry gate on this so share links stay read-only.
      const editable = !!(window.omelette && window.omelette.writeFile);
      this.toggleAttribute('data-editable', editable);
      this._sub.style.display = editable ? '' : 'none';

      // Content. The sidecar is also writable by the agent's write_file
      // tool, so its value isn't guaranteed canvas-originated — only accept
      // data:image/ URLs from it. The `src` attribute is author-controlled
      // (Claude wrote it into the HTML) so it passes through unchanged.
      let stored = this.id ? getSlot(this.id) : this._local;
      if (stored && stored.u && !/^data:image\//i.test(stored.u)) stored = null;
      const srcAttr = this.getAttribute('src') || '';
      this._userUrl = stored && stored.u || null;
      const url = this._userUrl || srcAttr;
      // Don't clobber an in-flight reframe with a store-triggered re-render.
      if (!this.hasAttribute('data-reframe')) {
        this._view = {
          s: stored && Number.isFinite(stored.s) ? clampS(stored.s) : 1,
          x: stored && Number.isFinite(stored.x) ? stored.x : 0,
          y: stored && Number.isFinite(stored.y) ? stored.y : 0
        };
      }
      this._cap.textContent = this.getAttribute('placeholder') || 'Drop an image';
      // Toggle via style.display — the [hidden] attribute alone loses to
      // the display:flex / display:block rules in the stylesheet above.
      // An Unsplash src with no credit attribute must NOT render — showing
      // the photo uncredited is the Unsplash-terms violation itself. The
      // error tile replaces the photo until the credit is written. A
      // user-dropped image is the user's own content and always renders.
      // Trimmed: credit is agent/user-editable content, and a whitespace-
      // only value must count as missing — otherwise it would suppress the
      // error tile AND render an empty credit box (no text, no links),
      // exactly the unattributed state this gate exists to prevent.
      const credit = (this.getAttribute('credit') || '').trim();
      const attrError = !!(!credit && !this._userUrl && srcAttr && isUnsplashHost(srcAttr));
      this.toggleAttribute('data-attribution-error', attrError);
      if (url && !attrError) {
        const prev = this._img.getAttribute('src');
        if (prev !== url) {
          // Replacing an already-shown image: mark the swap BEFORE setting
          // src so the stale frame is never revealed (see the data-swapping
          // stylesheet rules). First fill (prev empty) keeps the existing
          // placeholder-until-load behavior — no spinner. _hidShowing
          // covers the pick path's transient attribution-error wipe: prev
          // is gone, but an image WAS showing, so this is a replacement.
          if (prev || this._hidShowing) this.setAttribute('data-swapping', '');
          // Mark the swap BEFORE assigning src: complete keeps reporting
          // the old settled request until the browser's
          // update-the-image-data microtask runs, so same-task re-renders
          // (the pick path's credit/credit-href setAttributes) need this
          // flag, not complete, to know a load is in flight.
          this._loadPending = true;
          this._img.src = url;
          this._ghost.src = url;
        } else {
          // Same-src re-render — release if settled, so an ingest-set
          // spinner can't stick after a byte-identical re-upload (same
          // data URL, no further load event ever fires).
          this._releaseMask();
        }
        this._hidShowing = false;
        this._img.style.display = 'block';
        this._empty.style.display = 'none';
        this.setAttribute('data-filled', '');
        this._clampView();
        this._applyView();
      } else {
        this.removeAttribute('data-swapping');
        // The src is being removed — no load/error will ever fire for it.
        this._loadPending = false;
        // A transient attribution-error wipe of a showing image happens on
        // the pick path: the host sets src one setAttribute before credit,
        // so render N hides the old image (attrError) and render N+1
        // restores a URL. Remember the wipe so that restore renders as a
        // replacement (spinner), not a first fill (blank frame).
        this._hidShowing = attrError && !!this._img.getAttribute('src');
        this._img.style.display = 'none';
        this._img.removeAttribute('src');
        this._ghost.removeAttribute('src');
        // The error tile owns the blocked-photo state; .empty stays for
        // the genuinely-empty slot.
        this._empty.style.display = attrError ? 'none' : 'flex';
        this.removeAttribute('data-filled');
      }

      // Credit belongs to the author src, so a user drop hides it.
      // textContent + the http(s)-only funnel keep external strings inert.
      const showCredit = !!(url && credit && !this._userUrl && !attrError);
      this._credit.textContent = '';
      if (showCredit) {
        // Validate once (resolved against the document, http(s) only),
        // then append the terms-required utm referral params to links
        // that point back at unsplash.com.
        let href = '';
        const rawHref = this.getAttribute('credit-href') || '';
        if (rawHref) {
          try {
            const u = new URL(rawHref, document.baseURI);
            if (u.protocol === 'http:' || u.protocol === 'https:') {
              href = withReferral(u.href);
            }
          } catch {}
        }
        const mkLink = (text, linkHref) => {
          const a = document.createElement('a');
          a.setAttribute('target', '_blank');
          a.setAttribute('rel', 'noopener noreferrer');
          a.setAttribute('href', linkHref);
          a.textContent = text;
          return a;
        };
        // Unsplash's prescribed credit is TWO links — the photographer's
        // name to their profile (credit-href) and 'Unsplash' to the
        // homepage. Render that split whenever the text has the canonical
        // shape; other text keeps the legacy single-link rendering.
        const m = /^Photo by (.+) on Unsplash$/.exec(credit);
        if (m) {
          this._credit.appendChild(document.createTextNode('Photo by '));
          this._credit.appendChild(href ? mkLink(m[1], href) : document.createTextNode(m[1]));
          this._credit.appendChild(document.createTextNode(' on '));
          this._credit.appendChild(mkLink('Unsplash', UNSPLASH_HOMEPAGE_HREF));
        } else if (href) {
          this._credit.appendChild(mkLink(credit, href));
        } else {
          this._credit.textContent = credit;
        }
      }
      this.toggleAttribute('data-credit', showCredit);
    }
  }
  if (!customElements.get('image-slot')) {
    customElements.define('image-slot', ImageSlot);
  }
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "image-slot.js", error: String((e && e.message) || e) }); }

// tokens/tokens.js
try { (() => {
// CyberSkill design tokens — generated from tokens/*.css (v2.11.0). Do not edit by hand.
const tokens = {
  "$meta": {
    "system": "CyberSkill Design System",
    "version": "2.11.0",
    "generated": "2026-07-19",
    "note": "Generated from tokens/*.css. Root grouped by category; theme/element/expression scopes carry overrides only. Regenerate after any token change (see docs/conventions.md)."
  },
  "root": {
    "color": {
      "--cs-color-brand-umber": "#45210E",
      "--cs-color-brand-ochre": "#F4BA17",
      "--cs-color-surface-page": "#FFFDF8",
      "--cs-color-surface-panel": "#FFFFFF",
      "--cs-color-surface-raised": "#FBF4E9",
      "--cs-color-text-primary": "#45210E",
      "--cs-color-text-inverse": "#FFFFFF",
      "--cs-color-text-accent": "#6E3B0E",
      "--cs-color-text-muted": "#6E5A4C",
      "--cs-color-semantic-success": "#166534",
      "--cs-color-semantic-danger": "#B42318",
      "--cs-color-semantic-warning": "#92400E",
      "--cs-color-semantic-info": "#1D4ED8",
      "--cs-color-accent-ochre": "#F4BA17",
      "--cs-color-border-default": "#E7D9C6",
      "--cs-color-link": "#6E3B0E",
      "--cs-color-link-hover": "#45210E"
    },
    "component": {
      "--cs-component-button-primary-bg": "#45210E",
      "--cs-component-button-primary-fg": "#FFFFFF",
      "--cs-component-button-gap": "8px",
      "--cs-component-button-radius": "8px",
      "--cs-component-button-sm-paddingY": "8px",
      "--cs-component-button-sm-paddingX": "14px",
      "--cs-component-button-sm-minHeight": "36px",
      "--cs-component-button-md-paddingY": "12px",
      "--cs-component-button-md-paddingX": "20px",
      "--cs-component-button-md-minHeight": "44px",
      "--cs-component-button-lg-paddingY": "16px",
      "--cs-component-button-lg-paddingX": "28px",
      "--cs-component-button-lg-minHeight": "52px",
      "--cs-component-textfield-border-default": "#BFA58F",
      "--cs-component-textfield-paddingY": "10px",
      "--cs-component-textfield-paddingX": "12px",
      "--cs-component-textfield-minHeight": "44px"
    },
    "accent": {
      "--cs-accent": "#F4BA17",
      "--cs-accent-strong": "#B98200",
      "--cs-accent-bright": "#F4BA17",
      "--cs-accent-on": "#45210E",
      "--cs-accent-tint": "#FBF4E9",
      "--cs-accent-ink": "#45210E",
      "--cs-accent-glow": "rgba(244, 186, 23, 0.5)",
      "--cs-accent-grad-a": "#F4BA17",
      "--cs-accent-grad-b": "#C77B4A"
    },
    "depth": {
      "--cs-depth-bg": "0",
      "--cs-depth-section": "5",
      "--cs-depth-card": "10",
      "--cs-depth-nav": "50",
      "--cs-depth-modal": "100",
      "--cs-depth-toast": "200"
    },
    "glass": {
      "--cs-glass-whisper-blur": "8px",
      "--cs-glass-whisper-saturate": "110%",
      "--cs-glass-whisper-opacity": "60%",
      "--cs-glass-light-blur": "12px",
      "--cs-glass-light-saturate": "130%",
      "--cs-glass-light-opacity": "70%",
      "--cs-glass-standard-blur": "16px",
      "--cs-glass-standard-saturate": "150%",
      "--cs-glass-standard-opacity": "80%",
      "--cs-glass-heavy-blur": "24px",
      "--cs-glass-heavy-saturate": "180%",
      "--cs-glass-heavy-opacity": "90%"
    },
    "shadow": {
      "--cs-shadow-xs": "0 1px 2px rgba(69, 33, 14, 0.06)",
      "--cs-shadow-sm": "0 1px 3px rgba(69, 33, 14, 0.04), 0 8px 24px rgba(69, 33, 14, 0.06)",
      "--cs-shadow-md": "0 12px 30px rgba(31, 19, 11, 0.18)",
      "--cs-shadow-lg": "0 20px 60px rgba(69, 33, 14, 0.18)",
      "--cs-shadow-xl": "0 24px 80px rgba(31, 19, 11, 0.26)"
    },
    "duration": {
      "--cs-duration-instant": "0ms",
      "--cs-duration-fast": "0ms",
      "--cs-duration-base": "0ms",
      "--cs-duration-slow": "0ms"
    },
    "ease": {
      "--cs-ease-standard": "cubic-bezier(0.2, 0, 0, 1)",
      "--cs-ease-entrance": "cubic-bezier(0, 0, 0, 1)",
      "--cs-ease-exit": "cubic-bezier(0.4, 0, 1, 1)",
      "--cs-ease-emphasized": "cubic-bezier(0.2, 0, 0, 1)"
    },
    "space": {
      "--cs-space-0": "0px",
      "--cs-space-1": "4px",
      "--cs-space-2": "8px",
      "--cs-space-3": "12px",
      "--cs-space-4": "16px",
      "--cs-space-5": "20px",
      "--cs-space-6": "24px",
      "--cs-space-8": "32px",
      "--cs-space-10": "40px",
      "--cs-space-12": "48px",
      "--cs-space-16": "64px",
      "--cs-space-20": "80px",
      "--cs-space-24": "96px"
    },
    "radius": {
      "--cs-radius-sm": "4px",
      "--cs-radius-md": "8px",
      "--cs-radius-lg": "14px",
      "--cs-radius-full": "999px"
    },
    "breakpoint": {
      "--cs-breakpoint-xs": "0px",
      "--cs-breakpoint-sm": "640px",
      "--cs-breakpoint-md": "768px",
      "--cs-breakpoint-lg": "1024px",
      "--cs-breakpoint-xl": "1280px",
      "--cs-breakpoint-2xl": "1536px"
    },
    "container": {
      "--cs-container-max": "1320px"
    },
    "font": {
      "--cs-font-family-ui": "'Be Vietnam Pro', ui-sans-serif, system-ui, sans-serif",
      "--cs-font-family-mono": "'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace",
      "--cs-font-weight-regular": "400",
      "--cs-font-weight-medium": "500",
      "--cs-font-weight-semibold": "600",
      "--cs-font-weight-bold": "700",
      "--cs-font-weight-extrabold": "800",
      "--cs-font-lineHeight-body": "1.5",
      "--cs-font-lineHeight-heading": "1.35",
      "--cs-font-lineHeight-tight": "1.15",
      "--cs-font-size-xs": "0.75rem",
      "--cs-font-size-sm": "0.875rem",
      "--cs-font-size-base": "1rem",
      "--cs-font-size-md": "1.125rem",
      "--cs-font-size-lg": "1.25rem",
      "--cs-font-size-xl": "1.5rem",
      "--cs-font-size-2xl": "1.875rem",
      "--cs-font-size-3xl": "2.25rem",
      "--cs-font-size-4xl": "3rem",
      "--cs-font-size-5xl": "3.75rem"
    },
    "text": {
      "--cs-text-display": "var(--cs-font-size-5xl)",
      "--cs-text-h1": "var(--cs-font-size-3xl)",
      "--cs-text-h2": "var(--cs-font-size-2xl)",
      "--cs-text-h3": "var(--cs-font-size-xl)",
      "--cs-text-h4": "var(--cs-font-size-lg)",
      "--cs-text-body-lg": "var(--cs-font-size-md)",
      "--cs-text-body": "var(--cs-font-size-base)",
      "--cs-text-secondary": "var(--cs-font-size-sm)",
      "--cs-text-caption": "var(--cs-font-size-xs)",
      "--cs-text-eyebrow": "var(--cs-font-size-xs)"
    },
    "letter": {
      "--cs-letter-spacing-tight": "-0.01em",
      "--cs-letter-spacing-normal": "0",
      "--cs-letter-spacing-caps": "0.08em"
    },
    "icon": {
      "--cs-icon-sm": "16px",
      "--cs-icon-md": "20px",
      "--cs-icon-lg": "24px"
    }
  },
  "themes": {
    "dark": {
      "--cs-color-surface-page": "#1a1108",
      "--cs-color-surface-panel": "#221710",
      "--cs-color-surface-raised": "#2b1e14",
      "--cs-color-text-primary": "#f5ead9",
      "--cs-color-text-inverse": "#1a1108",
      "--cs-color-text-accent": "#f4ba17",
      "--cs-color-text-muted": "#c9b7a3",
      "--cs-color-semantic-success": "#4ADE80",
      "--cs-color-semantic-danger": "#F87171",
      "--cs-color-semantic-warning": "#FBBF24",
      "--cs-color-semantic-info": "#7CB2FB",
      "--cs-color-border-default": "#4a3a2c",
      "--cs-color-link": "#f4ba17",
      "--cs-color-link-hover": "#f5ead9",
      "--cs-component-button-primary-bg": "#f4ba17",
      "--cs-component-button-primary-fg": "#1a1108",
      "--cs-component-textfield-border-default": "#5a4636",
      "--cs-shadow-xs": "0 1px 2px rgba(0, 0, 0, 0.40)",
      "--cs-shadow-sm": "0 1px 3px rgba(0, 0, 0, 0.34), 0 8px 24px rgba(0, 0, 0, 0.42)",
      "--cs-shadow-md": "0 12px 30px rgba(0, 0, 0, 0.50)",
      "--cs-shadow-lg": "0 20px 60px rgba(0, 0, 0, 0.55)",
      "--cs-shadow-xl": "0 24px 80px rgba(0, 0, 0, 0.62)"
    },
    "system": {
      "--cs-color-surface-page": "#1a1108",
      "--cs-color-surface-panel": "#221710",
      "--cs-color-surface-raised": "#2b1e14",
      "--cs-color-text-primary": "#f5ead9",
      "--cs-color-text-inverse": "#1a1108",
      "--cs-color-text-accent": "#f4ba17",
      "--cs-color-text-muted": "#c9b7a3",
      "--cs-color-semantic-success": "#4ADE80",
      "--cs-color-semantic-danger": "#F87171",
      "--cs-color-semantic-warning": "#FBBF24",
      "--cs-color-semantic-info": "#7CB2FB",
      "--cs-color-border-default": "#4a3a2c",
      "--cs-color-link": "#f4ba17",
      "--cs-color-link-hover": "#f5ead9",
      "--cs-component-button-primary-bg": "#f4ba17",
      "--cs-component-button-primary-fg": "#1a1108",
      "--cs-component-textfield-border-default": "#5a4636",
      "--cs-shadow-xs": "0 1px 2px rgba(0, 0, 0, 0.40)",
      "--cs-shadow-sm": "0 1px 3px rgba(0, 0, 0, 0.34), 0 8px 24px rgba(0, 0, 0, 0.42)",
      "--cs-shadow-md": "0 12px 30px rgba(0, 0, 0, 0.50)",
      "--cs-shadow-lg": "0 20px 60px rgba(0, 0, 0, 0.55)",
      "--cs-shadow-xl": "0 24px 80px rgba(0, 0, 0, 0.62)"
    }
  },
  "elements": {
    "tho": {
      "--cs-accent": "#F4BA17",
      "--cs-accent-strong": "#B98200",
      "--cs-accent-bright": "#F4BA17",
      "--cs-accent-on": "#45210E",
      "--cs-accent-tint": "#FBF4E9",
      "--cs-accent-ink": "#45210E",
      "--cs-accent-glow": "rgba(244, 186, 23, 0.5)",
      "--cs-accent-grad-a": "#F4BA17",
      "--cs-accent-grad-b": "#C77B4A"
    },
    "tho.clay": {
      "--cs-accent": "#C77B4A",
      "--cs-accent-strong": "#8F5230",
      "--cs-accent-bright": "#EDBF9D",
      "--cs-accent-on": "#2E1607",
      "--cs-accent-tint": "#F8EDE4",
      "--cs-accent-ink": "#2E1607",
      "--cs-accent-glow": "rgba(199, 123, 74, 0.5)",
      "--cs-accent-grad-a": "#C77B4A",
      "--cs-accent-grad-b": "#F4BA17",
      "--cs-color-text-accent": "#8F5230",
      "--cs-color-link": "#8F5230"
    },
    "tho.sand": {
      "--cs-accent": "#E4C98F",
      "--cs-accent-strong": "#A98B4E",
      "--cs-accent-bright": "#EFDCB2",
      "--cs-accent-on": "#3A2A10",
      "--cs-accent-tint": "#FAF3E3",
      "--cs-accent-ink": "#3A2A10",
      "--cs-accent-glow": "rgba(228, 201, 143, 0.55)",
      "--cs-accent-grad-a": "#E4C98F",
      "--cs-accent-grad-b": "#F4BA17",
      "--cs-color-text-accent": "#8A6F3C",
      "--cs-color-link": "#8A6F3C"
    },
    "hoa": {
      "--cs-accent": "#E0632B",
      "--cs-accent-strong": "#A93E14",
      "--cs-accent-bright": "#F6A26B",
      "--cs-accent-on": "#231007",
      "--cs-accent-tint": "#FBEADF",
      "--cs-accent-ink": "#231007",
      "--cs-accent-glow": "rgba(224, 99, 43, 0.5)",
      "--cs-accent-grad-a": "#E0632B",
      "--cs-accent-grad-b": "#F4BA17",
      "--cs-color-text-accent": "#A93E14",
      "--cs-color-link": "#A93E14"
    },
    "hoa.lava": {
      "--cs-accent": "#C43D1F",
      "--cs-accent-strong": "#7E1F0E",
      "--cs-accent-bright": "#F6AC85",
      "--cs-accent-on": "#1D0B05",
      "--cs-accent-tint": "#FAE5DC",
      "--cs-accent-ink": "#1D0B05",
      "--cs-accent-glow": "rgba(196, 61, 31, 0.55)",
      "--cs-accent-grad-a": "#C43D1F",
      "--cs-accent-grad-b": "#E0632B",
      "--cs-color-text-accent": "#9B2D14",
      "--cs-color-link": "#9B2D14"
    },
    "hoa.plasma": {
      "--cs-accent": "#E8506E",
      "--cs-accent-strong": "#B02348",
      "--cs-accent-bright": "#F7A8B8",
      "--cs-accent-on": "#200812",
      "--cs-accent-tint": "#FBE6EB",
      "--cs-accent-ink": "#200812",
      "--cs-accent-glow": "rgba(232, 80, 110, 0.5)",
      "--cs-accent-grad-a": "#E8506E",
      "--cs-accent-grad-b": "#F08A52",
      "--cs-color-text-accent": "#B02348",
      "--cs-color-link": "#B02348"
    },
    "thuy": {
      "--cs-accent": "#4E8E9B",
      "--cs-accent-strong": "#2F5E6B",
      "--cs-accent-bright": "#A6D0DA",
      "--cs-accent-on": "#0E1D20",
      "--cs-accent-tint": "#E6F1F2",
      "--cs-accent-ink": "#0E1D20",
      "--cs-accent-glow": "rgba(78, 142, 155, 0.5)",
      "--cs-accent-grad-a": "#4E8E9B",
      "--cs-accent-grad-b": "#7A9B57",
      "--cs-color-text-accent": "#2F5E6B",
      "--cs-color-link": "#2F5E6B"
    },
    "thuy.ocean": {
      "--cs-accent": "#2E5E7E",
      "--cs-accent-strong": "#1C3E56",
      "--cs-accent-bright": "#A9CCE0",
      "--cs-accent-on": "#0B161E",
      "--cs-accent-tint": "#E4EEF4",
      "--cs-accent-ink": "#0B161E",
      "--cs-accent-glow": "rgba(46, 94, 126, 0.55)",
      "--cs-accent-grad-a": "#2E5E7E",
      "--cs-accent-grad-b": "#4E8E9B",
      "--cs-color-text-accent": "#1C3E56",
      "--cs-color-link": "#1C3E56"
    },
    "thuy.mist": {
      "--cs-accent": "#A7C4C9",
      "--cs-accent-strong": "#5E7F85",
      "--cs-accent-bright": "#C4DADD",
      "--cs-accent-on": "#16262A",
      "--cs-accent-tint": "#F0F6F7",
      "--cs-accent-ink": "#16262A",
      "--cs-accent-glow": "rgba(167, 196, 201, 0.55)",
      "--cs-accent-grad-a": "#A7C4C9",
      "--cs-accent-grad-b": "#4E8E9B",
      "--cs-color-text-accent": "#4A686E",
      "--cs-color-link": "#4A686E"
    },
    "moc": {
      "--cs-accent": "#7A9B57",
      "--cs-accent-strong": "#4C6B35",
      "--cs-accent-bright": "#A9C177",
      "--cs-accent-on": "#18200F",
      "--cs-accent-tint": "#EEF3E2",
      "--cs-accent-ink": "#18200F",
      "--cs-accent-glow": "rgba(122, 155, 87, 0.5)",
      "--cs-accent-grad-a": "#7A9B57",
      "--cs-accent-grad-b": "#E0632B",
      "--cs-color-text-accent": "#4C6B35",
      "--cs-color-link": "#4C6B35"
    },
    "moc.bamboo": {
      "--cs-accent": "#A9C177",
      "--cs-accent-strong": "#6B8442",
      "--cs-accent-bright": "#C4D69B",
      "--cs-accent-on": "#1C240F",
      "--cs-accent-tint": "#F3F7E8",
      "--cs-accent-ink": "#1C240F",
      "--cs-accent-glow": "rgba(169, 193, 119, 0.55)",
      "--cs-accent-grad-a": "#A9C177",
      "--cs-accent-grad-b": "#7A9B57",
      "--cs-color-text-accent": "#5B7237",
      "--cs-color-link": "#5B7237"
    },
    "moc.forest": {
      "--cs-accent": "#3E5A2E",
      "--cs-accent-strong": "#263A1B",
      "--cs-accent-bright": "#B3CE9B",
      "--cs-accent-on": "#101708",
      "--cs-accent-tint": "#E9EFE1",
      "--cs-accent-ink": "#101708",
      "--cs-accent-glow": "rgba(62, 90, 46, 0.55)",
      "--cs-accent-grad-a": "#3E5A2E",
      "--cs-accent-grad-b": "#7A9B57",
      "--cs-color-text-accent": "#31481F",
      "--cs-color-link": "#31481F"
    },
    "kim": {
      "--cs-accent": "#BFB29B",
      "--cs-accent-strong": "#7E7160",
      "--cs-accent-bright": "#D8CCB6",
      "--cs-accent-on": "#221C12",
      "--cs-accent-tint": "#F3EFE6",
      "--cs-accent-ink": "#221C12",
      "--cs-accent-glow": "rgba(191, 178, 155, 0.5)",
      "--cs-accent-grad-a": "#BFB29B",
      "--cs-accent-grad-b": "#4E8E9B",
      "--cs-color-text-accent": "#6E6151",
      "--cs-color-link": "#6E6151"
    },
    "kim.steel": {
      "--cs-accent": "#9FA3A3",
      "--cs-accent-strong": "#63696B",
      "--cs-accent-bright": "#C2C6C6",
      "--cs-accent-on": "#191D1E",
      "--cs-accent-tint": "#EFF1F1",
      "--cs-accent-ink": "#191D1E",
      "--cs-accent-glow": "rgba(159, 163, 163, 0.5)",
      "--cs-accent-grad-a": "#9FA3A3",
      "--cs-accent-grad-b": "#BFB29B",
      "--cs-color-text-accent": "#565C5E",
      "--cs-color-link": "#565C5E"
    },
    "kim.titanium": {
      "--cs-accent": "#8A7F72",
      "--cs-accent-strong": "#56504A",
      "--cs-accent-bright": "#D9CFC0",
      "--cs-accent-on": "#171310",
      "--cs-accent-tint": "#EFEBE5",
      "--cs-accent-ink": "#171310",
      "--cs-accent-glow": "rgba(138, 127, 114, 0.5)",
      "--cs-accent-grad-a": "#8A7F72",
      "--cs-accent-grad-b": "#BFB29B",
      "--cs-color-text-accent": "#56504A",
      "--cs-color-link": "#56504A"
    }
  },
  "expressions": {
    "solid": {
      "--cs-radius-md": "6px",
      "--cs-radius-lg": "10px",
      "--cs-component-button-radius": "6px",
      "--cs-shadow-xs": "0 0 0 rgba(0,0,0,0)",
      "--cs-shadow-sm": "0 1px 2px rgba(69,33,14,0.05)",
      "--cs-shadow-md": "0 2px 6px rgba(69,33,14,0.08)",
      "--cs-shadow-lg": "0 4px 12px rgba(69,33,14,0.10)",
      "--cs-shadow-xl": "0 8px 24px rgba(69,33,14,0.12)",
      "--cs-glass-whisper-blur": "0px",
      "--cs-glass-light-blur": "0px",
      "--cs-glass-standard-blur": "0px",
      "--cs-glass-heavy-blur": "0px",
      "--cs-glass-whisper-opacity": "100%",
      "--cs-glass-light-opacity": "100%",
      "--cs-glass-standard-opacity": "100%",
      "--cs-glass-heavy-opacity": "100%"
    },
    "paper": {
      "--cs-radius-sm": "3px",
      "--cs-radius-md": "4px",
      "--cs-radius-lg": "6px",
      "--cs-component-button-radius": "4px",
      "--cs-shadow-xs": "0 0 0 rgba(0,0,0,0)",
      "--cs-shadow-sm": "0 0 0 rgba(0,0,0,0)",
      "--cs-shadow-md": "0 0 0 rgba(0,0,0,0)",
      "--cs-shadow-lg": "0 0 0 rgba(0,0,0,0)",
      "--cs-shadow-xl": "0 0 0 rgba(0,0,0,0)",
      "--cs-glass-whisper-blur": "0px",
      "--cs-glass-light-blur": "0px",
      "--cs-glass-standard-blur": "0px",
      "--cs-glass-heavy-blur": "0px",
      "--cs-glass-whisper-opacity": "100%",
      "--cs-glass-light-opacity": "100%",
      "--cs-glass-standard-opacity": "100%",
      "--cs-glass-heavy-opacity": "100%",
      "--cs-duration-base": "160ms",
      "--cs-duration-slow": "240ms"
    },
    "soft": {
      "--cs-radius-sm": "6px",
      "--cs-radius-md": "12px",
      "--cs-radius-lg": "22px",
      "--cs-component-button-radius": "12px",
      "--cs-shadow-xs": "0 2px 6px rgba(69,33,14,0.05)",
      "--cs-shadow-sm": "0 6px 18px rgba(69,33,14,0.06), 0 16px 40px rgba(69,33,14,0.07)",
      "--cs-shadow-md": "0 18px 44px rgba(31,19,11,0.14)",
      "--cs-shadow-lg": "0 26px 70px rgba(69,33,14,0.14)",
      "--cs-shadow-xl": "0 30px 90px rgba(31,19,11,0.20)",
      "--cs-duration-base": "240ms",
      "--cs-duration-slow": "380ms"
    },
    "dense": {
      "--cs-radius-sm": "3px",
      "--cs-radius-md": "6px",
      "--cs-radius-lg": "10px",
      "--cs-component-button-radius": "6px",
      "--cs-component-button-sm-paddingY": "5px",
      "--cs-component-button-sm-paddingX": "10px",
      "--cs-component-button-sm-minHeight": "44px",
      "--cs-component-button-md-paddingY": "12px",
      "--cs-component-button-md-paddingX": "14px",
      "--cs-component-button-md-minHeight": "44px",
      "--cs-component-button-lg-paddingY": "10px",
      "--cs-component-button-lg-paddingX": "18px",
      "--cs-component-button-lg-minHeight": "48px",
      "--cs-component-textfield-paddingY": "10px",
      "--cs-component-textfield-paddingX": "10px",
      "--cs-component-textfield-minHeight": "44px",
      "--cs-shadow-sm": "0 1px 2px rgba(69,33,14,0.05)",
      "--cs-shadow-md": "0 2px 8px rgba(69,33,14,0.10)",
      "--cs-glass-whisper-blur": "0px",
      "--cs-glass-light-blur": "0px",
      "--cs-glass-standard-blur": "0px",
      "--cs-glass-heavy-blur": "0px",
      "--cs-glass-whisper-opacity": "100%",
      "--cs-glass-light-opacity": "100%",
      "--cs-glass-standard-opacity": "100%",
      "--cs-glass-heavy-opacity": "100%",
      "--cs-duration-fast": "100ms",
      "--cs-duration-base": "150ms"
    },
    "luxe": {
      "--cs-radius-sm": "6px",
      "--cs-radius-md": "10px",
      "--cs-radius-lg": "18px",
      "--cs-component-button-radius": "999px",
      "--cs-color-border-default": "#EEE3D0",
      "--cs-shadow-xs": "0 2px 10px rgba(69,33,14,0.06)",
      "--cs-shadow-sm": "0 10px 34px rgba(69,33,14,0.10)",
      "--cs-shadow-md": "0 22px 60px rgba(31,19,11,0.16)",
      "--cs-shadow-lg": "0 32px 90px rgba(69,33,14,0.18)",
      "--cs-shadow-xl": "0 40px 120px rgba(31,19,11,0.24)",
      "--cs-glass-whisper-blur": "12px",
      "--cs-glass-light-blur": "18px",
      "--cs-glass-standard-blur": "24px",
      "--cs-glass-heavy-blur": "32px",
      "--cs-glass-whisper-saturate": "120%",
      "--cs-glass-light-saturate": "140%",
      "--cs-glass-standard-saturate": "160%",
      "--cs-glass-heavy-saturate": "190%",
      "--cs-duration-base": "280ms",
      "--cs-duration-slow": "460ms"
    },
    "bold": {
      "--cs-radius-sm": "2px",
      "--cs-radius-md": "4px",
      "--cs-radius-lg": "6px",
      "--cs-component-button-radius": "4px",
      "--cs-color-border-default": "#45210E",
      "--cs-component-textfield-border-default": "#45210E",
      "--cs-shadow-xs": "2px 2px 0 #45210E",
      "--cs-shadow-sm": "3px 3px 0 #45210E",
      "--cs-shadow-md": "4px 4px 0 #45210E",
      "--cs-shadow-lg": "6px 6px 0 #45210E",
      "--cs-shadow-xl": "8px 8px 0 #45210E",
      "--cs-glass-whisper-blur": "0px",
      "--cs-glass-light-blur": "0px",
      "--cs-glass-standard-blur": "0px",
      "--cs-glass-heavy-blur": "0px",
      "--cs-glass-whisper-opacity": "100%",
      "--cs-glass-light-opacity": "100%",
      "--cs-glass-standard-opacity": "100%",
      "--cs-glass-heavy-opacity": "100%",
      "--cs-duration-fast": "100ms",
      "--cs-duration-base": "140ms"
    }
  }
};
Object.assign(__ds_scope, { tokens, __ds_default_tokens_tokens_padim4: tokens });
})(); } catch (e) { __ds_ns.__errors.push({ path: "tokens/tokens.js", error: String((e && e.message) || e) }); }

// ui_kits/status-hub/StatusHub.jsx
try { (() => {
const {
  useState,
  useMemo
} = React;
const ICON = {
  search: "M11 4a7 7 0 1 0 4.2 12.6L20 21l1.4-1.4-4.4-4.4A7 7 0 0 0 11 4zm0 2a5 5 0 1 1 0 10 5 5 0 0 1 0-10z",
  close: "M6 6l12 12M18 6L6 18",
  sun: null,
  moon: null
};
function Glyph({
  d,
  size = 18
}) {
  return /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.9",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("path", {
    d: d
  }));
}
function SearchGlyph() {
  return /*#__PURE__*/React.createElement("svg", {
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "currentColor",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("path", {
    d: ICON.search
  }));
}
function ThemeGlyph({
  dark
}) {
  return dark ? /*#__PURE__*/React.createElement("svg", {
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "4"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M12 2v2M12 20v2M2 12h2M20 12h2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M19.1 4.9l-1.4 1.4M6.3 17.7l-1.4 1.4"
  })) : /*#__PURE__*/React.createElement("svg", {
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M20 13.5A8 8 0 1 1 10.5 4a6.5 6.5 0 0 0 9.5 9.5z"
  }));
}
const STATUS_LABEL = {
  done: "Shipped",
  active: "In progress",
  hold: "Blocked",
  todo: "Planned"
};
function Segments({
  seg,
  className
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: className
  }, seg.done > 0 && /*#__PURE__*/React.createElement("i", {
    className: "seg-done",
    style: {
      width: seg.done + "%"
    }
  }), seg.active > 0 && /*#__PURE__*/React.createElement("i", {
    className: "seg-active",
    style: {
      width: seg.active + "%"
    }
  }), seg.hold > 0 && /*#__PURE__*/React.createElement("i", {
    className: "seg-hold",
    style: {
      width: seg.hold + "%"
    }
  }), seg.todo > 0 && /*#__PURE__*/React.createElement("i", {
    className: "seg-todo",
    style: {
      width: seg.todo + "%"
    }
  }));
}
function Drawer({
  p,
  onClose
}) {
  if (!p) return null;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "scrim",
    onClick: onClose
  }), /*#__PURE__*/React.createElement("aside", {
    className: "drawer",
    role: "dialog",
    "aria-modal": "true",
    "aria-label": p.name
  }, /*#__PURE__*/React.createElement("div", {
    className: "dw-h"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row"
  }, /*#__PURE__*/React.createElement("span", {
    className: "mono muted",
    style: {
      fontWeight: 700
    }
  }, p.key), /*#__PURE__*/React.createElement("span", {
    className: "pill " + p.status
  }, STATUS_LABEL[p.status]), /*#__PURE__*/React.createElement("button", {
    className: "dw-x",
    onClick: onClose,
    "aria-label": "Close"
  }, /*#__PURE__*/React.createElement(Glyph, {
    d: ICON.close,
    size: 20
  }))), /*#__PURE__*/React.createElement("h2", null, p.name)), /*#__PURE__*/React.createElement("div", {
    className: "dw-b"
  }, /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: {
      marginTop: 0
    }
  }, p.blurb), /*#__PURE__*/React.createElement("dl", {
    className: "meta"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("dt", null, "Owner"), /*#__PURE__*/React.createElement("dd", null, p.owner)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("dt", null, "Squad"), /*#__PURE__*/React.createElement("dd", null, p.squad)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("dt", null, "Progress"), /*#__PURE__*/React.createElement("dd", null, p.pct, "%")), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("dt", null, "Updated"), /*#__PURE__*/React.createElement("dd", null, p.updated))), /*#__PURE__*/React.createElement(Segments, {
    seg: p.seg,
    className: "minibar"
  }), /*#__PURE__*/React.createElement("div", {
    className: "sect"
  }, /*#__PURE__*/React.createElement("h3", null, "Tasks"), /*#__PURE__*/React.createElement("div", {
    className: "task-chips"
  }, p.tasks.map(t => /*#__PURE__*/React.createElement("span", {
    key: t[0],
    className: "chip " + t[2],
    title: t[1]
  }, t[0]))), /*#__PURE__*/React.createElement("ul", {
    className: "tasks"
  }, p.tasks.map(t => /*#__PURE__*/React.createElement("li", {
    key: t[0]
  }, t[1], " \u2014 ", /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, STATUS_LABEL[t[2]]))))), p.risk ? /*#__PURE__*/React.createElement("div", {
    className: "sect"
  }, /*#__PURE__*/React.createElement("h3", null, "Risk"), /*#__PURE__*/React.createElement("div", {
    className: "risk"
  }, p.risk)) : null)));
}
function StatusHub() {
  const D = window.SH_DATA;
  const [dark, setDark] = useState(false);
  const [lens, setLens] = useState("board");
  const [q, setQ] = useState("");
  const [facet, setFacet] = useState("all");
  const [kpi, setKpi] = useState(null);
  const [sel, setSel] = useState(null);
  const counts = useMemo(() => {
    const c = {
      active: 0,
      done: 0,
      hold: 0
    };
    D.projects.forEach(p => {
      if (c[p.status] != null) c[p.status]++;
    });
    const avg = Math.round(D.projects.reduce((a, p) => a + p.pct, 0) / D.projects.length);
    return {
      total: D.projects.length,
      ...c,
      avg
    };
  }, [D]);
  const shown = useMemo(() => {
    const needle = q.trim().toLowerCase();
    const eff = kpi || (facet === "all" ? null : facet);
    return D.projects.filter(p => {
      if (eff && p.status !== eff) return false;
      if (!needle) return true;
      return (p.name + " " + p.key + " " + p.blurb).toLowerCase().includes(needle);
    });
  }, [D, q, facet, kpi]);
  const maxV = Math.max(...D.velocity.map(v => v.n));
  const toggleKpi = k => setKpi(cur => cur === k ? null : k);
  return /*#__PURE__*/React.createElement("div", {
    className: "sh",
    "data-theme": dark ? "dark" : undefined
  }, /*#__PURE__*/React.createElement("header", {
    className: "hd"
  }, /*#__PURE__*/React.createElement("div", {
    className: "hd-in"
  }, /*#__PURE__*/React.createElement("div", {
    className: "hd-id"
  }, /*#__PURE__*/React.createElement("span", {
    className: "hd-mark"
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/logo-mark.svg",
    alt: ""
  })), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h1", null, "Status Hub"), /*#__PURE__*/React.createElement("p", {
    className: "hd-sub"
  }, "CyberSkill \xB7 one page, three lenses \xB7 Hi\u1EC7n Th\u1EF1c Ho\xE1 \xDD Ch\xED"))), /*#__PURE__*/React.createElement("div", {
    className: "hd-side"
  }, /*#__PURE__*/React.createElement("p", {
    className: "hd-meta"
  }, "Updated", /*#__PURE__*/React.createElement("br", null), D.updated), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost",
    onClick: () => setDark(v => !v),
    "aria-label": "Toggle theme"
  }, /*#__PURE__*/React.createElement(ThemeGlyph, {
    dark: dark
  }))))), /*#__PURE__*/React.createElement("div", {
    className: "wrap"
  }, /*#__PURE__*/React.createElement("div", {
    className: "deck"
  }, /*#__PURE__*/React.createElement("section", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h2", null, "Portfolio"), /*#__PURE__*/React.createElement("div", {
    className: "kpis"
  }, /*#__PURE__*/React.createElement("button", {
    className: "kpi",
    "data-on": kpi === null ? "1" : "0",
    onClick: () => setKpi(null)
  }, /*#__PURE__*/React.createElement("b", null, counts.total), /*#__PURE__*/React.createElement("span", null, "Projects")), /*#__PURE__*/React.createElement("button", {
    className: "kpi",
    "data-on": kpi === "active" ? "1" : "0",
    onClick: () => toggleKpi("active")
  }, /*#__PURE__*/React.createElement("b", null, counts.active), /*#__PURE__*/React.createElement("span", null, "In progress")), /*#__PURE__*/React.createElement("button", {
    className: "kpi",
    "data-on": kpi === "done" ? "1" : "0",
    onClick: () => toggleKpi("done")
  }, /*#__PURE__*/React.createElement("b", null, counts.done), /*#__PURE__*/React.createElement("span", null, "Shipped")), /*#__PURE__*/React.createElement("button", {
    className: "kpi",
    "data-on": kpi === "hold" ? "1" : "0",
    onClick: () => toggleKpi("hold")
  }, /*#__PURE__*/React.createElement("b", null, counts.hold), /*#__PURE__*/React.createElement("span", null, "Blocked")), /*#__PURE__*/React.createElement("button", {
    className: "kpi",
    onClick: () => setKpi(null)
  }, /*#__PURE__*/React.createElement("b", null, counts.avg, "%"), /*#__PURE__*/React.createElement("span", null, "Avg progress"))), /*#__PURE__*/React.createElement(Segments, {
    seg: {
      done: 60,
      active: 22,
      hold: 6,
      todo: 12
    },
    className: "bar-seg"
  }), /*#__PURE__*/React.createElement("div", {
    className: "legend"
  }, /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("i", {
    className: "dot",
    style: {
      background: "var(--cs-color-semantic-success)"
    }
  }), "Shipped"), /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("i", {
    className: "dot",
    style: {
      background: "var(--cs-color-brand-ochre)"
    }
  }), "In progress"), /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("i", {
    className: "dot",
    style: {
      background: "var(--cs-color-semantic-danger)"
    }
  }), "Blocked"), /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("i", {
    className: "dot",
    style: {
      background: "var(--cs-status-todo)"
    }
  }), "Planned"))), /*#__PURE__*/React.createElement("section", {
    className: "panel"
  }, /*#__PURE__*/React.createElement("h2", null, "Velocity \xB7 tasks shipped / week"), /*#__PURE__*/React.createElement("div", {
    className: "spark"
  }, D.velocity.map(v => /*#__PURE__*/React.createElement("a", {
    key: v.wk,
    title: v.n + " shipped"
  }, /*#__PURE__*/React.createElement("b", null, v.n), /*#__PURE__*/React.createElement("i", {
    style: {
      height: v.n / maxV * 72 + "px"
    }
  }), /*#__PURE__*/React.createElement("span", null, v.wk)))))), /*#__PURE__*/React.createElement("div", {
    className: "now"
  }, /*#__PURE__*/React.createElement("h2", null, "Now shipping"), /*#__PURE__*/React.createElement("p", null, /*#__PURE__*/React.createElement("b", null, "v1.6.0 \u2014 Glass bindings + style-pack v2."), " Opt-in ", /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, ".cs-surface-*"), " component bindings, a zero-warning verifier across all 50 packs, and a dark-mode APCA re-check. Ships this week.")), /*#__PURE__*/React.createElement("div", {
    className: "bar"
  }, /*#__PURE__*/React.createElement("div", {
    className: "bar-top"
  }, /*#__PURE__*/React.createElement("label", {
    className: "srch"
  }, /*#__PURE__*/React.createElement(SearchGlyph, null), /*#__PURE__*/React.createElement("input", {
    placeholder: "Search projects\u2026",
    value: q,
    onChange: e => setQ(e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    className: "lenses",
    role: "tablist",
    "aria-label": "View"
  }, ["board", "table", "releases"].map(l => /*#__PURE__*/React.createElement("button", {
    key: l,
    className: "ln",
    role: "tab",
    "aria-selected": lens === l,
    onClick: () => setLens(l)
  }, l[0].toUpperCase() + l.slice(1)))), /*#__PURE__*/React.createElement("label", {
    className: "facet"
  }, "Status", /*#__PURE__*/React.createElement("select", {
    value: kpi || facet,
    onChange: e => {
      setFacet(e.target.value);
      setKpi(null);
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: "all"
  }, "All"), /*#__PURE__*/React.createElement("option", {
    value: "active"
  }, "In progress"), /*#__PURE__*/React.createElement("option", {
    value: "done"
  }, "Shipped"), /*#__PURE__*/React.createElement("option", {
    value: "hold"
  }, "Blocked"))), /*#__PURE__*/React.createElement("span", {
    className: "cnt"
  }, shown.length, " of ", D.projects.length))), lens === "board" && /*#__PURE__*/React.createElement("div", {
    className: "grid"
  }, shown.map(p => /*#__PURE__*/React.createElement("button", {
    className: "card" + (p.status === "active" ? " hot" : ""),
    key: p.key,
    onClick: () => setSel(p)
  }, /*#__PURE__*/React.createElement("div", {
    className: "card-h"
  }, /*#__PURE__*/React.createElement("h3", null, /*#__PURE__*/React.createElement("span", {
    className: "k mono"
  }, p.key), " \xB7 ", p.name), /*#__PURE__*/React.createElement("span", {
    className: "pct"
  }, p.pct, "%")), /*#__PURE__*/React.createElement("p", null, p.blurb), /*#__PURE__*/React.createElement(Segments, {
    seg: p.seg,
    className: "minibar"
  }), /*#__PURE__*/React.createElement("div", {
    className: "task-chips"
  }, /*#__PURE__*/React.createElement("span", {
    className: "pill " + p.status
  }, STATUS_LABEL[p.status]), p.tasks.slice(0, 3).map(t => /*#__PURE__*/React.createElement("span", {
    key: t[0],
    className: "chip " + t[2]
  }, t[0])))))), lens === "table" && /*#__PURE__*/React.createElement("div", {
    className: "tbl-wrap"
  }, /*#__PURE__*/React.createElement("table", {
    className: "tbl"
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Key"), /*#__PURE__*/React.createElement("th", null, "Project"), /*#__PURE__*/React.createElement("th", null, "Squad"), /*#__PURE__*/React.createElement("th", null, "Owner"), /*#__PURE__*/React.createElement("th", null, "Progress"), /*#__PURE__*/React.createElement("th", null, "Status"))), /*#__PURE__*/React.createElement("tbody", null, shown.map(p => /*#__PURE__*/React.createElement("tr", {
    key: p.key,
    onClick: () => setSel(p)
  }, /*#__PURE__*/React.createElement("td", {
    className: "mono",
    style: {
      fontWeight: 700
    }
  }, p.key), /*#__PURE__*/React.createElement("td", null, p.name), /*#__PURE__*/React.createElement("td", null, p.squad), /*#__PURE__*/React.createElement("td", null, p.owner), /*#__PURE__*/React.createElement("td", null, p.pct, "%"), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("span", {
    className: "pill " + p.status
  }, STATUS_LABEL[p.status]))))))), lens === "releases" && /*#__PURE__*/React.createElement("div", {
    className: "rels"
  }, D.releases.map(r => /*#__PURE__*/React.createElement("div", {
    className: "rel" + (r.now ? " now" : ""),
    key: r.ver
  }, /*#__PURE__*/React.createElement("div", {
    className: "tick"
  }, r.done ? "✓" : "→"), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "rel-h"
  }, /*#__PURE__*/React.createElement("b", null, r.ver), /*#__PURE__*/React.createElement("span", {
    className: "pill " + (r.now ? "active" : "done")
  }, r.now ? "This week" : "Shipped"), /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, r.when)), /*#__PURE__*/React.createElement("div", {
    className: "rel-sec"
  }, /*#__PURE__*/React.createElement("h4", null, r.title), /*#__PURE__*/React.createElement("ul", null, r.items.map((it, i) => /*#__PURE__*/React.createElement("li", {
    key: i
  }, it)))))))), /*#__PURE__*/React.createElement("p", {
    className: "ft"
  }, "CyberSkill Status Hub \u2014 a design-system UI kit recreation. Built on ", /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, "styles.css"), " tokens. Turn Your Will Into Real.")), /*#__PURE__*/React.createElement(Drawer, {
    p: sel,
    onClose: () => setSel(null)
  }));
}
window.StatusHub = StatusHub;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/status-hub/StatusHub.jsx", error: String((e && e.message) || e) }); }

// ui_kits/status-hub/data.js
try { (() => {
// CyberSkill Status Hub — sample data (fictional, on-brand). window.SH_DATA
window.SH_DATA = {
  updated: "18 Jul 2026, 13:20 ICT",
  velocity: [{
    wk: "W23",
    n: 4
  }, {
    wk: "W24",
    n: 7
  }, {
    wk: "W25",
    n: 5
  }, {
    wk: "W26",
    n: 9
  }, {
    wk: "W27",
    n: 6
  }, {
    wk: "W28",
    n: 11
  }, {
    wk: "W29",
    n: 8
  }],
  projects: [{
    key: "CDS",
    name: "Design System v1.6",
    status: "active",
    pct: 72,
    blurb: "Liquid Glass component bindings + 50 style packs.",
    seg: {
      done: 62,
      active: 20,
      hold: 4,
      todo: 14
    },
    owner: "Design Lead",
    updated: "2h ago",
    squad: "Platform",
    tasks: [["TASK-DS-041", "Glass surface bindings", "done"], ["TASK-DS-047", "Style-pack verifier v2", "active"], ["TASK-DS-052", "Dark-mode APCA re-check", "active"], ["TASK-DS-055", "Figma variable sync", "todo"]],
    risk: ""
  }, {
    key: "GENIE",
    name: "Lumi Genie Chat",
    status: "active",
    pct: 58,
    blurb: "Keyless Claude proxy; scripted wish-capture flow.",
    seg: {
      done: 48,
      active: 24,
      hold: 8,
      todo: 20
    },
    owner: "AI Lead",
    updated: "5h ago",
    squad: "AI",
    tasks: [["TASK-CHAR-026", "In-chat wish flow", "done"], ["TASK-CHAR-030", "One-time mascot hint", "active"], ["TASK-CHAR-021", "Golden-genie GLB", "hold"]],
    risk: "GLB art asset is a hard external dependency — commission not yet confirmed (TASK-CHAR-021)."
  }, {
    key: "SITE",
    name: "cyberskill.world",
    status: "active",
    pct: 84,
    blurb: "Bilingual storytelling landing; lead capture live.",
    seg: {
      done: 78,
      active: 12,
      hold: 0,
      todo: 10
    },
    owner: "Web Lead",
    updated: "1h ago",
    squad: "Growth",
    tasks: [["TASK-BIZ-003", "Lead pipeline e2e", "done"], ["TASK-SEO-012", "llms.txt route", "done"], ["TASK-CTA-020", "Teardown funnel polish", "active"]],
    risk: ""
  }, {
    key: "OS",
    name: "CyberOS Agent Spine",
    status: "hold",
    pct: 41,
    blurb: "BRAIN memory store + HITL acceptance gates.",
    seg: {
      done: 36,
      active: 8,
      hold: 30,
      todo: 26
    },
    owner: "Eng Lead",
    updated: "1d ago",
    squad: "Platform",
    tasks: [["TASK-OS-018", "Gate runner", "done"], ["TASK-OS-022", "Memory compaction", "hold"], ["TASK-OS-025", "Provenance ledger", "hold"]],
    risk: "Blocked on infra: provenance ledger needs the signing service (target Q1 2027)."
  }, {
    key: "AUDIT",
    name: "DS Audit Framework",
    status: "done",
    pct: 100,
    blurb: "125-criteria vendor-neutral audit; L3 tier held.",
    seg: {
      done: 100,
      active: 0,
      hold: 0,
      todo: 0
    },
    owner: "DesignOps",
    updated: "3d ago",
    squad: "Platform",
    tasks: [["TASK-AUD-009", "Deterministic engine", "done"], ["TASK-AUD-011", "No-regression CI gate", "done"]],
    risk: ""
  }, {
    key: "TOKENS",
    name: "Token Pipeline",
    status: "active",
    pct: 66,
    blurb: "DTCG → CSS/TS/Swift/Kotlin/Flutter/Figma.",
    seg: {
      done: 58,
      active: 22,
      hold: 0,
      todo: 20
    },
    owner: "Eng Lead",
    updated: "6h ago",
    squad: "Platform",
    tasks: [["TASK-TOK-014", "Contrast grid export", "done"], ["TASK-TOK-019", "Flutter target", "active"], ["TASK-TOK-021", "Provenance JSON", "todo"]],
    risk: ""
  }],
  releases: [{
    ver: "v1.6.0",
    when: "This week",
    now: true,
    done: false,
    title: "Glass bindings + style-pack v2",
    items: ["Opt-in .cs-surface-* component bindings", "Style-pack verifier v2 (0 warnings across 50)", "Dark-mode APCA re-check"]
  }, {
    ver: "v1.5.1",
    when: "04 Jul 2026",
    now: false,
    done: true,
    title: "50 style packs ship",
    items: ["All 50 packs promoted to shipped", "Per-pack screenshot regression", "Gallery build script"]
  }, {
    ver: "v1.4.0",
    when: "13 Jun 2026",
    now: false,
    done: true,
    title: "Liquid Glass in code",
    items: ["--cs-glass-* + --cs-depth-* scalars", "glass.css material layer", "Reduced-transparency fallbacks"]
  }, {
    ver: "v1.3.0",
    when: "14 May 2026",
    now: false,
    done: true,
    title: "Brand assets + Logo",
    items: ["Official master mark ships", "React <Logo> from master artwork", "Light becomes default theme"]
  }]
};
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/status-hub/data.js", error: String((e && e.message) || e) }); }

// ui_kits/website/Website.jsx
try { (() => {
const {
  useState,
  useRef,
  useEffect
} = React;
function I({
  name,
  size = 20,
  stroke = 1.9
}) {
  const p = {
    sparkle: "M12 3l1.8 5.4L19 10l-5.2 1.6L12 17l-1.8-5.4L5 10l5.2-1.6z",
    chat: "M4 5h16v11H8l-4 4z",
    check: "M4 12.5l5 5 11-11",
    "arrow-right": "M5 12h14M13 6l6 6-6 6",
    close: "M6 6l12 12M18 6L6 18",
    sun: "M12 8a4 4 0 1 0 0 8 4 4 0 0 0 0-8zM12 2v2M12 20v2M2 12h2M20 12h2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M19.1 4.9l-1.4 1.4M6.3 17.7l-1.4 1.4",
    moon: "M20 13.5A8 8 0 1 1 10.5 4a6.5 6.5 0 0 0 9.5 9.5z"
  }[name];
  return /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: stroke,
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("path", {
    d: p
  }));
}
function Chat({
  t,
  seed,
  onClose
}) {
  const [msgs, setMsgs] = useState([{
    who: "bot",
    text: t.genie.greeting
  }]);
  const [val, setVal] = useState("");
  const bodyRef = useRef(null);
  useEffect(() => {
    if (seed) send(seed);
  }, []);
  useEffect(() => {
    const b = bodyRef.current;
    if (b) b.scrollTop = b.scrollHeight;
  }, [msgs]);
  function send(text) {
    const m = (text || "").trim();
    if (!m) return;
    setMsgs(cur => [...cur, {
      who: "me",
      text: m
    }]);
    setVal("");
    setTimeout(() => setMsgs(cur => [...cur, {
      who: "bot",
      text: "A wish worth granting. I’ll hand it to the team — first, what should I call you?"
    }]), 500);
  }
  return /*#__PURE__*/React.createElement("div", {
    className: "chat"
  }, /*#__PURE__*/React.createElement("div", {
    className: "chat-h"
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/lumi-poster.webp",
    alt: ""
  }), /*#__PURE__*/React.createElement("b", null, t.genie.title), /*#__PURE__*/React.createElement("button", {
    onClick: onClose,
    "aria-label": "Close chat"
  }, /*#__PURE__*/React.createElement(I, {
    name: "close",
    size: 18
  }))), /*#__PURE__*/React.createElement("div", {
    className: "chat-b",
    ref: bodyRef
  }, msgs.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "msg " + m.who
  }, m.text)), msgs.length === 1 && /*#__PURE__*/React.createElement("div", {
    className: "chips"
  }, t.genie.chips.map(c => /*#__PURE__*/React.createElement("button", {
    key: c,
    onClick: () => send(c)
  }, c)))), /*#__PURE__*/React.createElement("div", {
    className: "chat-f"
  }, /*#__PURE__*/React.createElement("input", {
    value: val,
    onChange: e => setVal(e.target.value),
    onKeyDown: e => e.key === "Enter" && send(val),
    placeholder: t.genie.placeholder
  }), /*#__PURE__*/React.createElement("button", {
    onClick: () => send(val)
  }, t.genie.send)));
}
function Website() {
  const [loc, setLoc] = useState("en");
  const [dark, setDark] = useState(false);
  const [chat, setChat] = useState(false);
  const [seed, setSeed] = useState("");
  const [wish, setWish] = useState("");
  const [sent, setSent] = useState(false);
  const t = window.SITE_COPY[loc];
  function openChat(s) {
    setSeed(s || "");
    setChat(true);
  }
  return /*#__PURE__*/React.createElement("div", {
    className: "site",
    "data-theme": dark ? "dark" : undefined
  }, /*#__PURE__*/React.createElement("header", {
    className: "hdr cs-surface-light"
  }, /*#__PURE__*/React.createElement("div", {
    className: "container hdr-in"
  }, /*#__PURE__*/React.createElement("a", {
    className: "brand",
    href: "#top"
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/logo-mark.svg",
    alt: ""
  }), "CyberSkill"), /*#__PURE__*/React.createElement("nav", {
    className: "nav"
  }, t.nav.map((n, i) => /*#__PURE__*/React.createElement("a", {
    key: i,
    href: "#" + ["services", "work", "process", "careers", "contact"][i]
  }, n))), /*#__PURE__*/React.createElement("div", {
    className: "hdr-side"
  }, /*#__PURE__*/React.createElement("button", {
    className: "mini",
    onClick: () => setLoc(loc === "en" ? "vi" : "en")
  }, t.langLabel), /*#__PURE__*/React.createElement("button", {
    className: "mini icon",
    onClick: () => setDark(!dark),
    "aria-label": "Toggle theme"
  }, /*#__PURE__*/React.createElement(I, {
    name: dark ? "sun" : "moon",
    size: 18
  })), /*#__PURE__*/React.createElement("button", {
    className: "mini",
    style: {
      background: "var(--cs-color-brand-umber)",
      color: "#fff",
      borderColor: "transparent"
    },
    onClick: () => openChat("")
  }, t.talk)))), /*#__PURE__*/React.createElement("section", {
    className: "hero",
    id: "top"
  }, /*#__PURE__*/React.createElement("div", {
    className: "container hero-in"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, t.eyebrow), /*#__PURE__*/React.createElement("h1", null, t.slogan), /*#__PURE__*/React.createElement("div", {
    className: "vn"
  }, t.sub), /*#__PURE__*/React.createElement("p", {
    className: "lead"
  }, t.lead), /*#__PURE__*/React.createElement("div", {
    className: "wish"
  }, /*#__PURE__*/React.createElement("input", {
    value: wish,
    onChange: e => setWish(e.target.value),
    onKeyDown: e => e.key === "Enter" && openChat(wish),
    placeholder: t.wishPlaceholder,
    "aria-label": t.wishPlaceholder
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn-gold",
    onClick: () => openChat(wish)
  }, /*#__PURE__*/React.createElement(I, {
    name: "sparkle",
    size: 18
  }), t.wishCta), /*#__PURE__*/React.createElement("button", {
    className: "btn-ghost-light",
    onClick: () => openChat("")
  }, t.talk))), /*#__PURE__*/React.createElement("div", {
    className: "hero-art"
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/lumi-poster.webp",
    alt: "Lumi, the golden genie"
  })))), /*#__PURE__*/React.createElement("div", {
    className: "marq"
  }, /*#__PURE__*/React.createElement("div", {
    className: "container marq-in"
  }, t.marquee.map((m, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: i
  }, i > 0 && /*#__PURE__*/React.createElement("b", null, "\xB7"), /*#__PURE__*/React.createElement("span", null, m))))), /*#__PURE__*/React.createElement("section", {
    className: "sec",
    id: "services"
  }, /*#__PURE__*/React.createElement("div", {
    className: "container"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, t.nav[0]), /*#__PURE__*/React.createElement("h2", null, t.servicesTitle), /*#__PURE__*/React.createElement("p", {
    className: "sec-lead"
  }, t.servicesLead), /*#__PURE__*/React.createElement("div", {
    className: "svc-grid"
  }, t.services.map((s, i) => /*#__PURE__*/React.createElement("article", {
    className: "svc cs-surface-standard",
    key: i
  }, /*#__PURE__*/React.createElement("div", {
    className: "svc-ic"
  }, /*#__PURE__*/React.createElement(I, {
    name: s.icon
  })), /*#__PURE__*/React.createElement("h3", null, s.t), /*#__PURE__*/React.createElement("p", null, s.s), /*#__PURE__*/React.createElement("ul", null, s.o.map(o => /*#__PURE__*/React.createElement("li", {
    key: o
  }, o)))))))), /*#__PURE__*/React.createElement("section", {
    className: "sec sec-alt",
    id: "process"
  }, /*#__PURE__*/React.createElement("div", {
    className: "container"
  }, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, t.nav[2]), /*#__PURE__*/React.createElement("h2", null, t.processTitle), /*#__PURE__*/React.createElement("p", {
    className: "sec-lead"
  }, t.processLead), /*#__PURE__*/React.createElement("div", {
    className: "steps"
  }, t.steps.map(s => /*#__PURE__*/React.createElement("div", {
    className: "step",
    key: s.n
  }, /*#__PURE__*/React.createElement("span", {
    className: "n"
  }, s.n), /*#__PURE__*/React.createElement("h3", null, s.t), /*#__PURE__*/React.createElement("p", null, s.s)))))), /*#__PURE__*/React.createElement("section", {
    className: "sec",
    id: "careers"
  }, /*#__PURE__*/React.createElement("div", {
    className: "container"
  }, /*#__PURE__*/React.createElement("div", {
    className: "band"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h2", null, t.careersTitle), /*#__PURE__*/React.createElement("p", null, t.careersLead)), /*#__PURE__*/React.createElement("button", {
    className: "btn-gold",
    onClick: () => openChat("")
  }, t.careersCta, /*#__PURE__*/React.createElement(I, {
    name: "arrow-right",
    size: 18
  }))))), /*#__PURE__*/React.createElement("section", {
    className: "sec sec-alt",
    id: "contact"
  }, /*#__PURE__*/React.createElement("div", {
    className: "container contact-grid"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "eyebrow"
  }, t.nav[4]), /*#__PURE__*/React.createElement("h2", null, t.contactTitle), /*#__PURE__*/React.createElement("p", {
    className: "sec-lead",
    style: {
      marginBottom: 16
    }
  }, t.contactLead), /*#__PURE__*/React.createElement("p", {
    className: "trust"
  }, /*#__PURE__*/React.createElement(I, {
    name: "check",
    size: 16
  }), t.form.trust)), sent ? /*#__PURE__*/React.createElement("div", {
    className: "sent"
  }, /*#__PURE__*/React.createElement(I, {
    name: "check",
    size: 18
  }), " ", loc === "vi" ? "Đã nhận được lời nhắn. Cảm ơn bạn!" : "Message received. Thank you!") : /*#__PURE__*/React.createElement("form", {
    className: "form",
    onSubmit: e => {
      e.preventDefault();
      setSent(true);
    }
  }, /*#__PURE__*/React.createElement("label", {
    className: "cs-field"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cs-field__label"
  }, t.form.name), /*#__PURE__*/React.createElement("input", {
    className: "cs-field__control",
    required: true
  })), /*#__PURE__*/React.createElement("label", {
    className: "cs-field"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cs-field__label"
  }, t.form.email), /*#__PURE__*/React.createElement("input", {
    className: "cs-field__control",
    type: "email",
    required: true
  })), /*#__PURE__*/React.createElement("label", {
    className: "cs-field"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cs-field__label"
  }, t.form.company, " ", /*#__PURE__*/React.createElement("span", {
    className: "muted",
    style: {
      fontWeight: 400,
      color: "var(--cs-color-text-muted)"
    }
  }, "\xB7 ", t.form.optional)), /*#__PURE__*/React.createElement("input", {
    className: "cs-field__control"
  })), /*#__PURE__*/React.createElement("label", {
    className: "cs-field"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cs-field__label"
  }, t.form.message), /*#__PURE__*/React.createElement("textarea", {
    className: "cs-field__control",
    rows: "3"
  })), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("button", {
    type: "submit",
    className: "btn-gold"
  }, t.form.submit, /*#__PURE__*/React.createElement(I, {
    name: "arrow-right",
    size: 18
  })))))), /*#__PURE__*/React.createElement("footer", {
    className: "ft"
  }, /*#__PURE__*/React.createElement("div", {
    className: "container ft-in"
  }, /*#__PURE__*/React.createElement("a", {
    className: "brand",
    href: "#top"
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/logo-mark.svg",
    alt: ""
  }), "CyberSkill"), /*#__PURE__*/React.createElement("div", {
    className: "ft-links"
  }, t.footerLinks.map(l => /*#__PURE__*/React.createElement("a", {
    key: l,
    href: "#top"
  }, l))), /*#__PURE__*/React.createElement("small", null, "\xA9 2026 CyberSkill \xB7 ", t.footerRights, " \xB7 Saigon"))), !chat && /*#__PURE__*/React.createElement("button", {
    className: "fab",
    onClick: () => openChat("")
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/lumi-poster.webp",
    alt: ""
  }), t.talk), chat && /*#__PURE__*/React.createElement(Chat, {
    t: t,
    seed: seed,
    onClose: () => setChat(false)
  }));
}
window.Website = Website;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/Website.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/copy.js
try { (() => {
// cyberskill.world — real bilingual copy (EN + VN) lifted from the product
// i18n dictionaries + content. window.SITE_COPY[locale]. Vietnamese-first.
window.SITE_COPY = {
  en: {
    nav: ["Services", "Work", "Process", "Careers", "Contact"],
    langLabel: "Tiếng Việt",
    eyebrow: "A Saigon studio that turns will into working software. Since 2020.",
    slogan: "Turn Your Will Into Real",
    sub: "Hiện Thực Hoá Ý Chí",
    lead: "Lumi, our golden genie, takes a clear wish and helps the human team turn it into software your people actually run — built to ship, and built to last.",
    wishPlaceholder: "Make a wish… a faster checkout, an app, a tool",
    wishCta: "Make a wish",
    talk: "Talk to Lumi",
    marquee: ["Web apps", "Mobile apps", "Internal systems", "Automation", "Design systems", "Built to last"],
    servicesTitle: "What we build",
    servicesLead: "Three focused practices. One team that answers for the result.",
    services: [{
      icon: "sparkle",
      t: "Web & mobile apps",
      s: "Product apps your customers reach for — fast, accessible, bilingual by default.",
      o: ["Web apps", "Mobile apps", "Design systems"]
    }, {
      icon: "chat",
      t: "Internal systems & automation",
      s: "The tools your team runs on: dashboards, workflows, and the automation behind them.",
      o: ["Internal systems", "Automation"]
    }, {
      icon: "check",
      t: "Built to last",
      s: "Shipped software that keeps running — tested, documented, handed over cleanly.",
      o: ["Testing", "Docs", "Handover"]
    }],
    processTitle: "The arc of a wish",
    processLead: "Every project follows one arc: from a clear wish to software running in the real world.",
    steps: [{
      n: "01",
      t: "Make a wish",
      s: "Tell us the outcome in a sentence or two. We reply within one business day."
    }, {
      n: "02",
      t: "Shape it",
      s: "A small senior team scopes the work and is honest about trade-offs."
    }, {
      n: "03",
      t: "Build to ship",
      s: "Short cycles, real software you can run — not slideware."
    }, {
      n: "04",
      t: "Built to last",
      s: "Tested, documented, handed over. It keeps running after we leave."
    }],
    careersTitle: "Build with us",
    careersLead: "We hire for craft and care — senior, small, and honest about trade-offs.",
    careersCta: "See open roles",
    contactTitle: "Tell us your wish",
    contactLead: "Send a short note. We reply within one business day.",
    form: {
      name: "Your name",
      email: "Work email",
      company: "Company",
      message: "Anything we should know?",
      submit: "Send",
      optional: "optional",
      trust: "Your details go straight to the team. We never share them."
    },
    genie: {
      title: "Lumi · CyberSkill’s golden genie",
      greeting: "Hello, I’m Lumi. Think of me as a friendly guide: tell me a wish, and I’ll share plain advice before asking for anything. When a wish is clear, it goes to a real person on the team.",
      placeholder: "Type your wish…",
      send: "Send",
      chips: ["A web app", "An internal tool", "A design system"]
    },
    footerRights: "All rights reserved.",
    footerLinks: ["Privacy", "Accessibility", "Terms"]
  },
  vi: {
    nav: ["Dịch vụ", "Dự án", "Quy trình", "Tuyển dụng", "Liên hệ"],
    langLabel: "English",
    eyebrow: "Studio Sài Gòn. Biến ý chí thành phần mềm chạy thật. Từ năm 2020.",
    slogan: "Hiện Thực Hoá Ý Chí",
    sub: "Turn Your Will Into Real",
    lead: "Lumi, thần đèn vàng của CyberSkill, cùng đội ngũ biến điều ước rõ thành phần mềm đội bạn dùng mỗi ngày: bàn giao được, dùng lâu được.",
    wishPlaceholder: "Ước một điều… thanh toán nhanh hơn, một ứng dụng, một công cụ",
    wishCta: "Ước điều này",
    talk: "Trò chuyện với Lumi",
    marquee: ["Ứng dụng web", "Ứng dụng di động", "Hệ thống nội bộ", "Tự động hóa", "Hệ thống thiết kế", "Bền bỉ lâu dài"],
    servicesTitle: "Chúng tôi xây dựng những gì",
    servicesLead: "Ba mảng chuyên sâu. Một đội chịu trách nhiệm đến kết quả cuối cùng.",
    services: [{
      icon: "sparkle",
      t: "Ứng dụng web & di động",
      s: "Sản phẩm khách hàng dùng mỗi ngày — nhanh, dễ tiếp cận, song ngữ mặc định.",
      o: ["Ứng dụng web", "Di động", "Hệ thống thiết kế"]
    }, {
      icon: "chat",
      t: "Hệ thống nội bộ & tự động hóa",
      s: "Công cụ đội bạn vận hành: bảng điều khiển, quy trình và tự động hóa phía sau.",
      o: ["Hệ thống nội bộ", "Tự động hóa"]
    }, {
      icon: "check",
      t: "Bền bỉ lâu dài",
      s: "Phần mềm bàn giao vẫn chạy tốt — được kiểm thử, tài liệu đầy đủ, bàn giao gọn.",
      o: ["Kiểm thử", "Tài liệu", "Bàn giao"]
    }],
    processTitle: "Hành trình của một điều ước",
    processLead: "Mỗi dự án một hành trình: từ điều ước rõ đến phần mềm chạy giữa đời thật.",
    steps: [{
      n: "01",
      t: "Ước một điều",
      s: "Kể kết quả bạn muốn trong một hai câu. Chúng tôi phản hồi trong một ngày làm việc."
    }, {
      n: "02",
      t: "Định hình",
      s: "Đội nhỏ, kinh nghiệm bóc tách công việc và nói thẳng về đánh đổi."
    }, {
      n: "03",
      t: "Xây để bàn giao",
      s: "Chu kỳ ngắn, phần mềm chạy thật — không phải slide."
    }, {
      n: "04",
      t: "Dùng lâu được",
      s: "Kiểm thử, tài liệu, bàn giao. Vẫn chạy tốt sau khi chúng tôi rời đi."
    }],
    careersTitle: "Cùng chúng tôi kiến tạo",
    careersLead: "Tuyển người có tay nghề và chỉn chu — đội gọn, kinh nghiệm, nói thẳng đánh đổi.",
    careersCta: "Xem vị trí đang mở",
    contactTitle: "Kể chúng tôi nghe điều bạn ước",
    contactLead: "Một lời nhắn ngắn là đủ. Chúng tôi phản hồi trong một ngày làm việc.",
    form: {
      name: "Tên của bạn",
      email: "Email công việc",
      company: "Công ty",
      message: "Bạn muốn chia sẻ thêm điều gì?",
      submit: "Gửi",
      optional: "không bắt buộc",
      trust: "Thông tin của bạn được gửi thẳng đến đội ngũ. Chúng tôi không chia sẻ cho bên thứ ba."
    },
    genie: {
      title: "Lumi · thần đèn vàng CyberSkill",
      greeting: "Chào bạn, mình là Lumi. Cứ kể mình một điều ước; mình tư vấn trước, chỉ xin thông tin khi bạn muốn gửi. Điều ước rõ sẽ được chuyển cho người thật trong đội.",
      placeholder: "Viết điều ước của bạn…",
      send: "Gửi",
      chips: ["Một ứng dụng web", "Công cụ nội bộ", "Hệ thống thiết kế"]
    },
    footerRights: "Bảo lưu mọi quyền.",
    footerLinks: ["Quyền riêng tư", "Khả năng tiếp cận", "Điều khoản"]
  }
};
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/copy.js", error: String((e && e.message) || e) }); }

__ds_ns.AIDisclosureBadge = __ds_scope.AIDisclosureBadge;

__ds_ns.ChatMessage = __ds_scope.ChatMessage;

__ds_ns.CitationList = __ds_scope.CitationList;

__ds_ns.ConfidenceMeter = __ds_scope.ConfidenceMeter;

__ds_ns.HumanReviewGate = __ds_scope.HumanReviewGate;

__ds_ns.PromptInput = __ds_scope.PromptInput;

__ds_ns.PromptSuggestions = __ds_scope.PromptSuggestions;

__ds_ns.TypingIndicator = __ds_scope.TypingIndicator;

__ds_ns.LumiAvatar = __ds_scope.LumiAvatar;

__ds_ns.Button = __ds_scope.Button;

__ds_ns.Accordion = __ds_scope.Accordion;

__ds_ns.Avatar = __ds_scope.Avatar;

__ds_ns.AvatarGroup = __ds_scope.AvatarGroup;

__ds_ns.Card = __ds_scope.Card;

__ds_ns.CardHeader = __ds_scope.CardHeader;

__ds_ns.CardBody = __ds_scope.CardBody;

__ds_ns.CardFooter = __ds_scope.CardFooter;

__ds_ns.CodeBlock = __ds_scope.CodeBlock;

__ds_ns.DescriptionList = __ds_scope.DescriptionList;

__ds_ns.Divider = __ds_scope.Divider;

__ds_ns.Kbd = __ds_scope.Kbd;

__ds_ns.List = __ds_scope.List;

__ds_ns.ListItem = __ds_scope.ListItem;

__ds_ns.Stat = __ds_scope.Stat;

__ds_ns.Timeline = __ds_scope.Timeline;

__ds_ns.Tooltip = __ds_scope.Tooltip;

__ds_ns.DataTable = __ds_scope.DataTable;

__ds_ns.Dialog = __ds_scope.Dialog;

__ds_ns.Alert = __ds_scope.Alert;

__ds_ns.Badge = __ds_scope.Badge;

__ds_ns.EmptyState = __ds_scope.EmptyState;

__ds_ns.ProgressBar = __ds_scope.ProgressBar;

__ds_ns.Skeleton = __ds_scope.Skeleton;

__ds_ns.Spinner = __ds_scope.Spinner;

__ds_ns.StatusIndicator = __ds_scope.StatusIndicator;

__ds_ns.Tag = __ds_scope.Tag;

__ds_ns.ToastStack = __ds_scope.ToastStack;

__ds_ns.Toast = __ds_scope.Toast;

__ds_ns.Checkbox = __ds_scope.Checkbox;

__ds_ns.FileUpload = __ds_scope.FileUpload;

__ds_ns.NumberField = __ds_scope.NumberField;

__ds_ns.Radio = __ds_scope.Radio;

__ds_ns.RadioGroup = __ds_scope.RadioGroup;

__ds_ns.SearchField = __ds_scope.SearchField;

__ds_ns.SegmentedControl = __ds_scope.SegmentedControl;

__ds_ns.Select = __ds_scope.Select;

__ds_ns.Slider = __ds_scope.Slider;

__ds_ns.Switch = __ds_scope.Switch;

__ds_ns.Textarea = __ds_scope.Textarea;

__ds_ns.CS_ICONS = __ds_scope.CS_ICONS;

__ds_ns.Icon = __ds_scope.Icon;

__ds_ns.Logo = __ds_scope.Logo;

__ds_ns.CS_LOGO_VIEWBOX = __ds_scope.CS_LOGO_VIEWBOX;

__ds_ns.CS_LOGO_MARK_INNER = __ds_scope.CS_LOGO_MARK_INNER;

__ds_ns.Breadcrumb = __ds_scope.Breadcrumb;

__ds_ns.CommandPalette = __ds_scope.CommandPalette;

__ds_ns.Menu = __ds_scope.Menu;

__ds_ns.MenuItem = __ds_scope.MenuItem;

__ds_ns.Pagination = __ds_scope.Pagination;

__ds_ns.Sidebar = __ds_scope.Sidebar;

__ds_ns.NavItem = __ds_scope.NavItem;

__ds_ns.Steps = __ds_scope.Steps;

__ds_ns.Tabs = __ds_scope.Tabs;

__ds_ns.Tab = __ds_scope.Tab;

__ds_ns.Drawer = __ds_scope.Drawer;

__ds_ns.Popover = __ds_scope.Popover;

__ds_ns.TextField = __ds_scope.TextField;

})();
